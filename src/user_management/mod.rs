// @generated by apis-code-gen. Do not edit: change the generator or the specification.

//! The UserManagement surface.

pub mod api;
pub mod models;

pub use api::*;
pub use models::*;

/// Every UserManagement operation, grouped the way the API documents them.
///
/// Build the transport once and hand it to each surface: a second client is a second set of OAuth tokens,
/// and whichever refreshes first invalidates the other.
pub struct UserManagementClient {
    client: crate::core::Client,
}

impl UserManagementClient {
    pub fn new(client: crate::core::Client) -> Self {
        Self { client }
    }

    /// The transport underneath, for a request this surface does not describe.
    pub fn client(&self) -> &crate::core::Client {
        &self.client
    }

    /// The Manage operations.
    pub fn manage(&self) -> ManageService<'_> {
        ManageService::new(&self.client)
    }

    /// The Profile operations.
    pub fn profile(&self) -> ProfileService<'_> {
        ProfileService::new(&self.client)
    }

    /// The Email operations.
    pub fn email(&self) -> EmailService<'_> {
        EmailService::new(&self.client)
    }

    /// The ApiTokens operations.
    pub fn api_tokens(&self) -> ApiTokensService<'_> {
        ApiTokensService::new(&self.client)
    }

    /// The Lifecycle operations.
    pub fn lifecycle(&self) -> LifecycleService<'_> {
        LifecycleService::new(&self.client)
    }
}

impl From<crate::core::Client> for UserManagementClient {
    fn from(client: crate::core::Client) -> Self {
        Self::new(client)
    }
}
