// @generated by apis-code-gen. Do not edit: change the generator or the specification.

use super::super::models::*;
use serde::{Deserialize, Serialize};

crate::open_enum! {
    pub enum GetUserRoleAssignmentsRequestRoleIds {
        AtlassianUser => "atlassian/user",
        AtlassianAdmin => "atlassian/admin",
        AtlassianGuest => "atlassian/guest",
        AtlassianCustomer => "atlassian/customer",
        AtlassianUserAccessAdmin => "atlassian/user-access-admin",
        AtlassianContributor => "atlassian/contributor",
        AtlassianBasic => "atlassian/basic",
        AtlassianStakeholder => "atlassian/stakeholder",
        AtlassianOrgAdmin => "atlassian/org-admin",
        AtlassianSiteAdmin => "atlassian/site-admin",
        AtlassianAiAccess => "atlassian/ai-access",
    }
}

crate::open_enum! {
    /// The claim status for the user account. By default, both `managed` and `unmanaged` accounts are returned.
    ///   - `managed` - Returns only managed accounts. For more on managed accounts: https://support.atlassian.com/user-management/docs/what-are-managed-accounts/
    ///   - `unmanaged` - Returns only unmanaged accounts.
    pub enum GetDirectoryUsersCountRequestClaimStatus {
        Managed => "managed",
        Unmanaged => "unmanaged",
    }
}

crate::open_enum! {
    pub enum GetDirectoryUsersCountRequestStatus {
        Active => "active",
        Suspended => "suspended",
        NotInvited => "not_invited",
        Deactivated => "deactivated",
        ForDeletion => "for_deletion",
    }
}

crate::open_enum! {
    pub enum GetDirectoryUsersCountRequestAccountStatus {
        Active => "active",
        Inactive => "inactive",
        Closed => "closed",
    }
}

crate::open_enum! {
    pub enum GetDirectoryUsersCountRequestMembershipStatus {
        Active => "active",
        Suspended => "suspended",
        NoMembership => "no_membership",
    }
}

crate::open_enum! {
    pub enum GetDirectoryUsersCountRequestRoleIds {
        AtlassianUser => "atlassian/user",
        AtlassianAdmin => "atlassian/admin",
        AtlassianGuest => "atlassian/guest",
        AtlassianCustomer => "atlassian/customer",
        AtlassianUserAccessAdmin => "atlassian/user-access-admin",
        AtlassianContributor => "atlassian/contributor",
        AtlassianBasic => "atlassian/basic",
        AtlassianStakeholder => "atlassian/stakeholder",
        AtlassianOrgAdmin => "atlassian/org-admin",
        AtlassianSiteAdmin => "atlassian/site-admin",
        AtlassianAiAccess => "atlassian/ai-access",
    }
}

crate::open_enum! {
    /// The claim status for the user account. By default, both `managed` and `unmanaged` accounts are returned.
    ///   - `managed` - Returns only managed accounts. For more on managed accounts: https://support.atlassian.com/user-management/docs/what-are-managed-accounts/
    ///   - `unmanaged` - Returns only unmanaged accounts.
    pub enum GetDirectoryUsersRequestClaimStatus {
        Managed => "managed",
        Unmanaged => "unmanaged",
    }
}

crate::open_enum! {
    pub enum GetDirectoryUsersRequestStatus {
        Active => "active",
        Suspended => "suspended",
        NotInvited => "not_invited",
        Deactivated => "deactivated",
        ForDeletion => "for_deletion",
    }
}

crate::open_enum! {
    pub enum GetDirectoryUsersRequestAccountStatus {
        Active => "active",
        Inactive => "inactive",
        Closed => "closed",
    }
}

crate::open_enum! {
    pub enum GetDirectoryUsersRequestMembershipStatus {
        Active => "active",
        Suspended => "suspended",
        NoMembership => "no_membership",
    }
}

crate::open_enum! {
    pub enum GetDirectoryUsersRequestRoleIds {
        AtlassianUser => "atlassian/user",
        AtlassianAdmin => "atlassian/admin",
        AtlassianGuest => "atlassian/guest",
        AtlassianCustomer => "atlassian/customer",
        AtlassianUserAccessAdmin => "atlassian/user-access-admin",
        AtlassianContributor => "atlassian/contributor",
        AtlassianBasic => "atlassian/basic",
        AtlassianStakeholder => "atlassian/stakeholder",
        AtlassianOrgAdmin => "atlassian/org-admin",
        AtlassianSiteAdmin => "atlassian/site-admin",
        AtlassianAiAccess => "atlassian/ai-access",
    }
}

crate::open_enum! {
    /// The name of the field to sort the results by.
    pub enum GetDirectoryUsersRequestSortByField {
        NickName => "nick_name",
    }
}

crate::open_enum! {
    /// The direction to sort the results by.
    pub enum GetDirectoryUsersRequestSortByDirection {
        Asc => "asc",
        Desc => "desc",
    }
}

#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct GetDirectoryUsersRequestSortBy {
    /// The name of the field to sort the results by.
    pub field: GetDirectoryUsersRequestSortByField,
    /// The direction to sort the results by.
    pub direction: GetDirectoryUsersRequestSortByDirection,
}

/// The Users operations.
pub struct UsersService<'a> {
    client: &'a crate::core::Client,
}

impl<'a> UsersService<'a> {
    pub(crate) fn new(client: &'a crate::core::Client) -> Self {
        Self { client }
    }

    /// Return a page of users in an organization that match the supplied parameters.
    ///
    /// Use `searchTerm` for free-text search across user display names and email addresses. Use `emails` for exact-match filtering by full email addresses. `searchTerm` and `emails` are mutually exclusive. Providing both in the same request returns `400 Bad Request`. Use the `expand` field to include additional fields such as `platformRoles`, `counts.resources`, `productAccess`, and `groups` in the response.
    pub fn search_directory_users(
        &self,
        org_id: impl Into<String>,
        directory_id: impl Into<String>,
    ) -> SearchDirectoryUsersRequest<'a> {
        SearchDirectoryUsersRequest::new(self.client, org_id, directory_id)
    }

    /// Returns a list of managed accounts in an organization.
    ///
    /// #### Scopes
    /// **[Authorization scopes](/cloud/admin/scopes/) required:** `read:accounts:admin`
    pub fn get_users(&self, org_id: impl Into<String>) -> GetUsersRequest<'a> {
        GetUsersRequest::new(self.client, org_id)
    }

    /// Invite people to your organization. When you invite someone:
    /// - they’re given app roles according to your invitation.
    /// - they’re added to directories based on apps in your invitation.
    /// - they’re added to groups according to your invitation.
    /// - they receive an email invitation if the `sendNotification` field is set to `true` and the `notificationText` field contains a message to include in the email invitation.
    ///
    /// **This API is only available to customers who have at least one paid subscription in their organization.**
    pub fn invite_users(&self, org_id: impl Into<String>) -> InviteUsersRequest<'a> {
        InviteUsersRequest::new(self.client, org_id)
    }

    /// Returns a page of role assignments for a user that match the supplied parameters.
    ///
    /// #### Scopes
    /// **[Authorization scopes](/cloud/admin/scopes/) required:** `read:directories:admin`
    pub fn get_user_role_assignments(
        &self,
        org_id: impl Into<String>,
        directory_id: impl Into<String>,
        account_id: impl Into<String>,
    ) -> GetUserRoleAssignmentsRequest<'a> {
        GetUserRoleAssignmentsRequest::new(self.client, org_id, directory_id, account_id)
    }

    /// This API can be used to grant Platform Roles to a user.
    pub fn grant_user_access(
        &self,
        org_id: impl Into<String>,
        user_id: impl Into<String>,
    ) -> GrantUserAccessRequest<'a> {
        GrantUserAccessRequest::new(self.client, org_id, user_id)
    }

    /// This API can be used to revoke Platform Roles from a user.
    pub fn revoke_user_access(
        &self,
        org_id: impl Into<String>,
        user_id: impl Into<String>,
    ) -> RevokeUserAccessRequest<'a> {
        RevokeUserAccessRequest::new(self.client, org_id, user_id)
    }

    /// Suspend a user’s access in a directory to remove their access to apps temporarily. You’re not billed for a user when their access is suspended. They regain their roles and group memberships when you restore their access.
    pub fn suspend_directory_user(
        &self,
        org_id: impl Into<String>,
        directory_id: impl Into<String>,
        account_id: impl Into<String>,
    ) -> SuspendDirectoryUserRequest<'a> {
        SuspendDirectoryUserRequest::new(self.client, org_id, directory_id, account_id)
    }

    /// Restore a user’s access in a directory to let them access apps again. They regain their roles and group memberships from before their access was suspended. We resume billing you for this user.
    pub fn restore_directory_user(
        &self,
        org_id: impl Into<String>,
        directory_id: impl Into<String>,
        account_id: impl Into<String>,
    ) -> RestoreDirectoryUserRequest<'a> {
        RestoreDirectoryUserRequest::new(self.client, org_id, directory_id, account_id)
    }

    /// Returns detailed information about a specific user in a directory within an organization.
    pub fn get_directory_user_details(
        &self,
        org_id: impl Into<String>,
        directory_id: impl Into<String>,
        account_id: impl Into<String>,
    ) -> GetDirectoryUserDetailsRequest<'a> {
        GetDirectoryUserDetailsRequest::new(self.client, org_id, directory_id, account_id)
    }

    /// Remove a user from a directory if you don’t want them to appear in your directory or have access to your apps anymore. You’re not billed for a user once they’re removed.
    /// You must invite the user to your organization again if you want to reinstate their access to your apps. You’ll need to assign their roles and group memberships again.
    pub fn remove_directory_user(
        &self,
        org_id: impl Into<String>,
        directory_id: impl Into<String>,
        account_id: impl Into<String>,
    ) -> RemoveDirectoryUserRequest<'a> {
        RemoveDirectoryUserRequest::new(self.client, org_id, directory_id, account_id)
    }

    /// Assign an organization-level role to a user. These are roles that have organization-wide privileges, like organization admin.
    ///
    /// This operation follows eventual consistency. Changes may take up to 30 seconds to be reflected after the operation is performed.
    pub fn assign_organization_role(
        &self,
        org_id: impl Into<String>,
        user_id: impl Into<String>,
    ) -> AssignOrganizationRoleRequest<'a> {
        AssignOrganizationRoleRequest::new(self.client, org_id, user_id)
    }

    /// Remove an organization-level role from a user. These are roles that have organization-wide privileges, like organization admin.
    ///
    /// This operation follows eventual consistency. Changes may take up to 30 seconds to be reflected after the operation is performed.
    pub fn revoke_organization_role(
        &self,
        org_id: impl Into<String>,
        user_id: impl Into<String>,
    ) -> RevokeOrganizationRoleRequest<'a> {
        RevokeOrganizationRoleRequest::new(self.client, org_id, user_id)
    }

    /// Returns a count of users in an organization that match the supplied parameters. By default, users in all your directories and all your managed accounts are counted (including managed accounts that aren’t in a directory).
    ///
    /// To count users in a directory only, use the `directoryIds` field. To count your managed accounts, regardless if they’re in a directory or not, use the `claimStatus` field.
    pub fn get_directory_users_count(
        &self,
        org_id: impl Into<String>,
        directory_id: impl Into<String>,
    ) -> GetDirectoryUsersCountRequest<'a> {
        GetDirectoryUsersCountRequest::new(self.client, org_id, directory_id)
    }

    /// Return user stats for the organization.
    ///
    /// #### Scopes
    /// **[Authorization scopes](/cloud/admin/scopes/) required:** `read:directories:admin`
    pub fn get_user_stats(
        &self,
        org_id: impl Into<String>,
        directory_id: impl Into<String>,
    ) -> GetUserStatsRequest<'a> {
        GetUserStatsRequest::new(self.client, org_id, directory_id)
    }

    /// **Additional response parameters of the API (for e.g., `added_to_org`) are available only to customers using the new user management experience.** Learn more about the [new user management experience](https://community.atlassian.com/t5/Atlassian-Access-articles/User-management-for-cloud-admins-just-got-easier/ba-p/1576592).
    ///
    /// Specifications:
    /// - Return a user’s last active date for each product listed in Atlassian Administration.
    /// - Active is defined as viewing a product's page for a minimum of 2 seconds.
    /// - The data for the last activity may be delayed by up to 24 hours.
    /// - If the user has not accessed a product, the `product_access` response field will be empty.
    ///
    /// Learn the fastest way to call the API with a detailed [tutorial](https://developer.atlassian.com/cloud/admin/organization/user-last-active-dates/).
    pub fn get_user_last_active_dates(
        &self,
        org_id: impl Into<String>,
        account_id: impl Into<String>,
    ) -> GetUserLastActiveDatesRequest<'a> {
        GetUserLastActiveDatesRequest::new(self.client, org_id, account_id)
    }

    /// **This API is deprecated and will no longer work after June 30, 2027.** Use the [Search for users in an organization endpoint](https://developer.atlassian.com/cloud/admin/organization/rest/api-group-users/#api-v2-orgs-orgid-directories-directoryid-users-search-post) instead.
    ///
    /// Return a page of users in your organization that match the supplied parameters. By default, returns users in all your directories.
    ///
    /// To get users in a directory only, use the `directoryIds` field. To get your managed accounts, regardless if they’re in a directory or not, use the `claimStatus` field.
    ///
    /// #### Scopes
    /// **[Authorization scopes](/cloud/admin/scopes/) required:** `read:directories:admin`
    pub fn get_directory_users(
        &self,
        org_id: impl Into<String>,
        directory_id: impl Into<String>,
    ) -> GetDirectoryUsersRequest<'a> {
        GetDirectoryUsersRequest::new(self.client, org_id, directory_id)
    }
}

/// Return a page of users in an organization that match the supplied parameters.
///
/// Use `searchTerm` for free-text search across user display names and email addresses. Use `emails` for exact-match filtering by full email addresses. `searchTerm` and `emails` are mutually exclusive. Providing both in the same request returns `400 Bad Request`. Use the `expand` field to include additional fields such as `platformRoles`, `counts.resources`, `productAccess`, and `groups` in the response.
pub struct SearchDirectoryUsersRequest<'a> {
    client: &'a crate::core::Client,
    org_id: String,
    directory_id: String,
    multi_directory_user_search_request: Option<MultiDirectoryUserSearchRequest>,
}

impl<'a> SearchDirectoryUsersRequest<'a> {
    fn new(client: &'a crate::core::Client, org_id: impl Into<String>, directory_id: impl Into<String>) -> Self {
        Self {
            client,
            org_id: org_id.into(),
            directory_id: directory_id.into(),
            multi_directory_user_search_request: None,
        }
    }

    #[must_use]
    pub fn multi_directory_user_search_request(mut self, value: MultiDirectoryUserSearchRequest) -> Self {
        self.multi_directory_user_search_request = Some(value);

        self
    }

    /// The request as the transport will send it.
    pub fn config(&self) -> crate::core::Result<crate::core::RequestConfig> {
        let mut config = crate::core::RequestConfig::new(
            crate::core::Method::POST,
            format!("/admin/v2/orgs/{}/directories/{}/users/search", self.org_id, self.directory_id),
        );

        let body = match serde_json::to_value(&self.multi_directory_user_search_request)? {
            serde_json::Value::Object(object) => object,
            _ => serde_json::Map::new(),
        };

        config.body = Some(crate::core::Body::Json(serde_json::Value::Object(body)));

        Ok(config)
    }

    /// Sends the request.
    pub async fn send(self) -> crate::core::Result<MultiDirectoryUserSearchPage> {
        self.client.send(&self.config()?).await
    }

    /// Sends the request and hands back the body unmodelled.
    pub async fn send_raw(self) -> crate::core::Result<serde_json::Value> {
        self.client.send_raw(&self.config()?).await
    }
}

/// Returns a list of managed accounts in an organization.
///
/// #### Scopes
/// **[Authorization scopes](/cloud/admin/scopes/) required:** `read:accounts:admin`
pub struct GetUsersRequest<'a> {
    client: &'a crate::core::Client,
    org_id: String,
    cursor: Option<String>,
}

impl<'a> GetUsersRequest<'a> {
    fn new(client: &'a crate::core::Client, org_id: impl Into<String>) -> Self {
        Self { client, org_id: org_id.into(), cursor: None }
    }

    /// Sets the starting point for the page of results to return.
    #[must_use]
    pub fn cursor(mut self, value: impl Into<String>) -> Self {
        self.cursor = Some(value.into());

        self
    }

    /// The request as the transport will send it.
    pub fn config(&self) -> crate::core::Result<crate::core::RequestConfig> {
        let mut config =
            crate::core::RequestConfig::new(crate::core::Method::GET, format!("/admin/v1/orgs/{}/users", self.org_id));

        if let Some(value) = &self.cursor {
            config.query.push(("cursor".to_owned(), crate::core::QueryValue::Scalar(value.clone())));
        }

        Ok(config)
    }

    /// Sends the request.
    pub async fn send(self) -> crate::core::Result<UserPage> {
        self.client.send(&self.config()?).await
    }

    /// Sends the request and hands back the body unmodelled.
    pub async fn send_raw(self) -> crate::core::Result<serde_json::Value> {
        self.client.send_raw(&self.config()?).await
    }
}

/// Invite people to your organization. When you invite someone:
/// - they’re given app roles according to your invitation.
/// - they’re added to directories based on apps in your invitation.
/// - they’re added to groups according to your invitation.
/// - they receive an email invitation if the `sendNotification` field is set to `true` and the `notificationText` field contains a message to include in the email invitation.
///
/// **This API is only available to customers who have at least one paid subscription in their organization.**
pub struct InviteUsersRequest<'a> {
    client: &'a crate::core::Client,
    org_id: String,
    multidirectory_invite_api_request: Option<MultidirectoryInviteApiRequest>,
}

impl<'a> InviteUsersRequest<'a> {
    fn new(client: &'a crate::core::Client, org_id: impl Into<String>) -> Self {
        Self { client, org_id: org_id.into(), multidirectory_invite_api_request: None }
    }

    #[must_use]
    pub fn multidirectory_invite_api_request(mut self, value: MultidirectoryInviteApiRequest) -> Self {
        self.multidirectory_invite_api_request = Some(value);

        self
    }

    /// The request as the transport will send it.
    pub fn config(&self) -> crate::core::Result<crate::core::RequestConfig> {
        let mut config = crate::core::RequestConfig::new(
            crate::core::Method::POST,
            format!("/admin/v2/orgs/{}/users/invite", self.org_id),
        );

        let body = match serde_json::to_value(&self.multidirectory_invite_api_request)? {
            serde_json::Value::Object(object) => object,
            _ => serde_json::Map::new(),
        };

        config.body = Some(crate::core::Body::Json(serde_json::Value::Object(body)));

        Ok(config)
    }

    /// Sends the request.
    pub async fn send(self) -> crate::core::Result<MultidirectoryInviteSuccessResponse> {
        self.client.send(&self.config()?).await
    }

    /// Sends the request and hands back the body unmodelled.
    pub async fn send_raw(self) -> crate::core::Result<serde_json::Value> {
        self.client.send_raw(&self.config()?).await
    }
}

/// Returns a page of role assignments for a user that match the supplied parameters.
///
/// #### Scopes
/// **[Authorization scopes](/cloud/admin/scopes/) required:** `read:directories:admin`
pub struct GetUserRoleAssignmentsRequest<'a> {
    client: &'a crate::core::Client,
    org_id: String,
    directory_id: String,
    account_id: String,
    cursor: Option<String>,
    limit: Option<i64>,
    directory_ids: Option<Vec<String>>,
    resource_owners: Option<Vec<String>>,
    resource_ids: Option<Vec<String>>,
    role_ids: Option<Vec<GetUserRoleAssignmentsRequestRoleIds>>,
}

impl<'a> GetUserRoleAssignmentsRequest<'a> {
    fn new(
        client: &'a crate::core::Client,
        org_id: impl Into<String>,
        directory_id: impl Into<String>,
        account_id: impl Into<String>,
    ) -> Self {
        Self {
            client,
            org_id: org_id.into(),
            directory_id: directory_id.into(),
            account_id: account_id.into(),
            cursor: None,
            limit: None,
            directory_ids: None,
            resource_owners: None,
            resource_ids: None,
            role_ids: None,
        }
    }

    /// Sets the cursor position to retrieve the next set of results. If present, all other parameters are discarded when searching.
    #[must_use]
    pub fn cursor(mut self, value: impl Into<String>) -> Self {
        self.cursor = Some(value.into());

        self
    }

    /// The desired number of results for the search request.
    #[must_use]
    pub fn limit(mut self, value: i64) -> Self {
        self.limit = Some(value);

        self
    }

    /// A list of directory IDs. The requestor must have permissions to  administer resources linked to these directories.
    #[must_use]
    pub fn directory_ids(mut self, value: impl IntoIterator<Item = impl Into<String>>) -> Self {
        self.directory_ids = Some(value.into_iter().map(Into::into).collect());

        self
    }

    /// The list of resource owners to filter the results by. Used to identify resources using their owner to which the user has at least one role assigned to.
    #[must_use]
    pub fn resource_owners(mut self, value: impl IntoIterator<Item = impl Into<String>>) -> Self {
        self.resource_owners = Some(value.into_iter().map(Into::into).collect());

        self
    }

    /// A list of resource IDs. The resource IDs should be specified  using the Atlassian Resource Identifier (ARI) format. Example ARI: `ari:cloud:jira-core::site/1`
    #[must_use]
    pub fn resource_ids(mut self, value: impl IntoIterator<Item = impl Into<String>>) -> Self {
        self.resource_ids = Some(value.into_iter().map(Into::into).collect());

        self
    }

    /// A list of role IDs. The Atlassian canonical roles are used to determine the permissions of the user against resources within  the organization. The allowed roles are:
    ///    - `atlassian/user` - Can access the product, with no product admin permissions
    ///    - `atlassian/admin` - Can access the product, with product admin permissions
    ///    - `atlassian/guest` - Can only access one space you or space admins specify
    ///    - `atlassian/customer` - (Jira Service Management) Can visit help center, submit help requests, and view articles (non-billable)
    ///    - `atlassian/user-access-admin` - No product access. Can administer users and groups for this product in Atlassian Administration
    ///    - `atlassian/contributor` - Can access the product to view, comment, and vote only (non-billable)
    ///    - `atlassian/basic` - Can access basic product features, with no product admin permissions (non-billable)
    ///    - `atlassian/stakeholder` - Can receive incident updates and has the same product access as Customer. Non-billable but available only on Premium and Enterprise plans
    ///    - `atlassian/org-admin` - An organization admin is the highest level of admin and can complete any administrative task in Atlassian Administration
    ///    - `atlassian/site-admin` - Site admins can access Atlassian Administration and complete tasks related to the specific site they are administering.
    ///    - `atlassian/ai-access` - Can use AI features in AI-enabled apps they have access to.
    #[must_use]
    pub fn role_ids(
        mut self,
        value: impl IntoIterator<Item = impl Into<GetUserRoleAssignmentsRequestRoleIds>>,
    ) -> Self {
        self.role_ids = Some(value.into_iter().map(Into::into).collect());

        self
    }

    /// The request as the transport will send it.
    pub fn config(&self) -> crate::core::Result<crate::core::RequestConfig> {
        let mut config = crate::core::RequestConfig::new(
            crate::core::Method::GET,
            format!(
                "/admin/v2/orgs/{}/directories/{}/users/{}/role-assignments",
                self.org_id, self.directory_id, self.account_id
            ),
        );

        if let Some(value) = &self.cursor {
            config.query.push(("cursor".to_owned(), crate::core::QueryValue::Scalar(value.clone())));
        }

        if let Some(value) = &self.limit {
            config.query.push(("limit".to_owned(), crate::core::QueryValue::Scalar(value.to_string())));
        }

        if let Some(value) = &self.directory_ids {
            config.query.push(("directoryIds".to_owned(), crate::core::QueryValue::List(value.clone())));
        }

        if let Some(value) = &self.resource_owners {
            config.query.push(("resourceOwners".to_owned(), crate::core::QueryValue::List(value.clone())));
        }

        if let Some(value) = &self.resource_ids {
            config.query.push(("resourceIds".to_owned(), crate::core::QueryValue::List(value.clone())));
        }

        if let Some(value) = &self.role_ids {
            config.query.push(("roleIds".to_owned(), crate::core::QueryValue::from_serializable(value)?));
        }

        Ok(config)
    }

    /// Sends the request.
    pub async fn send(self) -> crate::core::Result<MultiDirectoryUserRoleAssignmentPage> {
        self.client.send(&self.config()?).await
    }

    /// Sends the request and hands back the body unmodelled.
    pub async fn send_raw(self) -> crate::core::Result<serde_json::Value> {
        self.client.send_raw(&self.config()?).await
    }
}

/// This API can be used to grant Platform Roles to a user.
pub struct GrantUserAccessRequest<'a> {
    client: &'a crate::core::Client,
    org_id: String,
    user_id: String,
    role_api_request: Option<RoleApiRequest>,
}

impl<'a> GrantUserAccessRequest<'a> {
    fn new(client: &'a crate::core::Client, org_id: impl Into<String>, user_id: impl Into<String>) -> Self {
        Self { client, org_id: org_id.into(), user_id: user_id.into(), role_api_request: None }
    }

    #[must_use]
    pub fn role_api_request(mut self, value: RoleApiRequest) -> Self {
        self.role_api_request = Some(value);

        self
    }

    /// The request as the transport will send it.
    pub fn config(&self) -> crate::core::Result<crate::core::RequestConfig> {
        let mut config = crate::core::RequestConfig::new(
            crate::core::Method::POST,
            format!("/admin/v1/orgs/{}/users/{}/roles/assign", self.org_id, self.user_id),
        );

        let body = match serde_json::to_value(&self.role_api_request)? {
            serde_json::Value::Object(object) => object,
            _ => serde_json::Map::new(),
        };

        config.body = Some(crate::core::Body::Json(serde_json::Value::Object(body)));

        Ok(config)
    }

    /// Sends the request.
    pub async fn send(self) -> crate::core::Result<()> {
        self.client.send_empty(&self.config()?).await
    }

    /// Sends the request and hands back the body unmodelled.
    pub async fn send_raw(self) -> crate::core::Result<serde_json::Value> {
        self.client.send_raw(&self.config()?).await
    }
}

/// This API can be used to revoke Platform Roles from a user.
pub struct RevokeUserAccessRequest<'a> {
    client: &'a crate::core::Client,
    org_id: String,
    user_id: String,
    role_api_request: Option<RoleApiRequest>,
}

impl<'a> RevokeUserAccessRequest<'a> {
    fn new(client: &'a crate::core::Client, org_id: impl Into<String>, user_id: impl Into<String>) -> Self {
        Self { client, org_id: org_id.into(), user_id: user_id.into(), role_api_request: None }
    }

    #[must_use]
    pub fn role_api_request(mut self, value: RoleApiRequest) -> Self {
        self.role_api_request = Some(value);

        self
    }

    /// The request as the transport will send it.
    pub fn config(&self) -> crate::core::Result<crate::core::RequestConfig> {
        let mut config = crate::core::RequestConfig::new(
            crate::core::Method::POST,
            format!("/admin/v1/orgs/{}/users/{}/roles/revoke", self.org_id, self.user_id),
        );

        let body = match serde_json::to_value(&self.role_api_request)? {
            serde_json::Value::Object(object) => object,
            _ => serde_json::Map::new(),
        };

        config.body = Some(crate::core::Body::Json(serde_json::Value::Object(body)));

        Ok(config)
    }

    /// Sends the request.
    pub async fn send(self) -> crate::core::Result<()> {
        self.client.send_empty(&self.config()?).await
    }

    /// Sends the request and hands back the body unmodelled.
    pub async fn send_raw(self) -> crate::core::Result<serde_json::Value> {
        self.client.send_raw(&self.config()?).await
    }
}

/// Suspend a user’s access in a directory to remove their access to apps temporarily. You’re not billed for a user when their access is suspended. They regain their roles and group memberships when you restore their access.
pub struct SuspendDirectoryUserRequest<'a> {
    client: &'a crate::core::Client,
    org_id: String,
    directory_id: String,
    account_id: String,
}

impl<'a> SuspendDirectoryUserRequest<'a> {
    fn new(
        client: &'a crate::core::Client,
        org_id: impl Into<String>,
        directory_id: impl Into<String>,
        account_id: impl Into<String>,
    ) -> Self {
        Self { client, org_id: org_id.into(), directory_id: directory_id.into(), account_id: account_id.into() }
    }

    /// The request as the transport will send it.
    pub fn config(&self) -> crate::core::Result<crate::core::RequestConfig> {
        let config = crate::core::RequestConfig::new(
            crate::core::Method::POST,
            format!(
                "/admin/v2/orgs/{}/directories/{}/users/{}/suspend",
                self.org_id, self.directory_id, self.account_id
            ),
        );

        Ok(config)
    }

    /// Sends the request.
    pub async fn send(self) -> crate::core::Result<()> {
        self.client.send_empty(&self.config()?).await
    }

    /// Sends the request and hands back the body unmodelled.
    pub async fn send_raw(self) -> crate::core::Result<serde_json::Value> {
        self.client.send_raw(&self.config()?).await
    }
}

/// Restore a user’s access in a directory to let them access apps again. They regain their roles and group memberships from before their access was suspended. We resume billing you for this user.
pub struct RestoreDirectoryUserRequest<'a> {
    client: &'a crate::core::Client,
    org_id: String,
    directory_id: String,
    account_id: String,
}

impl<'a> RestoreDirectoryUserRequest<'a> {
    fn new(
        client: &'a crate::core::Client,
        org_id: impl Into<String>,
        directory_id: impl Into<String>,
        account_id: impl Into<String>,
    ) -> Self {
        Self { client, org_id: org_id.into(), directory_id: directory_id.into(), account_id: account_id.into() }
    }

    /// The request as the transport will send it.
    pub fn config(&self) -> crate::core::Result<crate::core::RequestConfig> {
        let config = crate::core::RequestConfig::new(
            crate::core::Method::POST,
            format!(
                "/admin/v2/orgs/{}/directories/{}/users/{}/restore",
                self.org_id, self.directory_id, self.account_id
            ),
        );

        Ok(config)
    }

    /// Sends the request.
    pub async fn send(self) -> crate::core::Result<()> {
        self.client.send_empty(&self.config()?).await
    }

    /// Sends the request and hands back the body unmodelled.
    pub async fn send_raw(self) -> crate::core::Result<serde_json::Value> {
        self.client.send_raw(&self.config()?).await
    }
}

/// Returns detailed information about a specific user in a directory within an organization.
pub struct GetDirectoryUserDetailsRequest<'a> {
    client: &'a crate::core::Client,
    org_id: String,
    directory_id: String,
    account_id: String,
}

impl<'a> GetDirectoryUserDetailsRequest<'a> {
    fn new(
        client: &'a crate::core::Client,
        org_id: impl Into<String>,
        directory_id: impl Into<String>,
        account_id: impl Into<String>,
    ) -> Self {
        Self { client, org_id: org_id.into(), directory_id: directory_id.into(), account_id: account_id.into() }
    }

    /// The request as the transport will send it.
    pub fn config(&self) -> crate::core::Result<crate::core::RequestConfig> {
        let config = crate::core::RequestConfig::new(
            crate::core::Method::GET,
            format!("/admin/v2/orgs/{}/directories/{}/users/{}", self.org_id, self.directory_id, self.account_id),
        );

        Ok(config)
    }

    /// Sends the request.
    pub async fn send(self) -> crate::core::Result<MultiDirectoryUserDetails> {
        self.client.send(&self.config()?).await
    }

    /// Sends the request and hands back the body unmodelled.
    pub async fn send_raw(self) -> crate::core::Result<serde_json::Value> {
        self.client.send_raw(&self.config()?).await
    }
}

/// Remove a user from a directory if you don’t want them to appear in your directory or have access to your apps anymore. You’re not billed for a user once they’re removed.
/// You must invite the user to your organization again if you want to reinstate their access to your apps. You’ll need to assign their roles and group memberships again.
pub struct RemoveDirectoryUserRequest<'a> {
    client: &'a crate::core::Client,
    org_id: String,
    directory_id: String,
    account_id: String,
}

impl<'a> RemoveDirectoryUserRequest<'a> {
    fn new(
        client: &'a crate::core::Client,
        org_id: impl Into<String>,
        directory_id: impl Into<String>,
        account_id: impl Into<String>,
    ) -> Self {
        Self { client, org_id: org_id.into(), directory_id: directory_id.into(), account_id: account_id.into() }
    }

    /// The request as the transport will send it.
    pub fn config(&self) -> crate::core::Result<crate::core::RequestConfig> {
        let config = crate::core::RequestConfig::new(
            crate::core::Method::DELETE,
            format!("/admin/v2/orgs/{}/directories/{}/users/{}", self.org_id, self.directory_id, self.account_id),
        );

        Ok(config)
    }

    /// Sends the request.
    pub async fn send(self) -> crate::core::Result<()> {
        self.client.send_empty(&self.config()?).await
    }

    /// Sends the request and hands back the body unmodelled.
    pub async fn send_raw(self) -> crate::core::Result<serde_json::Value> {
        self.client.send_raw(&self.config()?).await
    }
}

/// Assign an organization-level role to a user. These are roles that have organization-wide privileges, like organization admin.
///
/// This operation follows eventual consistency. Changes may take up to 30 seconds to be reflected after the operation is performed.
pub struct AssignOrganizationRoleRequest<'a> {
    client: &'a crate::core::Client,
    org_id: String,
    user_id: String,
    organization_level_role_api_request: Option<OrganizationLevelRoleApiRequest>,
}

impl<'a> AssignOrganizationRoleRequest<'a> {
    fn new(client: &'a crate::core::Client, org_id: impl Into<String>, user_id: impl Into<String>) -> Self {
        Self { client, org_id: org_id.into(), user_id: user_id.into(), organization_level_role_api_request: None }
    }

    #[must_use]
    pub fn organization_level_role_api_request(mut self, value: OrganizationLevelRoleApiRequest) -> Self {
        self.organization_level_role_api_request = Some(value);

        self
    }

    /// The request as the transport will send it.
    pub fn config(&self) -> crate::core::Result<crate::core::RequestConfig> {
        let mut config = crate::core::RequestConfig::new(
            crate::core::Method::POST,
            format!("/admin/v1/orgs/{}/users/{}/role-assignments/assign", self.org_id, self.user_id),
        );

        let body = match serde_json::to_value(&self.organization_level_role_api_request)? {
            serde_json::Value::Object(object) => object,
            _ => serde_json::Map::new(),
        };

        config.body = Some(crate::core::Body::Json(serde_json::Value::Object(body)));

        Ok(config)
    }

    /// Sends the request.
    pub async fn send(self) -> crate::core::Result<()> {
        self.client.send_empty(&self.config()?).await
    }

    /// Sends the request and hands back the body unmodelled.
    pub async fn send_raw(self) -> crate::core::Result<serde_json::Value> {
        self.client.send_raw(&self.config()?).await
    }
}

/// Remove an organization-level role from a user. These are roles that have organization-wide privileges, like organization admin.
///
/// This operation follows eventual consistency. Changes may take up to 30 seconds to be reflected after the operation is performed.
pub struct RevokeOrganizationRoleRequest<'a> {
    client: &'a crate::core::Client,
    org_id: String,
    user_id: String,
    organization_level_role_api_request: Option<OrganizationLevelRoleApiRequest>,
}

impl<'a> RevokeOrganizationRoleRequest<'a> {
    fn new(client: &'a crate::core::Client, org_id: impl Into<String>, user_id: impl Into<String>) -> Self {
        Self { client, org_id: org_id.into(), user_id: user_id.into(), organization_level_role_api_request: None }
    }

    #[must_use]
    pub fn organization_level_role_api_request(mut self, value: OrganizationLevelRoleApiRequest) -> Self {
        self.organization_level_role_api_request = Some(value);

        self
    }

    /// The request as the transport will send it.
    pub fn config(&self) -> crate::core::Result<crate::core::RequestConfig> {
        let mut config = crate::core::RequestConfig::new(
            crate::core::Method::POST,
            format!("/admin/v1/orgs/{}/users/{}/role-assignments/revoke", self.org_id, self.user_id),
        );

        let body = match serde_json::to_value(&self.organization_level_role_api_request)? {
            serde_json::Value::Object(object) => object,
            _ => serde_json::Map::new(),
        };

        config.body = Some(crate::core::Body::Json(serde_json::Value::Object(body)));

        Ok(config)
    }

    /// Sends the request.
    pub async fn send(self) -> crate::core::Result<()> {
        self.client.send_empty(&self.config()?).await
    }

    /// Sends the request and hands back the body unmodelled.
    pub async fn send_raw(self) -> crate::core::Result<serde_json::Value> {
        self.client.send_raw(&self.config()?).await
    }
}

/// Returns a count of users in an organization that match the supplied parameters. By default, users in all your directories and all your managed accounts are counted (including managed accounts that aren’t in a directory).
///
/// To count users in a directory only, use the `directoryIds` field. To count your managed accounts, regardless if they’re in a directory or not, use the `claimStatus` field.
pub struct GetDirectoryUsersCountRequest<'a> {
    client: &'a crate::core::Client,
    org_id: String,
    directory_id: String,
    account_ids: Option<Vec<String>>,
    directory_ids: Option<Vec<String>>,
    resource_ids: Option<Vec<String>>,
    group_ids: Option<Vec<String>>,
    mfa_enabled: Option<bool>,
    claim_status: Option<GetDirectoryUsersCountRequestClaimStatus>,
    status: Option<Vec<GetDirectoryUsersCountRequestStatus>>,
    account_status: Option<Vec<GetDirectoryUsersCountRequestAccountStatus>>,
    membership_status: Option<Vec<GetDirectoryUsersCountRequestMembershipStatus>>,
    role_ids: Option<Vec<GetDirectoryUsersCountRequestRoleIds>>,
    search_term: Option<String>,
    email_domains: Option<Vec<String>>,
}

impl<'a> GetDirectoryUsersCountRequest<'a> {
    fn new(client: &'a crate::core::Client, org_id: impl Into<String>, directory_id: impl Into<String>) -> Self {
        Self {
            client,
            org_id: org_id.into(),
            directory_id: directory_id.into(),
            account_ids: None,
            directory_ids: None,
            resource_ids: None,
            group_ids: None,
            mfa_enabled: None,
            claim_status: None,
            status: None,
            account_status: None,
            membership_status: None,
            role_ids: None,
            search_term: None,
            email_domains: None,
        }
    }

    /// A list of user account IDs.
    #[must_use]
    pub fn account_ids(mut self, value: impl IntoIterator<Item = impl Into<String>>) -> Self {
        self.account_ids = Some(value.into_iter().map(Into::into).collect());

        self
    }

    /// A list of directory IDs. The requestor must have permissions to  administer resources linked to these directories.
    #[must_use]
    pub fn directory_ids(mut self, value: impl IntoIterator<Item = impl Into<String>>) -> Self {
        self.directory_ids = Some(value.into_iter().map(Into::into).collect());

        self
    }

    /// A list of resource IDs. The resource IDs should be specified  using the Atlassian Resource Identifier (ARI) format. Example ARI: `ari:cloud:jira-core::site/1`
    #[must_use]
    pub fn resource_ids(mut self, value: impl IntoIterator<Item = impl Into<String>>) -> Self {
        self.resource_ids = Some(value.into_iter().map(Into::into).collect());

        self
    }

    /// A list of group IDs.
    #[must_use]
    pub fn group_ids(mut self, value: impl IntoIterator<Item = impl Into<String>>) -> Self {
        self.group_ids = Some(value.into_iter().map(Into::into).collect());

        self
    }

    /// Whether or not a managed account has two-step verification enabled on their account. If `true`, they have two-step verification enabled.
    /// By default, all accounts are returned, regardless of two-step verification status.
    #[must_use]
    pub fn mfa_enabled(mut self, value: bool) -> Self {
        self.mfa_enabled = Some(value);

        self
    }

    /// The claim status for the user account. By default, both `managed` and `unmanaged` accounts are returned.
    ///   - `managed` - Returns only managed accounts. For more on managed accounts: https://support.atlassian.com/user-management/docs/what-are-managed-accounts/
    ///   - `unmanaged` - Returns only unmanaged accounts.
    #[must_use]
    pub fn claim_status(mut self, value: impl Into<GetDirectoryUsersCountRequestClaimStatus>) -> Self {
        self.claim_status = Some(value.into());

        self
    }

    /// The status for the user account. This status is a composite of `accountStatus` and `membershipStatus`.
    ///   - `active` - `accountStatus` is `active` and `membershipStatus` is `active`.
    ///   - `suspended` - `accountStatus` is `active` and `membershipStatus` is `suspended`.
    ///   - `not_invited` - `accountStatus` is `active` and `membershipStatus` is `no_membership`.
    ///   - `deactivated` - `accountStatus` is `inactive`.
    ///   - `for_deletion` - Indicates whether or not a managed account is scheduled for deletion.
    #[must_use]
    pub fn status(mut self, value: impl IntoIterator<Item = impl Into<GetDirectoryUsersCountRequestStatus>>) -> Self {
        self.status = Some(value.into_iter().map(Into::into).collect());

        self
    }

    /// The lifecycle status of the account.
    ///   - `active` - The account is active and can be used.
    ///   - `inactive` - The account is inactive and doesn't have access to any resources.
    ///   - `closed` - The account is closed and can't be used.
    #[must_use]
    pub fn account_status(
        mut self,
        value: impl IntoIterator<Item = impl Into<GetDirectoryUsersCountRequestAccountStatus>>,
    ) -> Self {
        self.account_status = Some(value.into_iter().map(Into::into).collect());

        self
    }

    /// A list of membership statuses. The membership status is the status of the user account in the organization.
    ///   - `active` - the account has an active membership for one or more directories within the organization.
    ///   - `suspended` - the account is suspended in ALL directories within the organization, to which the requestor has permission to access.
    ///   - `no_membership` - the account is in NONE of the organization’s directories.
    #[must_use]
    pub fn membership_status(
        mut self,
        value: impl IntoIterator<Item = impl Into<GetDirectoryUsersCountRequestMembershipStatus>>,
    ) -> Self {
        self.membership_status = Some(value.into_iter().map(Into::into).collect());

        self
    }

    /// A list of role IDs. The Atlassian canonical roles are used to determine the permissions of the user against resources within  the organization. The allowed roles are:
    ///    - `atlassian/user` - Can access the product, with no product admin permissions
    ///    - `atlassian/admin` - Can access the product, with product admin permissions
    ///    - `atlassian/guest` - Can only access one space you or space admins specify
    ///    - `atlassian/customer` - (Jira Service Management) Can visit help center, submit help requests, and view articles (non-billable)
    ///    - `atlassian/user-access-admin` - No product access. Can administer users and groups for this product in Atlassian Administration
    ///    - `atlassian/contributor` - Can access the product to view, comment, and vote only (non-billable)
    ///    - `atlassian/basic` - Can access basic product features, with no product admin permissions (non-billable)
    ///    - `atlassian/stakeholder` - Can receive incident updates and has the same product access as Customer. Non-billable but available only on Premium and Enterprise plans
    ///    - `atlassian/org-admin` - An organization admin is the highest level of admin and can complete any administrative task in Atlassian Administration
    ///    - `atlassian/site-admin` - Site admins can access Atlassian Administration and complete tasks related to the specific site they are administering.
    ///    - `atlassian/ai-access` - Can use AI features in AI-enabled apps they have access to.
    #[must_use]
    pub fn role_ids(
        mut self,
        value: impl IntoIterator<Item = impl Into<GetDirectoryUsersCountRequestRoleIds>>,
    ) -> Self {
        self.role_ids = Some(value.into_iter().map(Into::into).collect());

        self
    }

    /// A search term to search the `nickname` and `email` fields.
    #[must_use]
    pub fn search_term(mut self, value: impl Into<String>) -> Self {
        self.search_term = Some(value.into());

        self
    }

    /// The email domain to filter the results. The email domain will be used to search against the  account email domain. For example, get all users with the `@atlassian.com` or `@example.com` email domain.
    #[must_use]
    pub fn email_domains(mut self, value: impl IntoIterator<Item = impl Into<String>>) -> Self {
        self.email_domains = Some(value.into_iter().map(Into::into).collect());

        self
    }

    /// The request as the transport will send it.
    pub fn config(&self) -> crate::core::Result<crate::core::RequestConfig> {
        let mut config = crate::core::RequestConfig::new(
            crate::core::Method::GET,
            format!("/admin/v2/orgs/{}/directories/{}/users/count", self.org_id, self.directory_id),
        );

        if let Some(value) = &self.account_ids {
            config.query.push(("accountIds".to_owned(), crate::core::QueryValue::List(value.clone())));
        }

        if let Some(value) = &self.directory_ids {
            config.query.push(("directoryIds".to_owned(), crate::core::QueryValue::List(value.clone())));
        }

        if let Some(value) = &self.resource_ids {
            config.query.push(("resourceIds".to_owned(), crate::core::QueryValue::List(value.clone())));
        }

        if let Some(value) = &self.group_ids {
            config.query.push(("groupIds".to_owned(), crate::core::QueryValue::List(value.clone())));
        }

        if let Some(value) = &self.mfa_enabled {
            config.query.push(("mfaEnabled".to_owned(), crate::core::QueryValue::Scalar(value.to_string())));
        }

        if let Some(value) = &self.claim_status {
            config.query.push(("claimStatus".to_owned(), crate::core::QueryValue::from_serializable(value)?));
        }

        if let Some(value) = &self.status {
            config.query.push(("status".to_owned(), crate::core::QueryValue::from_serializable(value)?));
        }

        if let Some(value) = &self.account_status {
            config.query.push(("accountStatus".to_owned(), crate::core::QueryValue::from_serializable(value)?));
        }

        if let Some(value) = &self.membership_status {
            config.query.push(("membershipStatus".to_owned(), crate::core::QueryValue::from_serializable(value)?));
        }

        if let Some(value) = &self.role_ids {
            config.query.push(("roleIds".to_owned(), crate::core::QueryValue::from_serializable(value)?));
        }

        if let Some(value) = &self.search_term {
            config.query.push(("searchTerm".to_owned(), crate::core::QueryValue::Scalar(value.clone())));
        }

        if let Some(value) = &self.email_domains {
            config.query.push(("emailDomains".to_owned(), crate::core::QueryValue::List(value.clone())));
        }

        Ok(config)
    }

    /// Sends the request.
    pub async fn send(self) -> crate::core::Result<GetDirectoryUsersCount> {
        self.client.send(&self.config()?).await
    }

    /// Sends the request and hands back the body unmodelled.
    pub async fn send_raw(self) -> crate::core::Result<serde_json::Value> {
        self.client.send_raw(&self.config()?).await
    }
}

/// Return user stats for the organization.
///
/// #### Scopes
/// **[Authorization scopes](/cloud/admin/scopes/) required:** `read:directories:admin`
pub struct GetUserStatsRequest<'a> {
    client: &'a crate::core::Client,
    org_id: String,
    directory_id: String,
}

impl<'a> GetUserStatsRequest<'a> {
    fn new(client: &'a crate::core::Client, org_id: impl Into<String>, directory_id: impl Into<String>) -> Self {
        Self { client, org_id: org_id.into(), directory_id: directory_id.into() }
    }

    /// The request as the transport will send it.
    pub fn config(&self) -> crate::core::Result<crate::core::RequestConfig> {
        let config = crate::core::RequestConfig::new(
            crate::core::Method::GET,
            format!("/admin/v2/orgs/{}/directories/{}/users/stats", self.org_id, self.directory_id),
        );

        Ok(config)
    }

    /// Sends the request.
    pub async fn send(self) -> crate::core::Result<MultiDirectoryUserStats> {
        self.client.send(&self.config()?).await
    }

    /// Sends the request and hands back the body unmodelled.
    pub async fn send_raw(self) -> crate::core::Result<serde_json::Value> {
        self.client.send_raw(&self.config()?).await
    }
}

/// **Additional response parameters of the API (for e.g., `added_to_org`) are available only to customers using the new user management experience.** Learn more about the [new user management experience](https://community.atlassian.com/t5/Atlassian-Access-articles/User-management-for-cloud-admins-just-got-easier/ba-p/1576592).
///
/// Specifications:
/// - Return a user’s last active date for each product listed in Atlassian Administration.
/// - Active is defined as viewing a product's page for a minimum of 2 seconds.
/// - The data for the last activity may be delayed by up to 24 hours.
/// - If the user has not accessed a product, the `product_access` response field will be empty.
///
/// Learn the fastest way to call the API with a detailed [tutorial](https://developer.atlassian.com/cloud/admin/organization/user-last-active-dates/).
pub struct GetUserLastActiveDatesRequest<'a> {
    client: &'a crate::core::Client,
    org_id: String,
    account_id: String,
    cursor: Option<String>,
}

impl<'a> GetUserLastActiveDatesRequest<'a> {
    fn new(client: &'a crate::core::Client, org_id: impl Into<String>, account_id: impl Into<String>) -> Self {
        Self { client, org_id: org_id.into(), account_id: account_id.into(), cursor: None }
    }

    /// Cursor to fetch the next page
    #[must_use]
    pub fn cursor(mut self, value: impl Into<String>) -> Self {
        self.cursor = Some(value.into());

        self
    }

    /// The request as the transport will send it.
    pub fn config(&self) -> crate::core::Result<crate::core::RequestConfig> {
        let mut config = crate::core::RequestConfig::new(
            crate::core::Method::GET,
            format!("/admin/v1/orgs/{}/directory/users/{}/last-active-dates", self.org_id, self.account_id),
        );

        if let Some(value) = &self.cursor {
            config.query.push(("cursor".to_owned(), crate::core::QueryValue::Scalar(value.clone())));
        }

        Ok(config)
    }

    /// Sends the request.
    pub async fn send(self) -> crate::core::Result<UserProductAccessActivityPage> {
        self.client.send(&self.config()?).await
    }

    /// Sends the request and hands back the body unmodelled.
    pub async fn send_raw(self) -> crate::core::Result<serde_json::Value> {
        self.client.send_raw(&self.config()?).await
    }
}

/// **This API is deprecated and will no longer work after June 30, 2027.** Use the [Search for users in an organization endpoint](https://developer.atlassian.com/cloud/admin/organization/rest/api-group-users/#api-v2-orgs-orgid-directories-directoryid-users-search-post) instead.
///
/// Return a page of users in your organization that match the supplied parameters. By default, returns users in all your directories.
///
/// To get users in a directory only, use the `directoryIds` field. To get your managed accounts, regardless if they’re in a directory or not, use the `claimStatus` field.
///
/// #### Scopes
/// **[Authorization scopes](/cloud/admin/scopes/) required:** `read:directories:admin`
pub struct GetDirectoryUsersRequest<'a> {
    client: &'a crate::core::Client,
    org_id: String,
    directory_id: String,
    cursor: Option<String>,
    limit: Option<i64>,
    account_ids: Option<Vec<String>>,
    directory_ids: Option<Vec<String>>,
    resource_ids: Option<Vec<String>>,
    group_ids: Option<Vec<String>>,
    mfa_enabled: Option<bool>,
    claim_status: Option<GetDirectoryUsersRequestClaimStatus>,
    status: Option<Vec<GetDirectoryUsersRequestStatus>>,
    account_status: Option<Vec<GetDirectoryUsersRequestAccountStatus>>,
    membership_status: Option<Vec<GetDirectoryUsersRequestMembershipStatus>>,
    role_ids: Option<Vec<GetDirectoryUsersRequestRoleIds>>,
    email_domains: Option<Vec<String>>,
    search_term: Option<String>,
    sort_by: Option<Vec<GetDirectoryUsersRequestSortBy>>,
}

impl<'a> GetDirectoryUsersRequest<'a> {
    fn new(client: &'a crate::core::Client, org_id: impl Into<String>, directory_id: impl Into<String>) -> Self {
        Self {
            client,
            org_id: org_id.into(),
            directory_id: directory_id.into(),
            cursor: None,
            limit: None,
            account_ids: None,
            directory_ids: None,
            resource_ids: None,
            group_ids: None,
            mfa_enabled: None,
            claim_status: None,
            status: None,
            account_status: None,
            membership_status: None,
            role_ids: None,
            email_domains: None,
            search_term: None,
            sort_by: None,
        }
    }

    /// Sets the cursor position to retrieve the next set of results. If present, all other parameters are discarded when searching.
    #[must_use]
    pub fn cursor(mut self, value: impl Into<String>) -> Self {
        self.cursor = Some(value.into());

        self
    }

    /// The desired number of results for the search request.
    #[must_use]
    pub fn limit(mut self, value: i64) -> Self {
        self.limit = Some(value);

        self
    }

    /// A list of user account IDs.
    #[must_use]
    pub fn account_ids(mut self, value: impl IntoIterator<Item = impl Into<String>>) -> Self {
        self.account_ids = Some(value.into_iter().map(Into::into).collect());

        self
    }

    /// A list of directory IDs. The requestor must have permissions to  administer resources linked to these directories.
    #[must_use]
    pub fn directory_ids(mut self, value: impl IntoIterator<Item = impl Into<String>>) -> Self {
        self.directory_ids = Some(value.into_iter().map(Into::into).collect());

        self
    }

    /// A list of resource IDs. The resource IDs should be specified  using the Atlassian Resource Identifier (ARI) format. Example ARI: `ari:cloud:jira-core::site/1`
    #[must_use]
    pub fn resource_ids(mut self, value: impl IntoIterator<Item = impl Into<String>>) -> Self {
        self.resource_ids = Some(value.into_iter().map(Into::into).collect());

        self
    }

    /// A list of group IDs.
    #[must_use]
    pub fn group_ids(mut self, value: impl IntoIterator<Item = impl Into<String>>) -> Self {
        self.group_ids = Some(value.into_iter().map(Into::into).collect());

        self
    }

    /// Whether or not a managed account has two-step verification enabled on their account. If `true`, they have two-step verification enabled.
    /// By default, all accounts are returned, regardless of two-step verification status.
    #[must_use]
    pub fn mfa_enabled(mut self, value: bool) -> Self {
        self.mfa_enabled = Some(value);

        self
    }

    /// The claim status for the user account. By default, both `managed` and `unmanaged` accounts are returned.
    ///   - `managed` - Returns only managed accounts. For more on managed accounts: https://support.atlassian.com/user-management/docs/what-are-managed-accounts/
    ///   - `unmanaged` - Returns only unmanaged accounts.
    #[must_use]
    pub fn claim_status(mut self, value: impl Into<GetDirectoryUsersRequestClaimStatus>) -> Self {
        self.claim_status = Some(value.into());

        self
    }

    /// The status for the user account. This status is a composite of `accountStatus` and `membershipStatus`.
    ///   - `active` - `accountStatus` is `active` and `membershipStatus` is `active`.
    ///   - `suspended` - `accountStatus` is `active` and `membershipStatus` is `suspended`.
    ///   - `not_invited` - `accountStatus` is `active` and `membershipStatus` is `no_membership`.
    ///   - `deactivated` - `accountStatus` is `inactive`.
    ///   - `for_deletion` - Indicates whether or not a managed account is scheduled for deletion.
    #[must_use]
    pub fn status(mut self, value: impl IntoIterator<Item = impl Into<GetDirectoryUsersRequestStatus>>) -> Self {
        self.status = Some(value.into_iter().map(Into::into).collect());

        self
    }

    /// The lifecycle status of the account.
    ///   - `active` - The account is active and can be used.
    ///   - `inactive` - The account is inactive and doesn't have access to any resources.
    ///   - `closed` - The account is closed and can't be used.
    #[must_use]
    pub fn account_status(
        mut self,
        value: impl IntoIterator<Item = impl Into<GetDirectoryUsersRequestAccountStatus>>,
    ) -> Self {
        self.account_status = Some(value.into_iter().map(Into::into).collect());

        self
    }

    /// A list of membership statuses. The membership status is the status of the user account in the organization.
    ///   - `active` - the account has an active membership for one or more directories within the organization.
    ///   - `suspended` - the account is suspended in ALL directories within the organization, to which the requestor has permission to access.
    ///   - `no_membership` - the account is in NONE of the organization’s directories.
    #[must_use]
    pub fn membership_status(
        mut self,
        value: impl IntoIterator<Item = impl Into<GetDirectoryUsersRequestMembershipStatus>>,
    ) -> Self {
        self.membership_status = Some(value.into_iter().map(Into::into).collect());

        self
    }

    /// A list of role IDs. The Atlassian canonical roles are used to determine the permissions of the user against resources within  the organization. The allowed roles are:
    ///    - `atlassian/user` - Can access the product, with no product admin permissions
    ///    - `atlassian/admin` - Can access the product, with product admin permissions
    ///    - `atlassian/guest` - Can only access one space you or space admins specify
    ///    - `atlassian/customer` - (Jira Service Management) Can visit help center, submit help requests, and view articles (non-billable)
    ///    - `atlassian/user-access-admin` - No product access. Can administer users and groups for this product in Atlassian Administration
    ///    - `atlassian/contributor` - Can access the product to view, comment, and vote only (non-billable)
    ///    - `atlassian/basic` - Can access basic product features, with no product admin permissions (non-billable)
    ///    - `atlassian/stakeholder` - Can receive incident updates and has the same product access as Customer. Non-billable but available only on Premium and Enterprise plans
    ///    - `atlassian/org-admin` - An organization admin is the highest level of admin and can complete any administrative task in Atlassian Administration
    ///    - `atlassian/site-admin` - Site admins can access Atlassian Administration and complete tasks related to the specific site they are administering.
    ///    - `atlassian/ai-access` - Can use AI features in AI-enabled apps they have access to.
    #[must_use]
    pub fn role_ids(mut self, value: impl IntoIterator<Item = impl Into<GetDirectoryUsersRequestRoleIds>>) -> Self {
        self.role_ids = Some(value.into_iter().map(Into::into).collect());

        self
    }

    /// The email domain to filter the results. The email domain will be used to search against the  account email domain. For example, get all users with the `@atlassian.com` or `@example.com` email domain.
    #[must_use]
    pub fn email_domains(mut self, value: impl IntoIterator<Item = impl Into<String>>) -> Self {
        self.email_domains = Some(value.into_iter().map(Into::into).collect());

        self
    }

    /// A search term to search the `nickname` and `email` fields.
    #[must_use]
    pub fn search_term(mut self, value: impl Into<String>) -> Self {
        self.search_term = Some(value.into());

        self
    }

    /// The field and direction to sort the results by. Currently, only a single field can be sorted by. If `null`, the default sorting will be used.
    #[must_use]
    pub fn sort_by(mut self, value: impl IntoIterator<Item = GetDirectoryUsersRequestSortBy>) -> Self {
        self.sort_by = Some(value.into_iter().collect());

        self
    }

    /// The request as the transport will send it.
    pub fn config(&self) -> crate::core::Result<crate::core::RequestConfig> {
        let mut config = crate::core::RequestConfig::new(
            crate::core::Method::GET,
            format!("/admin/v2/orgs/{}/directories/{}/users", self.org_id, self.directory_id),
        );

        if let Some(value) = &self.cursor {
            config.query.push(("cursor".to_owned(), crate::core::QueryValue::Scalar(value.clone())));
        }

        if let Some(value) = &self.limit {
            config.query.push(("limit".to_owned(), crate::core::QueryValue::Scalar(value.to_string())));
        }

        if let Some(value) = &self.account_ids {
            config.query.push(("accountIds".to_owned(), crate::core::QueryValue::List(value.clone())));
        }

        if let Some(value) = &self.directory_ids {
            config.query.push(("directoryIds".to_owned(), crate::core::QueryValue::List(value.clone())));
        }

        if let Some(value) = &self.resource_ids {
            config.query.push(("resourceIds".to_owned(), crate::core::QueryValue::List(value.clone())));
        }

        if let Some(value) = &self.group_ids {
            config.query.push(("groupIds".to_owned(), crate::core::QueryValue::List(value.clone())));
        }

        if let Some(value) = &self.mfa_enabled {
            config.query.push(("mfaEnabled".to_owned(), crate::core::QueryValue::Scalar(value.to_string())));
        }

        if let Some(value) = &self.claim_status {
            config.query.push(("claimStatus".to_owned(), crate::core::QueryValue::from_serializable(value)?));
        }

        if let Some(value) = &self.status {
            config.query.push(("status".to_owned(), crate::core::QueryValue::from_serializable(value)?));
        }

        if let Some(value) = &self.account_status {
            config.query.push(("accountStatus".to_owned(), crate::core::QueryValue::from_serializable(value)?));
        }

        if let Some(value) = &self.membership_status {
            config.query.push(("membershipStatus".to_owned(), crate::core::QueryValue::from_serializable(value)?));
        }

        if let Some(value) = &self.role_ids {
            config.query.push(("roleIds".to_owned(), crate::core::QueryValue::from_serializable(value)?));
        }

        if let Some(value) = &self.email_domains {
            config.query.push(("emailDomains".to_owned(), crate::core::QueryValue::List(value.clone())));
        }

        if let Some(value) = &self.search_term {
            config.query.push(("searchTerm".to_owned(), crate::core::QueryValue::Scalar(value.clone())));
        }

        if let Some(value) = &self.sort_by {
            config.query.push(("sortBy".to_owned(), crate::core::QueryValue::from_serializable(value)?));
        }

        Ok(config)
    }

    /// Sends the request.
    pub async fn send(self) -> crate::core::Result<MultiDirectoryUserPage> {
        self.client.send(&self.config()?).await
    }

    /// Sends the request and hands back the body unmodelled.
    pub async fn send_raw(self) -> crate::core::Result<serde_json::Value> {
        self.client.send_raw(&self.config()?).await
    }
}
