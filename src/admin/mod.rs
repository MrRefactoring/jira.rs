// @generated by apis-code-gen. Do not edit: change the generator or the specification.

//! The Admin surface.

pub mod api;
pub mod models;

pub use api::*;
pub use models::*;

/// Every Admin operation, grouped the way the API documents them.
///
/// Build the transport once and hand it to each surface: a second client is a second set of OAuth tokens,
/// and whichever refreshes first invalidates the other.
pub struct AdminClient {
    client: crate::core::Client,
}

impl AdminClient {
    pub fn new(client: crate::core::Client) -> Self {
        Self { client }
    }

    /// The transport underneath, for a request this surface does not describe.
    pub fn client(&self) -> &crate::core::Client {
        &self.client
    }

    /// The Orgs operations.
    pub fn orgs(&self) -> OrgsService<'_> {
        OrgsService::new(&self.client)
    }

    /// The Users operations.
    pub fn users(&self) -> UsersService<'_> {
        UsersService::new(&self.client)
    }

    /// The Groups operations.
    pub fn groups(&self) -> GroupsService<'_> {
        GroupsService::new(&self.client)
    }

    /// The Directory operations.
    pub fn directory(&self) -> DirectoryService<'_> {
        DirectoryService::new(&self.client)
    }

    /// The Domains operations.
    pub fn domains(&self) -> DomainsService<'_> {
        DomainsService::new(&self.client)
    }

    /// The Events operations.
    pub fn events(&self) -> EventsService<'_> {
        EventsService::new(&self.client)
    }

    /// The Policies operations.
    pub fn policies(&self) -> PoliciesService<'_> {
        PoliciesService::new(&self.client)
    }

    /// The Workspaces operations.
    pub fn workspaces(&self) -> WorkspacesService<'_> {
        WorkspacesService::new(&self.client)
    }
}

impl From<crate::core::Client> for AdminClient {
    fn from(client: crate::core::Client) -> Self {
        Self::new(client)
    }
}
