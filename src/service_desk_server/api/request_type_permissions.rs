// @generated by apis-code-gen. Do not edit: change the generator or the specification.

use super::super::models::*;

/// The RequestTypePermissions operations.
pub struct RequestTypePermissionsService<'a> {
    client: &'a crate::core::Client,
}

impl<'a> RequestTypePermissionsService<'a> {
    pub(crate) fn new(client: &'a crate::core::Client) -> Self {
        Self { client }
    }

    /// Returns the permissions for a customer request type for a given request type ID.
    /// These permissions control who can create the customer request. The entities that can be granted permissions are users, groups, and organizations.
    pub fn get_permissions_by_request_type_id(
        &self,
        service_desk_id: impl Into<String>,
        request_type_id: impl Into<String>,
    ) -> GetPermissionsByRequestTypeIdRequest<'a> {
        GetPermissionsByRequestTypeIdRequest::new(self.client, service_desk_id, request_type_id)
    }

    /// Upsert the permissions for a customer request type. This operation will overwrite any existing permissions.
    pub fn upsert_request_type_permission(
        &self,
        service_desk_id: impl Into<String>,
        request_type_id: impl Into<String>,
        request_type_permission_request: RequestTypePermissionRequest,
    ) -> UpsertRequestTypePermissionRequest<'a> {
        UpsertRequestTypePermissionRequest::new(
            self.client,
            service_desk_id,
            request_type_id,
            request_type_permission_request,
        )
    }
}

/// Returns the permissions for a customer request type for a given request type ID.
/// These permissions control who can create the customer request. The entities that can be granted permissions are users, groups, and organizations.
pub struct GetPermissionsByRequestTypeIdRequest<'a> {
    client: &'a crate::core::Client,
    service_desk_id: String,
    request_type_id: String,
}

impl<'a> GetPermissionsByRequestTypeIdRequest<'a> {
    fn new(
        client: &'a crate::core::Client,
        service_desk_id: impl Into<String>,
        request_type_id: impl Into<String>,
    ) -> Self {
        Self {
            client,
            service_desk_id: service_desk_id.into(),
            request_type_id: request_type_id.into(),
        }
    }

    /// The request as the transport will send it.
    pub fn config(&self) -> crate::core::Result<crate::core::RequestConfig> {
        let config = crate::core::RequestConfig::new(
            crate::core::Method::GET,
            format!(
                "/rest/servicedeskapi/servicedesk/{}/requesttype/{}/permission",
                self.service_desk_id, self.request_type_id
            ),
        );

        Ok(config)
    }

    /// Sends the request.
    pub async fn send(self) -> crate::core::Result<RequestTypePermission> {
        self.client.send(&self.config()?).await
    }

    /// Sends the request and hands back the body unmodelled.
    pub async fn send_raw(self) -> crate::core::Result<serde_json::Value> {
        self.client.send_raw(&self.config()?).await
    }
}

/// Upsert the permissions for a customer request type. This operation will overwrite any existing permissions.
pub struct UpsertRequestTypePermissionRequest<'a> {
    client: &'a crate::core::Client,
    service_desk_id: String,
    request_type_id: String,
    request_type_permission_request: RequestTypePermissionRequest,
}

impl<'a> UpsertRequestTypePermissionRequest<'a> {
    fn new(
        client: &'a crate::core::Client,
        service_desk_id: impl Into<String>,
        request_type_id: impl Into<String>,
        request_type_permission_request: RequestTypePermissionRequest,
    ) -> Self {
        Self {
            client,
            service_desk_id: service_desk_id.into(),
            request_type_id: request_type_id.into(),
            request_type_permission_request,
        }
    }

    /// The request as the transport will send it.
    pub fn config(&self) -> crate::core::Result<crate::core::RequestConfig> {
        let mut config = crate::core::RequestConfig::new(
            crate::core::Method::PUT,
            format!(
                "/rest/servicedeskapi/servicedesk/{}/requesttype/{}/permission",
                self.service_desk_id, self.request_type_id
            ),
        );

        let body = match serde_json::to_value(&self.request_type_permission_request)? {
            serde_json::Value::Object(object) => object,
            _ => serde_json::Map::new(),
        };

        config.body = Some(crate::core::Body::Json(serde_json::Value::Object(body)));

        Ok(config)
    }

    /// Sends the request.
    pub async fn send(self) -> crate::core::Result<RequestTypePermission> {
        self.client.send(&self.config()?).await
    }

    /// Sends the request and hands back the body unmodelled.
    pub async fn send_raw(self) -> crate::core::Result<serde_json::Value> {
        self.client.send_raw(&self.config()?).await
    }
}
