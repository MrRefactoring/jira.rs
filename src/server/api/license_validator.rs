// @generated by apis-code-gen. Do not edit: change the generator or the specification.

use super::super::models::*;

/// The LicenseValidator operations.
pub struct LicenseValidatorService<'a> {
    client: &'a crate::core::Client,
}

impl<'a> LicenseValidatorService<'a> {
    pub(crate) fn new(client: &'a crate::core::Client) -> Self {
        Self { client }
    }

    /// Validates a Jira license
    pub fn validate(&self, body: impl Into<String>) -> ValidateRequest<'a> {
        ValidateRequest::new(self.client, body)
    }
}

/// Validates a Jira license
pub struct ValidateRequest<'a> {
    client: &'a crate::core::Client,
    body: String,
}

impl<'a> ValidateRequest<'a> {
    fn new(client: &'a crate::core::Client, body: impl Into<String>) -> Self {
        Self { client, body: body.into() }
    }

    /// The request as the transport will send it.
    pub fn config(&self) -> crate::core::Result<crate::core::RequestConfig> {
        let mut config =
            crate::core::RequestConfig::new(crate::core::Method::POST, "/rest/api/2/licenseValidator".to_owned());

        config.body = Some(crate::core::Body::Json(serde_json::to_value(&self.body)?));

        Ok(config)
    }

    /// Sends the request.
    pub async fn send(self) -> crate::core::Result<LicenseValidationResults> {
        self.client.send(&self.config()?).await
    }

    /// Sends the request and hands back the body unmodelled.
    pub async fn send_raw(self) -> crate::core::Result<serde_json::Value> {
        self.client.send_raw(&self.config()?).await
    }
}
