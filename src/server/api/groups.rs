// @generated by apis-code-gen. Do not edit: change the generator or the specification.

use super::super::models::*;

/// The Groups operations.
pub struct GroupsService<'a> {
    client: &'a crate::core::Client,
}

impl<'a> GroupsService<'a> {
    pub(crate) fn new(client: &'a crate::core::Client) -> Self {
        Self { client }
    }

    /// Creates a group by given group parameter
    pub fn create_group(&self) -> CreateGroupRequest<'a> {
        CreateGroupRequest::new(self.client)
    }

    /// Deletes a group by given group parameter
    pub fn remove_group(&self, groupname: impl Into<String>) -> RemoveGroupRequest<'a> {
        RemoveGroupRequest::new(self.client, groupname)
    }

    /// Returns a paginated list of users who are members of the specified group and its subgroups
    pub fn get_users_from_group(&self, groupname: impl Into<String>) -> GetUsersFromGroupRequest<'a> {
        GetUsersFromGroupRequest::new(self.client, groupname)
    }

    /// Adds given user to a group
    pub fn add_user_to_group(&self, groupname: impl Into<String>) -> AddUserToGroupRequest<'a> {
        AddUserToGroupRequest::new(self.client, groupname)
    }

    /// Removes given user from a group
    pub fn remove_user_from_group(
        &self,
        groupname: impl Into<String>,
        username: impl Into<String>,
    ) -> RemoveUserFromGroupRequest<'a> {
        RemoveUserFromGroupRequest::new(self.client, groupname, username)
    }

    /// Returns groups with substrings matching a given query
    pub fn find_groups(&self) -> FindGroupsRequest<'a> {
        FindGroupsRequest::new(self.client)
    }
}

/// Creates a group by given group parameter
pub struct CreateGroupRequest<'a> {
    client: &'a crate::core::Client,
    add_group: Option<AddGroup>,
}

impl<'a> CreateGroupRequest<'a> {
    fn new(client: &'a crate::core::Client) -> Self {
        Self { client, add_group: None }
    }

    #[must_use]
    pub fn add_group(mut self, value: AddGroup) -> Self {
        self.add_group = Some(value);

        self
    }

    /// The request as the transport will send it.
    pub fn config(&self) -> crate::core::Result<crate::core::RequestConfig> {
        let mut config = crate::core::RequestConfig::new(crate::core::Method::POST, "/rest/api/2/group".to_owned());

        let body = match serde_json::to_value(&self.add_group)? {
            serde_json::Value::Object(object) => object,
            _ => serde_json::Map::new(),
        };

        config.body = Some(crate::core::Body::Json(serde_json::Value::Object(body)));

        Ok(config)
    }

    /// Sends the request.
    pub async fn send(self) -> crate::core::Result<Group> {
        self.client.send(&self.config()?).await
    }

    /// Sends the request and hands back the body unmodelled.
    pub async fn send_raw(self) -> crate::core::Result<serde_json::Value> {
        self.client.send_raw(&self.config()?).await
    }
}

/// Deletes a group by given group parameter
pub struct RemoveGroupRequest<'a> {
    client: &'a crate::core::Client,
    groupname: String,
    swap_group: Option<String>,
}

impl<'a> RemoveGroupRequest<'a> {
    fn new(client: &'a crate::core::Client, groupname: impl Into<String>) -> Self {
        Self { client, groupname: groupname.into(), swap_group: None }
    }

    /// A different group to transfer the restrictions to.
    #[must_use]
    pub fn swap_group(mut self, value: impl Into<String>) -> Self {
        self.swap_group = Some(value.into());

        self
    }

    /// The request as the transport will send it.
    pub fn config(&self) -> crate::core::Result<crate::core::RequestConfig> {
        let mut config = crate::core::RequestConfig::new(crate::core::Method::DELETE, "/rest/api/2/group".to_owned());

        config.query.push(("groupname".to_owned(), crate::core::QueryValue::Scalar(self.groupname.clone())));

        if let Some(value) = &self.swap_group {
            config.query.push(("swapGroup".to_owned(), crate::core::QueryValue::Scalar(value.clone())));
        }

        Ok(config)
    }

    /// Sends the request.
    pub async fn send(self) -> crate::core::Result<()> {
        self.client.send_empty(&self.config()?).await
    }

    /// Sends the request and hands back the body unmodelled.
    pub async fn send_raw(self) -> crate::core::Result<serde_json::Value> {
        self.client.send_raw(&self.config()?).await
    }
}

/// Returns a paginated list of users who are members of the specified group and its subgroups
pub struct GetUsersFromGroupRequest<'a> {
    client: &'a crate::core::Client,
    include_inactive_users: Option<String>,
    max_results: Option<String>,
    groupname: String,
    start_at: Option<String>,
}

impl<'a> GetUsersFromGroupRequest<'a> {
    fn new(client: &'a crate::core::Client, groupname: impl Into<String>) -> Self {
        Self { client, groupname: groupname.into(), include_inactive_users: None, max_results: None, start_at: None }
    }

    /// Include inactive users.
    #[must_use]
    pub fn include_inactive_users(mut self, value: impl Into<String>) -> Self {
        self.include_inactive_users = Some(value.into());

        self
    }

    /// The maximum number of users to return.
    #[must_use]
    pub fn max_results(mut self, value: impl Into<String>) -> Self {
        self.max_results = Some(value.into());

        self
    }

    /// The index of the first user in group to return.
    #[must_use]
    pub fn start_at(mut self, value: impl Into<String>) -> Self {
        self.start_at = Some(value.into());

        self
    }

    /// The request as the transport will send it.
    pub fn config(&self) -> crate::core::Result<crate::core::RequestConfig> {
        let mut config =
            crate::core::RequestConfig::new(crate::core::Method::GET, "/rest/api/2/group/member".to_owned());

        if let Some(value) = &self.include_inactive_users {
            config.query.push(("includeInactiveUsers".to_owned(), crate::core::QueryValue::Scalar(value.clone())));
        }

        if let Some(value) = &self.max_results {
            config.query.push(("maxResults".to_owned(), crate::core::QueryValue::Scalar(value.clone())));
        }

        config.query.push(("groupname".to_owned(), crate::core::QueryValue::Scalar(self.groupname.clone())));

        if let Some(value) = &self.start_at {
            config.query.push(("startAt".to_owned(), crate::core::QueryValue::Scalar(value.clone())));
        }

        Ok(config)
    }

    /// Sends the request.
    pub async fn send(self) -> crate::core::Result<GetUsersFromGroup> {
        self.client.send(&self.config()?).await
    }

    /// Sends the request and hands back the body unmodelled.
    pub async fn send_raw(self) -> crate::core::Result<serde_json::Value> {
        self.client.send_raw(&self.config()?).await
    }
}

/// Adds given user to a group
pub struct AddUserToGroupRequest<'a> {
    client: &'a crate::core::Client,
    groupname: String,
    update_user_to_group: Option<UpdateUserToGroup>,
}

impl<'a> AddUserToGroupRequest<'a> {
    fn new(client: &'a crate::core::Client, groupname: impl Into<String>) -> Self {
        Self { client, groupname: groupname.into(), update_user_to_group: None }
    }

    #[must_use]
    pub fn update_user_to_group(mut self, value: UpdateUserToGroup) -> Self {
        self.update_user_to_group = Some(value);

        self
    }

    /// The request as the transport will send it.
    pub fn config(&self) -> crate::core::Result<crate::core::RequestConfig> {
        let mut config =
            crate::core::RequestConfig::new(crate::core::Method::POST, "/rest/api/2/group/user".to_owned());

        config.query.push(("groupname".to_owned(), crate::core::QueryValue::Scalar(self.groupname.clone())));

        let body = match serde_json::to_value(&self.update_user_to_group)? {
            serde_json::Value::Object(object) => object,
            _ => serde_json::Map::new(),
        };

        config.body = Some(crate::core::Body::Json(serde_json::Value::Object(body)));

        Ok(config)
    }

    /// Sends the request.
    pub async fn send(self) -> crate::core::Result<Group> {
        self.client.send(&self.config()?).await
    }

    /// Sends the request and hands back the body unmodelled.
    pub async fn send_raw(self) -> crate::core::Result<serde_json::Value> {
        self.client.send_raw(&self.config()?).await
    }
}

/// Removes given user from a group
pub struct RemoveUserFromGroupRequest<'a> {
    client: &'a crate::core::Client,
    groupname: String,
    username: String,
}

impl<'a> RemoveUserFromGroupRequest<'a> {
    fn new(client: &'a crate::core::Client, groupname: impl Into<String>, username: impl Into<String>) -> Self {
        Self { client, groupname: groupname.into(), username: username.into() }
    }

    /// The request as the transport will send it.
    pub fn config(&self) -> crate::core::Result<crate::core::RequestConfig> {
        let mut config =
            crate::core::RequestConfig::new(crate::core::Method::DELETE, "/rest/api/2/group/user".to_owned());

        config.query.push(("groupname".to_owned(), crate::core::QueryValue::Scalar(self.groupname.clone())));

        config.query.push(("username".to_owned(), crate::core::QueryValue::Scalar(self.username.clone())));

        Ok(config)
    }

    /// Sends the request.
    pub async fn send(self) -> crate::core::Result<()> {
        self.client.send_empty(&self.config()?).await
    }

    /// Sends the request and hands back the body unmodelled.
    pub async fn send_raw(self) -> crate::core::Result<serde_json::Value> {
        self.client.send_raw(&self.config()?).await
    }
}

/// Returns groups with substrings matching a given query
pub struct FindGroupsRequest<'a> {
    client: &'a crate::core::Client,
    max_results: Option<String>,
    query: Option<String>,
    exclude: Option<String>,
    user_name: Option<String>,
}

impl<'a> FindGroupsRequest<'a> {
    fn new(client: &'a crate::core::Client) -> Self {
        Self { client, max_results: None, query: None, exclude: None, user_name: None }
    }

    /// Maximum number of results to return
    #[must_use]
    pub fn max_results(mut self, value: impl Into<String>) -> Self {
        self.max_results = Some(value.into());

        self
    }

    /// A String to match groups against
    #[must_use]
    pub fn query(mut self, value: impl Into<String>) -> Self {
        self.query = Some(value.into());

        self
    }

    /// List of groups to exclude
    #[must_use]
    pub fn exclude(mut self, value: impl Into<String>) -> Self {
        self.exclude = Some(value.into());

        self
    }

    /// Username for the context
    #[must_use]
    pub fn user_name(mut self, value: impl Into<String>) -> Self {
        self.user_name = Some(value.into());

        self
    }

    /// The request as the transport will send it.
    pub fn config(&self) -> crate::core::Result<crate::core::RequestConfig> {
        let mut config =
            crate::core::RequestConfig::new(crate::core::Method::GET, "/rest/api/2/groups/picker".to_owned());

        if let Some(value) = &self.max_results {
            config.query.push(("maxResults".to_owned(), crate::core::QueryValue::Scalar(value.clone())));
        }

        if let Some(value) = &self.query {
            config.query.push(("query".to_owned(), crate::core::QueryValue::Scalar(value.clone())));
        }

        if let Some(value) = &self.exclude {
            config.query.push(("exclude".to_owned(), crate::core::QueryValue::Scalar(value.clone())));
        }

        if let Some(value) = &self.user_name {
            config.query.push(("userName".to_owned(), crate::core::QueryValue::Scalar(value.clone())));
        }

        Ok(config)
    }

    /// Sends the request.
    pub async fn send(self) -> crate::core::Result<GroupSuggestions> {
        self.client.send(&self.config()?).await
    }

    /// Sends the request and hands back the body unmodelled.
    pub async fn send_raw(self) -> crate::core::Result<serde_json::Value> {
        self.client.send_raw(&self.config()?).await
    }
}
