// @generated by apis-code-gen. Do not edit: change the generator or the specification.

use super::super::models::*;

/// The ApplicationRoles operations.
pub struct ApplicationRolesService<'a> {
    client: &'a crate::core::Client,
}

impl<'a> ApplicationRolesService<'a> {
    pub(crate) fn new(client: &'a crate::core::Client) -> Self {
        Self { client }
    }

    /// Returns all application roles in the system.
    pub fn get_all(&self) -> GetAllRequest<'a> {
        GetAllRequest::new(self.client)
    }

    /// Updates the ApplicationRoles with the passed data if the version hash is the same as the server. Only the groups and default groups setting of the role may be updated. Requests to change the key or the name of the role will be silently ignored. It is acceptable to pass only the roles that are updated as roles that are present in the server but not in data to update with, will not be deleted.
    pub fn put_bulk(&self) -> PutBulkRequest<'a> {
        PutBulkRequest::new(self.client)
    }

    /// Returns the ApplicationRole with passed key if it exists.
    pub fn get_application_role(&self, key: impl Into<String>) -> GetApplicationRoleRequest<'a> {
        GetApplicationRoleRequest::new(self.client, key)
    }

    /// Updates the ApplicationRole with the passed data. Only the groups and default groups setting of the role may be updated. Requests to change the key or the name of the role will be silently ignored.
    pub fn update_application_role(&self, key: impl Into<String>) -> UpdateApplicationRoleRequest<'a> {
        UpdateApplicationRoleRequest::new(self.client, key)
    }
}

/// Returns all application roles in the system.
pub struct GetAllRequest<'a> {
    client: &'a crate::core::Client,
}

impl<'a> GetAllRequest<'a> {
    fn new(client: &'a crate::core::Client) -> Self {
        Self { client }
    }

    /// The request as the transport will send it.
    pub fn config(&self) -> crate::core::Result<crate::core::RequestConfig> {
        let config =
            crate::core::RequestConfig::new(crate::core::Method::GET, "/rest/api/2/applicationrole".to_owned());

        Ok(config)
    }

    /// Sends the request.
    pub async fn send(self) -> crate::core::Result<Vec<ApplicationRole>> {
        self.client.send(&self.config()?).await
    }

    /// Sends the request and hands back the body unmodelled.
    pub async fn send_raw(self) -> crate::core::Result<serde_json::Value> {
        self.client.send_raw(&self.config()?).await
    }
}

/// Updates the ApplicationRoles with the passed data if the version hash is the same as the server. Only the groups and default groups setting of the role may be updated. Requests to change the key or the name of the role will be silently ignored. It is acceptable to pass only the roles that are updated as roles that are present in the server but not in data to update with, will not be deleted.
pub struct PutBulkRequest<'a> {
    client: &'a crate::core::Client,
    if_match: Option<String>,
    application_role: Option<ApplicationRole>,
}

impl<'a> PutBulkRequest<'a> {
    fn new(client: &'a crate::core::Client) -> Self {
        Self {
            client,
            if_match: None,
            application_role: None,
        }
    }

    #[must_use]
    pub fn if_match(mut self, value: impl Into<String>) -> Self {
        self.if_match = Some(value.into());

        self
    }

    #[must_use]
    pub fn application_role(mut self, value: ApplicationRole) -> Self {
        self.application_role = Some(value);

        self
    }

    /// The request as the transport will send it.
    pub fn config(&self) -> crate::core::Result<crate::core::RequestConfig> {
        let mut config =
            crate::core::RequestConfig::new(crate::core::Method::PUT, "/rest/api/2/applicationrole".to_owned());

        if let Some(value) = &self.if_match {
            config.headers.push(("If-Match".to_owned(), value.clone()));
        }

        let body = match serde_json::to_value(&self.application_role)? {
            serde_json::Value::Object(object) => object,
            _ => serde_json::Map::new(),
        };

        config.body = Some(crate::core::Body::Json(serde_json::Value::Object(body)));

        Ok(config)
    }

    /// Sends the request.
    pub async fn send(self) -> crate::core::Result<ApplicationRole> {
        self.client.send(&self.config()?).await
    }

    /// Sends the request and hands back the body unmodelled.
    pub async fn send_raw(self) -> crate::core::Result<serde_json::Value> {
        self.client.send_raw(&self.config()?).await
    }
}

/// Returns the ApplicationRole with passed key if it exists.
pub struct GetApplicationRoleRequest<'a> {
    client: &'a crate::core::Client,
    key: String,
}

impl<'a> GetApplicationRoleRequest<'a> {
    fn new(client: &'a crate::core::Client, key: impl Into<String>) -> Self {
        Self {
            client,
            key: key.into(),
        }
    }

    /// The request as the transport will send it.
    pub fn config(&self) -> crate::core::Result<crate::core::RequestConfig> {
        let config = crate::core::RequestConfig::new(
            crate::core::Method::GET,
            format!("/rest/api/2/applicationrole/{}", self.key),
        );

        Ok(config)
    }

    /// Sends the request.
    pub async fn send(self) -> crate::core::Result<ApplicationRole> {
        self.client.send(&self.config()?).await
    }

    /// Sends the request and hands back the body unmodelled.
    pub async fn send_raw(self) -> crate::core::Result<serde_json::Value> {
        self.client.send_raw(&self.config()?).await
    }
}

/// Updates the ApplicationRole with the passed data. Only the groups and default groups setting of the role may be updated. Requests to change the key or the name of the role will be silently ignored.
pub struct UpdateApplicationRoleRequest<'a> {
    client: &'a crate::core::Client,
    if_match: Option<String>,
    version_hash: Option<String>,
    key: String,
    body: Option<ApplicationRole>,
}

impl<'a> UpdateApplicationRoleRequest<'a> {
    fn new(client: &'a crate::core::Client, key: impl Into<String>) -> Self {
        Self {
            client,
            key: key.into(),
            if_match: None,
            version_hash: None,
            body: None,
        }
    }

    #[must_use]
    pub fn if_match(mut self, value: impl Into<String>) -> Self {
        self.if_match = Some(value.into());

        self
    }

    /// the hash of the version to update. Optional Param
    #[must_use]
    pub fn version_hash(mut self, value: impl Into<String>) -> Self {
        self.version_hash = Some(value.into());

        self
    }

    #[must_use]
    pub fn body(mut self, value: ApplicationRole) -> Self {
        self.body = Some(value);

        self
    }

    /// The request as the transport will send it.
    pub fn config(&self) -> crate::core::Result<crate::core::RequestConfig> {
        let mut config = crate::core::RequestConfig::new(
            crate::core::Method::PUT,
            format!("/rest/api/2/applicationrole/{}", self.key),
        );

        if let Some(value) = &self.if_match {
            config.headers.push(("If-Match".to_owned(), value.clone()));
        }

        if let Some(value) = &self.version_hash {
            config.headers.push(("versionHash".to_owned(), value.clone()));
        }

        config.body = Some(crate::core::Body::Json(serde_json::to_value(&self.body)?));

        Ok(config)
    }

    /// Sends the request.
    pub async fn send(self) -> crate::core::Result<ApplicationRole> {
        self.client.send(&self.config()?).await
    }

    /// Sends the request and hands back the body unmodelled.
    pub async fn send_raw(self) -> crate::core::Result<serde_json::Value> {
        self.client.send_raw(&self.config()?).await
    }
}
