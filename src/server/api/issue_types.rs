// @generated by apis-code-gen. Do not edit: change the generator or the specification.

use super::super::models::*;

/// The IssueTypes operations.
pub struct IssueTypesService<'a> {
    client: &'a crate::core::Client,
}

impl<'a> IssueTypesService<'a> {
    pub(crate) fn new(client: &'a crate::core::Client) -> Self {
        Self { client }
    }

    /// Returns a list of all issue types visible to the user
    pub fn get_issue_all_types(&self) -> GetIssueAllTypesRequest<'a> {
        GetIssueAllTypesRequest::new(self.client)
    }

    /// Creates an issue type from a JSON representation and adds the issue newly created issue type to the default issue type scheme.
    pub fn create_issue_type(&self, issue_type_create: IssueTypeCreate) -> CreateIssueTypeRequest<'a> {
        CreateIssueTypeRequest::new(self.client, issue_type_create)
    }

    /// Returns paginated list of filtered issue types
    pub fn get_paginated_issue_types(&self) -> GetPaginatedIssueTypesRequest<'a> {
        GetPaginatedIssueTypesRequest::new(self.client)
    }

    /// Returns a full representation of the issue type that has the given id.
    pub fn get_issue_type(&self, id: impl Into<String>) -> GetIssueTypeRequest<'a> {
        GetIssueTypeRequest::new(self.client, id)
    }

    /// Updates the specified issue type from a JSON representation.
    pub fn update_issue_type(
        &self,
        id: impl Into<String>,
        issue_type_update: IssueTypeUpdate,
    ) -> UpdateIssueTypeRequest<'a> {
        UpdateIssueTypeRequest::new(self.client, id, issue_type_update)
    }

    /// Deletes the specified issue type. If the issue type has any associated issues, these issues will be migrated to the alternative issue type specified in the parameter.
    pub fn delete_issue_type(&self, id: impl Into<String>) -> DeleteIssueTypeRequest<'a> {
        DeleteIssueTypeRequest::new(self.client, id)
    }

    /// Returns a list of all alternative issue types for the given issue type id.
    pub fn get_alternative_issue_types(&self, id: impl Into<String>) -> GetAlternativeIssueTypesRequest<'a> {
        GetAlternativeIssueTypesRequest::new(self.client, id)
    }

    /// Converts temporary avatar into a real avatar
    pub fn create_issue_type_avatar_from_temporary(
        &self,
        id: impl Into<String>,
        avatar_cropping: AvatarCropping,
    ) -> CreateIssueTypeAvatarFromTemporaryRequest<'a> {
        CreateIssueTypeAvatarFromTemporaryRequest::new(self.client, id, avatar_cropping)
    }

    /// Creates temporary avatar using multipart. The response is sent back as JSON stored in a textarea. This is because
    /// the client uses remote iframing to submit avatars using multipart. So we must send them a valid HTML page back from
    /// which the client parses the JSON from.
    /// Creating a temporary avatar is part of a 3-step process in uploading a new
    /// avatar for an issue type: upload, crop, confirm. This endpoint allows you to use a multipart upload
    /// instead of sending the image directly as the request body.
    /// You *must* use "avatar" as the name of the upload parameter:
    /// curl -c cookiejar.txt -X POST -u admin:admin -H "X-Atlassian-Token: no-check" \
    ///   -F "avatar=@mynewavatar.png;type=image/png" \
    ///   'http://localhost:8090/jira/rest/api/2/issuetype/1/avatar/temporary'
    pub fn store_temporary_issue_type_avatar_using_multi_part(
        &self,
        id: impl Into<String>,
        avatar: impl IntoIterator<Item = crate::core::Attachment>,
    ) -> StoreTemporaryIssueTypeAvatarUsingMultiPartRequest<'a> {
        StoreTemporaryIssueTypeAvatarUsingMultiPartRequest::new(self.client, id, avatar)
    }

    /// Returns the keys of all properties for the issue type identified by the id
    pub fn get_issue_type_property_keys(
        &self,
        issue_type_id: impl Into<String>,
    ) -> GetIssueTypePropertyKeysRequest<'a> {
        GetIssueTypePropertyKeysRequest::new(self.client, issue_type_id)
    }

    /// Returns the value of the property with a given key from the issue type identified by the id
    pub fn get_issue_type_property(
        &self,
        property_key: impl Into<String>,
        issue_type_id: impl Into<String>,
    ) -> GetIssueTypePropertyRequest<'a> {
        GetIssueTypePropertyRequest::new(self.client, property_key, issue_type_id)
    }

    /// Sets the value of the specified issue type's property
    pub fn set_issue_type_property(
        &self,
        property_key: impl Into<String>,
        issue_type_id: impl Into<String>,
        body: std::collections::HashMap<String, serde_json::Value>,
    ) -> SetIssueTypePropertyRequest<'a> {
        SetIssueTypePropertyRequest::new(self.client, property_key, issue_type_id, body)
    }

    /// Removes the property from the issue type identified by the id
    pub fn delete_issue_type_property(
        &self,
        property_key: impl Into<String>,
        issue_type_id: impl Into<String>,
    ) -> DeleteIssueTypePropertyRequest<'a> {
        DeleteIssueTypePropertyRequest::new(self.client, property_key, issue_type_id)
    }
}

/// Returns a list of all issue types visible to the user
pub struct GetIssueAllTypesRequest<'a> {
    client: &'a crate::core::Client,
}

impl<'a> GetIssueAllTypesRequest<'a> {
    fn new(client: &'a crate::core::Client) -> Self {
        Self { client }
    }

    /// The request as the transport will send it.
    pub fn config(&self) -> crate::core::Result<crate::core::RequestConfig> {
        let config = crate::core::RequestConfig::new(crate::core::Method::GET, "/rest/api/2/issuetype".to_owned());

        Ok(config)
    }

    /// Sends the request.
    pub async fn send(self) -> crate::core::Result<Vec<IssueTypeJson>> {
        self.client.send(&self.config()?).await
    }

    /// Sends the request and hands back the body unmodelled.
    pub async fn send_raw(self) -> crate::core::Result<serde_json::Value> {
        self.client.send_raw(&self.config()?).await
    }
}

/// Creates an issue type from a JSON representation and adds the issue newly created issue type to the default issue type scheme.
pub struct CreateIssueTypeRequest<'a> {
    client: &'a crate::core::Client,
    issue_type_create: IssueTypeCreate,
}

impl<'a> CreateIssueTypeRequest<'a> {
    fn new(client: &'a crate::core::Client, issue_type_create: IssueTypeCreate) -> Self {
        Self { client, issue_type_create }
    }

    /// The request as the transport will send it.
    pub fn config(&self) -> crate::core::Result<crate::core::RequestConfig> {
        let mut config = crate::core::RequestConfig::new(crate::core::Method::POST, "/rest/api/2/issuetype".to_owned());

        let body = match serde_json::to_value(&self.issue_type_create)? {
            serde_json::Value::Object(object) => object,
            _ => serde_json::Map::new(),
        };

        config.body = Some(crate::core::Body::Json(serde_json::Value::Object(body)));

        Ok(config)
    }

    /// Sends the request.
    pub async fn send(self) -> crate::core::Result<IssueTypeJson> {
        self.client.send(&self.config()?).await
    }

    /// Sends the request and hands back the body unmodelled.
    pub async fn send_raw(self) -> crate::core::Result<serde_json::Value> {
        self.client.send_raw(&self.config()?).await
    }
}

/// Returns paginated list of filtered issue types
pub struct GetPaginatedIssueTypesRequest<'a> {
    client: &'a crate::core::Client,
    x_requested_with: Option<String>,
    max_results: Option<i64>,
    query: Option<String>,
    project_ids: Option<Vec<i64>>,
    start_at: Option<i64>,
}

impl<'a> GetPaginatedIssueTypesRequest<'a> {
    fn new(client: &'a crate::core::Client) -> Self {
        Self { client, x_requested_with: None, max_results: None, query: None, project_ids: None, start_at: None }
    }

    #[must_use]
    pub fn x_requested_with(mut self, value: impl Into<String>) -> Self {
        self.x_requested_with = Some(value.into());

        self
    }

    /// The maximum number of issue types to return.
    #[must_use]
    pub fn max_results(mut self, value: i64) -> Self {
        self.max_results = Some(value);

        self
    }

    /// The string that issue type names will be matched with.
    #[must_use]
    pub fn query(mut self, value: impl Into<String>) -> Self {
        self.query = Some(value.into());

        self
    }

    /// The set of project ids to filter issue types.
    #[must_use]
    pub fn project_ids(mut self, value: impl IntoIterator<Item = i64>) -> Self {
        self.project_ids = Some(value.into_iter().collect());

        self
    }

    /// The index of the first issue type to return.
    #[must_use]
    pub fn start_at(mut self, value: i64) -> Self {
        self.start_at = Some(value);

        self
    }

    /// The request as the transport will send it.
    pub fn config(&self) -> crate::core::Result<crate::core::RequestConfig> {
        let mut config =
            crate::core::RequestConfig::new(crate::core::Method::GET, "/rest/api/2/issuetype/page".to_owned());

        if let Some(value) = &self.max_results {
            config.query.push(("maxResults".to_owned(), crate::core::QueryValue::Scalar(value.to_string())));
        }

        if let Some(value) = &self.query {
            config.query.push(("query".to_owned(), crate::core::QueryValue::Scalar(value.clone())));
        }

        if let Some(value) = &self.project_ids {
            config.query.push(("projectIds".to_owned(), crate::core::QueryValue::from_serializable(value)?));
        }

        if let Some(value) = &self.start_at {
            config.query.push(("startAt".to_owned(), crate::core::QueryValue::Scalar(value.to_string())));
        }

        if let Some(value) = &self.x_requested_with {
            config.headers.push(("X-Requested-With".to_owned(), value.clone()));
        }

        Ok(config)
    }

    /// Sends the request.
    pub async fn send(self) -> crate::core::Result<IssueTypeJson> {
        self.client.send(&self.config()?).await
    }

    /// Sends the request and hands back the body unmodelled.
    pub async fn send_raw(self) -> crate::core::Result<serde_json::Value> {
        self.client.send_raw(&self.config()?).await
    }
}

/// Returns a full representation of the issue type that has the given id.
pub struct GetIssueTypeRequest<'a> {
    client: &'a crate::core::Client,
    id: String,
}

impl<'a> GetIssueTypeRequest<'a> {
    fn new(client: &'a crate::core::Client, id: impl Into<String>) -> Self {
        Self { client, id: id.into() }
    }

    /// The request as the transport will send it.
    pub fn config(&self) -> crate::core::Result<crate::core::RequestConfig> {
        let config =
            crate::core::RequestConfig::new(crate::core::Method::GET, format!("/rest/api/2/issuetype/{}", self.id));

        Ok(config)
    }

    /// Sends the request.
    pub async fn send(self) -> crate::core::Result<IssueTypeJson> {
        self.client.send(&self.config()?).await
    }

    /// Sends the request and hands back the body unmodelled.
    pub async fn send_raw(self) -> crate::core::Result<serde_json::Value> {
        self.client.send_raw(&self.config()?).await
    }
}

/// Updates the specified issue type from a JSON representation.
pub struct UpdateIssueTypeRequest<'a> {
    client: &'a crate::core::Client,
    id: String,
    issue_type_update: IssueTypeUpdate,
}

impl<'a> UpdateIssueTypeRequest<'a> {
    fn new(client: &'a crate::core::Client, id: impl Into<String>, issue_type_update: IssueTypeUpdate) -> Self {
        Self { client, id: id.into(), issue_type_update }
    }

    /// The request as the transport will send it.
    pub fn config(&self) -> crate::core::Result<crate::core::RequestConfig> {
        let mut config =
            crate::core::RequestConfig::new(crate::core::Method::PUT, format!("/rest/api/2/issuetype/{}", self.id));

        let body = match serde_json::to_value(&self.issue_type_update)? {
            serde_json::Value::Object(object) => object,
            _ => serde_json::Map::new(),
        };

        config.body = Some(crate::core::Body::Json(serde_json::Value::Object(body)));

        Ok(config)
    }

    /// Sends the request.
    pub async fn send(self) -> crate::core::Result<IssueTypeJson> {
        self.client.send(&self.config()?).await
    }

    /// Sends the request and hands back the body unmodelled.
    pub async fn send_raw(self) -> crate::core::Result<serde_json::Value> {
        self.client.send_raw(&self.config()?).await
    }
}

/// Deletes the specified issue type. If the issue type has any associated issues, these issues will be migrated to the alternative issue type specified in the parameter.
pub struct DeleteIssueTypeRequest<'a> {
    client: &'a crate::core::Client,
    id: String,
    alternative_issue_type_id: Option<String>,
}

impl<'a> DeleteIssueTypeRequest<'a> {
    fn new(client: &'a crate::core::Client, id: impl Into<String>) -> Self {
        Self { client, id: id.into(), alternative_issue_type_id: None }
    }

    /// The id of an issue type to which issues associated with the removed issue type will be migrated.
    #[must_use]
    pub fn alternative_issue_type_id(mut self, value: impl Into<String>) -> Self {
        self.alternative_issue_type_id = Some(value.into());

        self
    }

    /// The request as the transport will send it.
    pub fn config(&self) -> crate::core::Result<crate::core::RequestConfig> {
        let mut config =
            crate::core::RequestConfig::new(crate::core::Method::DELETE, format!("/rest/api/2/issuetype/{}", self.id));

        if let Some(value) = &self.alternative_issue_type_id {
            config.query.push(("alternativeIssueTypeId".to_owned(), crate::core::QueryValue::Scalar(value.clone())));
        }

        Ok(config)
    }

    /// Sends the request.
    pub async fn send(self) -> crate::core::Result<()> {
        self.client.send_empty(&self.config()?).await
    }

    /// Sends the request and hands back the body unmodelled.
    pub async fn send_raw(self) -> crate::core::Result<serde_json::Value> {
        self.client.send_raw(&self.config()?).await
    }
}

/// Returns a list of all alternative issue types for the given issue type id.
pub struct GetAlternativeIssueTypesRequest<'a> {
    client: &'a crate::core::Client,
    id: String,
}

impl<'a> GetAlternativeIssueTypesRequest<'a> {
    fn new(client: &'a crate::core::Client, id: impl Into<String>) -> Self {
        Self { client, id: id.into() }
    }

    /// The request as the transport will send it.
    pub fn config(&self) -> crate::core::Result<crate::core::RequestConfig> {
        let config = crate::core::RequestConfig::new(
            crate::core::Method::GET,
            format!("/rest/api/2/issuetype/{}/alternatives", self.id),
        );

        Ok(config)
    }

    /// Sends the request.
    pub async fn send(self) -> crate::core::Result<Vec<IssueTypeJson>> {
        self.client.send(&self.config()?).await
    }

    /// Sends the request and hands back the body unmodelled.
    pub async fn send_raw(self) -> crate::core::Result<serde_json::Value> {
        self.client.send_raw(&self.config()?).await
    }
}

/// Converts temporary avatar into a real avatar
pub struct CreateIssueTypeAvatarFromTemporaryRequest<'a> {
    client: &'a crate::core::Client,
    id: String,
    avatar_cropping: AvatarCropping,
}

impl<'a> CreateIssueTypeAvatarFromTemporaryRequest<'a> {
    fn new(client: &'a crate::core::Client, id: impl Into<String>, avatar_cropping: AvatarCropping) -> Self {
        Self { client, id: id.into(), avatar_cropping }
    }

    /// The request as the transport will send it.
    pub fn config(&self) -> crate::core::Result<crate::core::RequestConfig> {
        let mut config = crate::core::RequestConfig::new(
            crate::core::Method::POST,
            format!("/rest/api/2/issuetype/{}/avatar", self.id),
        );

        let body = match serde_json::to_value(&self.avatar_cropping)? {
            serde_json::Value::Object(object) => object,
            _ => serde_json::Map::new(),
        };

        config.body = Some(crate::core::Body::Json(serde_json::Value::Object(body)));

        Ok(config)
    }

    /// Sends the request.
    pub async fn send(self) -> crate::core::Result<Avatar> {
        self.client.send(&self.config()?).await
    }

    /// Sends the request and hands back the body unmodelled.
    pub async fn send_raw(self) -> crate::core::Result<serde_json::Value> {
        self.client.send_raw(&self.config()?).await
    }
}

/// Creates temporary avatar using multipart. The response is sent back as JSON stored in a textarea. This is because
/// the client uses remote iframing to submit avatars using multipart. So we must send them a valid HTML page back from
/// which the client parses the JSON from.
/// Creating a temporary avatar is part of a 3-step process in uploading a new
/// avatar for an issue type: upload, crop, confirm. This endpoint allows you to use a multipart upload
/// instead of sending the image directly as the request body.
/// You *must* use "avatar" as the name of the upload parameter:
/// curl -c cookiejar.txt -X POST -u admin:admin -H "X-Atlassian-Token: no-check" \
///   -F "avatar=@mynewavatar.png;type=image/png" \
///   'http://localhost:8090/jira/rest/api/2/issuetype/1/avatar/temporary'
pub struct StoreTemporaryIssueTypeAvatarUsingMultiPartRequest<'a> {
    client: &'a crate::core::Client,
    id: String,
    avatar: Vec<crate::core::Attachment>,
}

impl<'a> StoreTemporaryIssueTypeAvatarUsingMultiPartRequest<'a> {
    fn new(
        client: &'a crate::core::Client,
        id: impl Into<String>,
        avatar: impl IntoIterator<Item = crate::core::Attachment>,
    ) -> Self {
        Self { client, id: id.into(), avatar: avatar.into_iter().collect() }
    }

    /// The request as the transport will send it.
    pub fn config(&self) -> crate::core::Result<crate::core::RequestConfig> {
        let mut config = crate::core::RequestConfig::new(
            crate::core::Method::POST,
            format!("/rest/api/2/issuetype/{}/avatar/temporary", self.id),
        );

        config.headers.push(("X-Atlassian-Token".to_owned(), "no-check".to_owned()));

        config.body =
            Some(crate::core::Body::Multipart(crate::core::MultipartBody::new("avatar", self.avatar.clone())));

        Ok(config)
    }

    /// Sends the request.
    pub async fn send(self) -> crate::core::Result<serde_json::Value> {
        self.client.send(&self.config()?).await
    }

    /// Sends the request and hands back the body unmodelled.
    pub async fn send_raw(self) -> crate::core::Result<serde_json::Value> {
        self.client.send_raw(&self.config()?).await
    }
}

/// Returns the keys of all properties for the issue type identified by the id
pub struct GetIssueTypePropertyKeysRequest<'a> {
    client: &'a crate::core::Client,
    issue_type_id: String,
}

impl<'a> GetIssueTypePropertyKeysRequest<'a> {
    fn new(client: &'a crate::core::Client, issue_type_id: impl Into<String>) -> Self {
        Self { client, issue_type_id: issue_type_id.into() }
    }

    /// The request as the transport will send it.
    pub fn config(&self) -> crate::core::Result<crate::core::RequestConfig> {
        let config = crate::core::RequestConfig::new(
            crate::core::Method::GET,
            format!("/rest/api/2/issuetype/{}/properties", self.issue_type_id),
        );

        Ok(config)
    }

    /// Sends the request.
    pub async fn send(self) -> crate::core::Result<EntityPropertiesKeys> {
        self.client.send(&self.config()?).await
    }

    /// Sends the request and hands back the body unmodelled.
    pub async fn send_raw(self) -> crate::core::Result<serde_json::Value> {
        self.client.send_raw(&self.config()?).await
    }
}

/// Returns the value of the property with a given key from the issue type identified by the id
pub struct GetIssueTypePropertyRequest<'a> {
    client: &'a crate::core::Client,
    property_key: String,
    issue_type_id: String,
}

impl<'a> GetIssueTypePropertyRequest<'a> {
    fn new(client: &'a crate::core::Client, property_key: impl Into<String>, issue_type_id: impl Into<String>) -> Self {
        Self { client, property_key: property_key.into(), issue_type_id: issue_type_id.into() }
    }

    /// The request as the transport will send it.
    pub fn config(&self) -> crate::core::Result<crate::core::RequestConfig> {
        let config = crate::core::RequestConfig::new(
            crate::core::Method::GET,
            format!("/rest/api/2/issuetype/{}/properties/{}", self.issue_type_id, self.property_key),
        );

        Ok(config)
    }

    /// Sends the request.
    pub async fn send(self) -> crate::core::Result<EntityProperty> {
        self.client.send(&self.config()?).await
    }

    /// Sends the request and hands back the body unmodelled.
    pub async fn send_raw(self) -> crate::core::Result<serde_json::Value> {
        self.client.send_raw(&self.config()?).await
    }
}

/// Sets the value of the specified issue type's property
pub struct SetIssueTypePropertyRequest<'a> {
    client: &'a crate::core::Client,
    property_key: String,
    issue_type_id: String,
    body: std::collections::HashMap<String, serde_json::Value>,
}

impl<'a> SetIssueTypePropertyRequest<'a> {
    fn new(
        client: &'a crate::core::Client,
        property_key: impl Into<String>,
        issue_type_id: impl Into<String>,
        body: std::collections::HashMap<String, serde_json::Value>,
    ) -> Self {
        Self { client, property_key: property_key.into(), issue_type_id: issue_type_id.into(), body }
    }

    /// The request as the transport will send it.
    pub fn config(&self) -> crate::core::Result<crate::core::RequestConfig> {
        let mut config = crate::core::RequestConfig::new(
            crate::core::Method::PUT,
            format!("/rest/api/2/issuetype/{}/properties/{}", self.issue_type_id, self.property_key),
        );

        config.body = Some(crate::core::Body::Json(serde_json::to_value(&self.body)?));

        Ok(config)
    }

    /// Sends the request.
    pub async fn send(self) -> crate::core::Result<()> {
        self.client.send_empty(&self.config()?).await
    }

    /// Sends the request and hands back the body unmodelled.
    pub async fn send_raw(self) -> crate::core::Result<serde_json::Value> {
        self.client.send_raw(&self.config()?).await
    }
}

/// Removes the property from the issue type identified by the id
pub struct DeleteIssueTypePropertyRequest<'a> {
    client: &'a crate::core::Client,
    property_key: String,
    issue_type_id: String,
}

impl<'a> DeleteIssueTypePropertyRequest<'a> {
    fn new(client: &'a crate::core::Client, property_key: impl Into<String>, issue_type_id: impl Into<String>) -> Self {
        Self { client, property_key: property_key.into(), issue_type_id: issue_type_id.into() }
    }

    /// The request as the transport will send it.
    pub fn config(&self) -> crate::core::Result<crate::core::RequestConfig> {
        let config = crate::core::RequestConfig::new(
            crate::core::Method::DELETE,
            format!("/rest/api/2/issuetype/{}/properties/{}", self.issue_type_id, self.property_key),
        );

        Ok(config)
    }

    /// Sends the request.
    pub async fn send(self) -> crate::core::Result<()> {
        self.client.send_empty(&self.config()?).await
    }

    /// Sends the request and hands back the body unmodelled.
    pub async fn send_raw(self) -> crate::core::Result<serde_json::Value> {
        self.client.send_raw(&self.config()?).await
    }
}
