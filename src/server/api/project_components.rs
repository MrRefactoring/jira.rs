// @generated by apis-code-gen. Do not edit: change the generator or the specification.

use super::super::models::*;

/// The ProjectComponents operations.
pub struct ProjectComponentsService<'a> {
    client: &'a crate::core::Client,
}

impl<'a> ProjectComponentsService<'a> {
    pub(crate) fn new(client: &'a crate::core::Client) -> Self {
        Self { client }
    }

    /// Create a component via POST.
    pub fn create_component(&self) -> CreateComponentRequest<'a> {
        CreateComponentRequest::new(self.client)
    }

    /// Returns paginated list of filtered active components
    pub fn get_paginated_components(&self) -> GetPaginatedComponentsRequest<'a> {
        GetPaginatedComponentsRequest::new(self.client)
    }

    /// Returns a project component.
    pub fn get_component(&self, id: impl Into<String>) -> GetComponentRequest<'a> {
        GetComponentRequest::new(self.client, id)
    }

    /// Modify a component via PUT. Any fields present in the PUT will override existing values. As a convenience, if a field is not present, it is silently ignored.
    pub fn update_component(&self, id: impl Into<String>) -> UpdateComponentRequest<'a> {
        UpdateComponentRequest::new(self.client, id)
    }

    /// Delete a project component.
    pub fn delete_component(&self, id: impl Into<String>) -> DeleteComponentRequest<'a> {
        DeleteComponentRequest::new(self.client, id)
    }

    /// Returns counts of issues related to this component.
    pub fn get_component_related_issues(&self, id: impl Into<String>) -> GetComponentRelatedIssuesRequest<'a> {
        GetComponentRelatedIssuesRequest::new(self.client, id)
    }
}

/// Create a component via POST.
pub struct CreateComponentRequest<'a> {
    client: &'a crate::core::Client,
    component: Option<Component>,
}

impl<'a> CreateComponentRequest<'a> {
    fn new(client: &'a crate::core::Client) -> Self {
        Self { client, component: None }
    }

    #[must_use]
    pub fn component(mut self, value: Component) -> Self {
        self.component = Some(value);

        self
    }

    /// The request as the transport will send it.
    pub fn config(&self) -> crate::core::Result<crate::core::RequestConfig> {
        let mut config = crate::core::RequestConfig::new(crate::core::Method::POST, "/rest/api/2/component".to_owned());

        let body = match serde_json::to_value(&self.component)? {
            serde_json::Value::Object(object) => object,
            _ => serde_json::Map::new(),
        };

        config.body = Some(crate::core::Body::Json(serde_json::Value::Object(body)));

        Ok(config)
    }

    /// Sends the request.
    pub async fn send(self) -> crate::core::Result<Component> {
        self.client.send(&self.config()?).await
    }

    /// Sends the request and hands back the body unmodelled.
    pub async fn send_raw(self) -> crate::core::Result<serde_json::Value> {
        self.client.send_raw(&self.config()?).await
    }
}

/// Returns paginated list of filtered active components
pub struct GetPaginatedComponentsRequest<'a> {
    client: &'a crate::core::Client,
    max_results: Option<String>,
    query: Option<String>,
    project_ids: Option<String>,
    start_at: Option<String>,
}

impl<'a> GetPaginatedComponentsRequest<'a> {
    fn new(client: &'a crate::core::Client) -> Self {
        Self { client, max_results: None, query: None, project_ids: None, start_at: None }
    }

    /// the maximum number of components to return
    #[must_use]
    pub fn max_results(mut self, value: impl Into<String>) -> Self {
        self.max_results = Some(value.into());

        self
    }

    /// the string that components names will be matched with
    #[must_use]
    pub fn query(mut self, value: impl Into<String>) -> Self {
        self.query = Some(value.into());

        self
    }

    /// the set of project ids to filter components
    #[must_use]
    pub fn project_ids(mut self, value: impl Into<String>) -> Self {
        self.project_ids = Some(value.into());

        self
    }

    /// the index of the first components to return
    #[must_use]
    pub fn start_at(mut self, value: impl Into<String>) -> Self {
        self.start_at = Some(value.into());

        self
    }

    /// The request as the transport will send it.
    pub fn config(&self) -> crate::core::Result<crate::core::RequestConfig> {
        let mut config =
            crate::core::RequestConfig::new(crate::core::Method::GET, "/rest/api/2/component/page".to_owned());

        if let Some(value) = &self.max_results {
            config.query.push(("maxResults".to_owned(), crate::core::QueryValue::Scalar(value.clone())));
        }

        if let Some(value) = &self.query {
            config.query.push(("query".to_owned(), crate::core::QueryValue::Scalar(value.clone())));
        }

        if let Some(value) = &self.project_ids {
            config.query.push(("projectIds".to_owned(), crate::core::QueryValue::Scalar(value.clone())));
        }

        if let Some(value) = &self.start_at {
            config.query.push(("startAt".to_owned(), crate::core::QueryValue::Scalar(value.clone())));
        }

        Ok(config)
    }

    /// Sends the request.
    pub async fn send(self) -> crate::core::Result<PagedResults> {
        self.client.send(&self.config()?).await
    }

    /// Sends the request and hands back the body unmodelled.
    pub async fn send_raw(self) -> crate::core::Result<serde_json::Value> {
        self.client.send_raw(&self.config()?).await
    }
}

/// Returns a project component.
pub struct GetComponentRequest<'a> {
    client: &'a crate::core::Client,
    id: String,
}

impl<'a> GetComponentRequest<'a> {
    fn new(client: &'a crate::core::Client, id: impl Into<String>) -> Self {
        Self { client, id: id.into() }
    }

    /// The request as the transport will send it.
    pub fn config(&self) -> crate::core::Result<crate::core::RequestConfig> {
        let config =
            crate::core::RequestConfig::new(crate::core::Method::GET, format!("/rest/api/2/component/{}", self.id));

        Ok(config)
    }

    /// Sends the request.
    pub async fn send(self) -> crate::core::Result<Component> {
        self.client.send(&self.config()?).await
    }

    /// Sends the request and hands back the body unmodelled.
    pub async fn send_raw(self) -> crate::core::Result<serde_json::Value> {
        self.client.send_raw(&self.config()?).await
    }
}

/// Modify a component via PUT. Any fields present in the PUT will override existing values. As a convenience, if a field is not present, it is silently ignored.
pub struct UpdateComponentRequest<'a> {
    client: &'a crate::core::Client,
    id: String,
    body: Option<Component>,
}

impl<'a> UpdateComponentRequest<'a> {
    fn new(client: &'a crate::core::Client, id: impl Into<String>) -> Self {
        Self { client, id: id.into(), body: None }
    }

    #[must_use]
    pub fn body(mut self, value: Component) -> Self {
        self.body = Some(value);

        self
    }

    /// The request as the transport will send it.
    pub fn config(&self) -> crate::core::Result<crate::core::RequestConfig> {
        let mut config =
            crate::core::RequestConfig::new(crate::core::Method::PUT, format!("/rest/api/2/component/{}", self.id));

        config.body = Some(crate::core::Body::Json(serde_json::to_value(&self.body)?));

        Ok(config)
    }

    /// Sends the request.
    pub async fn send(self) -> crate::core::Result<Component> {
        self.client.send(&self.config()?).await
    }

    /// Sends the request and hands back the body unmodelled.
    pub async fn send_raw(self) -> crate::core::Result<serde_json::Value> {
        self.client.send_raw(&self.config()?).await
    }
}

/// Delete a project component.
pub struct DeleteComponentRequest<'a> {
    client: &'a crate::core::Client,
    move_issues_to: Option<String>,
    id: String,
}

impl<'a> DeleteComponentRequest<'a> {
    fn new(client: &'a crate::core::Client, id: impl Into<String>) -> Self {
        Self { client, id: id.into(), move_issues_to: None }
    }

    /// The new component applied to issues whose 'id' component will be deleted. If this value is null, then the 'id' component is simply removed from the related isues.
    #[must_use]
    pub fn move_issues_to(mut self, value: impl Into<String>) -> Self {
        self.move_issues_to = Some(value.into());

        self
    }

    /// The request as the transport will send it.
    pub fn config(&self) -> crate::core::Result<crate::core::RequestConfig> {
        let mut config =
            crate::core::RequestConfig::new(crate::core::Method::DELETE, format!("/rest/api/2/component/{}", self.id));

        if let Some(value) = &self.move_issues_to {
            config.query.push(("moveIssuesTo".to_owned(), crate::core::QueryValue::Scalar(value.clone())));
        }

        Ok(config)
    }

    /// Sends the request.
    pub async fn send(self) -> crate::core::Result<()> {
        self.client.send_empty(&self.config()?).await
    }

    /// Sends the request and hands back the body unmodelled.
    pub async fn send_raw(self) -> crate::core::Result<serde_json::Value> {
        self.client.send_raw(&self.config()?).await
    }
}

/// Returns counts of issues related to this component.
pub struct GetComponentRelatedIssuesRequest<'a> {
    client: &'a crate::core::Client,
    id: String,
}

impl<'a> GetComponentRelatedIssuesRequest<'a> {
    fn new(client: &'a crate::core::Client, id: impl Into<String>) -> Self {
        Self { client, id: id.into() }
    }

    /// The request as the transport will send it.
    pub fn config(&self) -> crate::core::Result<crate::core::RequestConfig> {
        let config = crate::core::RequestConfig::new(
            crate::core::Method::GET,
            format!("/rest/api/2/component/{}/relatedIssueCounts", self.id),
        );

        Ok(config)
    }

    /// Sends the request.
    pub async fn send(self) -> crate::core::Result<ComponentIssueCounts> {
        self.client.send(&self.config()?).await
    }

    /// Sends the request and hands back the body unmodelled.
    pub async fn send_raw(self) -> crate::core::Result<serde_json::Value> {
        self.client.send_raw(&self.config()?).await
    }
}
