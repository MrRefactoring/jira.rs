// @generated by apis-code-gen. Do not edit: change the generator or the specification.

use super::super::models::*;

/// The Progress operations.
pub struct ProgressService<'a> {
    client: &'a crate::core::Client,
}

impl<'a> ProgressService<'a> {
    pub(crate) fn new(client: &'a crate::core::Client) -> Self {
        Self { client }
    }

    /// Get progress of ongoing tasks
    /// Retrieve the progress of the most recent reindex task `/progress/category/insight-reindex/reindex`
    /// Retrieve the progress of an import task `/progress/category/imports/{id}`.
    pub fn get_progress_for_category_and_resource_id(
        &self,
        resourceid: impl Into<String>,
        category: impl Into<String>,
    ) -> GetProgressForCategoryAndResourceIdRequest<'a> {
        GetProgressForCategoryAndResourceIdRequest::new(self.client, resourceid, category)
    }
}

/// Get progress of ongoing tasks
/// Retrieve the progress of the most recent reindex task `/progress/category/insight-reindex/reindex`
/// Retrieve the progress of an import task `/progress/category/imports/{id}`.
pub struct GetProgressForCategoryAndResourceIdRequest<'a> {
    client: &'a crate::core::Client,
    resourceid: String,
    category: String,
}

impl<'a> GetProgressForCategoryAndResourceIdRequest<'a> {
    fn new(client: &'a crate::core::Client, resourceid: impl Into<String>, category: impl Into<String>) -> Self {
        Self {
            client,
            resourceid: resourceid.into(),
            category: category.into(),
        }
    }

    /// The request as the transport will send it.
    pub fn config(&self) -> crate::core::Result<crate::core::RequestConfig> {
        let config = crate::core::RequestConfig::new(
            crate::core::Method::GET,
            format!(
                "/rest/assets/1.0/progress/category/{}/{}",
                self.category, self.resourceid
            ),
        );

        Ok(config)
    }

    /// Sends the request.
    pub async fn send(self) -> crate::core::Result<ProgressOut> {
        self.client.send(&self.config()?).await
    }

    /// Sends the request and hands back the body unmodelled.
    pub async fn send_raw(self) -> crate::core::Result<serde_json::Value> {
        self.client.send_raw(&self.config()?).await
    }
}
