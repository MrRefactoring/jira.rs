// @generated by apis-code-gen. Do not edit: change the generator or the specification.

use super::super::models::*;

/// The Aql operations.
pub struct AqlService<'a> {
    client: &'a crate::core::Client,
}

impl<'a> AqlService<'a> {
    pub(crate) fn new(client: &'a crate::core::Client) -> Self {
        Self { client }
    }

    /// Find objects using AQL.
    pub fn find_objects(&self) -> FindObjectsRequest<'a> {
        FindObjectsRequest::new(self.client)
    }
}

/// Find objects using AQL.
pub struct FindObjectsRequest<'a> {
    client: &'a crate::core::Client,
    include_attributes_deep: Option<String>,
    xoauth_requestor_id: Option<String>,
    include_type_attributes: Option<String>,
    result_per_page: Option<String>,
    include_attributes: Option<String>,
    ql_query: Option<String>,
    page: Option<String>,
    include_extended_info: Option<String>,
}

impl<'a> FindObjectsRequest<'a> {
    fn new(client: &'a crate::core::Client) -> Self {
        Self {
            client,
            include_attributes_deep: None,
            xoauth_requestor_id: None,
            include_type_attributes: None,
            result_per_page: None,
            include_attributes: None,
            ql_query: None,
            page: None,
            include_extended_info: None,
        }
    }

    /// How many levels of attributes should be included in the response.
    ///
    /// Example: Consider object A, which has a reference to object B, which has a reference to object C. If object A is included in the response and `includeAttributesDeep=1`, object A's reference to object B will be included in the attributes of object A but object B's reference to object C will not be included. However if `includeAttributesDeep=2`, then object B's reference to object C will be included in object B's attributes.
    #[must_use]
    pub fn include_attributes_deep(mut self, value: impl Into<String>) -> Self {
        self.include_attributes_deep = Some(value.into());

        self
    }

    #[must_use]
    pub fn xoauth_requestor_id(mut self, value: impl Into<String>) -> Self {
        self.xoauth_requestor_id = Some(value.into());

        self
    }

    /// Should the response include the object type attribute definition for each attribute that is returned with the objects.
    #[must_use]
    pub fn include_type_attributes(mut self, value: impl Into<String>) -> Self {
        self.include_type_attributes = Some(value.into());

        self
    }

    /// The number of objects returned per page.
    #[must_use]
    pub fn result_per_page(mut self, value: impl Into<String>) -> Self {
        self.result_per_page = Some(value.into());

        self
    }

    /// Should object attributes be included in the response. If this parameter is set to `false`, only the information on the object will be returned and object attributes will not be present.
    #[must_use]
    pub fn include_attributes(mut self, value: impl Into<String>) -> Self {
        self.include_attributes = Some(value.into());

        self
    }

    /// The query to determine which objects should be fetched. For example, `objectType = "Computer"`. An empty query means all objects are fetched.
    #[must_use]
    pub fn ql_query(mut self, value: impl Into<String>) -> Self {
        self.ql_query = Some(value.into());

        self
    }

    /// The page to fetch when paginating through the response.
    #[must_use]
    pub fn page(mut self, value: impl Into<String>) -> Self {
        self.page = Some(value.into());

        self
    }

    /// Should the response include information about open issues and attachments. If the value is set to `true`, each returned object contains information on whether there are open tickets connected to it and if the object has attachments.
    #[must_use]
    pub fn include_extended_info(mut self, value: impl Into<String>) -> Self {
        self.include_extended_info = Some(value.into());

        self
    }

    /// The request as the transport will send it.
    pub fn config(&self) -> crate::core::Result<crate::core::RequestConfig> {
        let mut config =
            crate::core::RequestConfig::new(crate::core::Method::GET, "/rest/assets/1.0/aql/objects".to_owned());

        if let Some(value) = &self.include_attributes_deep {
            config.query.push(("includeAttributesDeep".to_owned(), crate::core::QueryValue::Scalar(value.clone())));
        }

        if let Some(value) = &self.xoauth_requestor_id {
            config.query.push(("xoauth_requestor_id".to_owned(), crate::core::QueryValue::Scalar(value.clone())));
        }

        if let Some(value) = &self.include_type_attributes {
            config.query.push(("includeTypeAttributes".to_owned(), crate::core::QueryValue::Scalar(value.clone())));
        }

        if let Some(value) = &self.result_per_page {
            config.query.push(("resultPerPage".to_owned(), crate::core::QueryValue::Scalar(value.clone())));
        }

        if let Some(value) = &self.include_attributes {
            config.query.push(("includeAttributes".to_owned(), crate::core::QueryValue::Scalar(value.clone())));
        }

        if let Some(value) = &self.ql_query {
            config.query.push(("qlQuery".to_owned(), crate::core::QueryValue::Scalar(value.clone())));
        }

        if let Some(value) = &self.page {
            config.query.push(("page".to_owned(), crate::core::QueryValue::Scalar(value.clone())));
        }

        if let Some(value) = &self.include_extended_info {
            config.query.push(("includeExtendedInfo".to_owned(), crate::core::QueryValue::Scalar(value.clone())));
        }

        Ok(config)
    }

    /// Sends the request.
    pub async fn send(self) -> crate::core::Result<ObjectListResult> {
        self.client.send(&self.config()?).await
    }

    /// Sends the request and hands back the body unmodelled.
    pub async fn send_raw(self) -> crate::core::Result<serde_json::Value> {
        self.client.send_raw(&self.config()?).await
    }
}
