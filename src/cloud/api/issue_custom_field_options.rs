// @generated by apis-code-gen. Do not edit: change the generator or the specification.

use super::super::models::*;

/// The IssueCustomFieldOptions operations.
pub struct IssueCustomFieldOptionsService<'a> {
    client: &'a crate::core::Client,
}

impl<'a> IssueCustomFieldOptionsService<'a> {
    pub(crate) fn new(client: &'a crate::core::Client) -> Self {
        Self { client }
    }

    /// Returns a custom field option. For example, an option in a select list.
    ///
    /// Note that this operation **only works for issue field select list options created in Jira or using operations from the [Issue custom field options](#api-group-Issue-custom-field-options) resource**, it cannot be used with issue field select list options created by Connect apps.
    ///
    /// This operation can be accessed anonymously.
    ///
    /// **[Permissions](#permissions) required:** The custom field option is returned as follows:
    ///
    ///  *  if the user has the *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
    ///  *  if the user has the *Browse projects* [project permission](https://confluence.atlassian.com/x/yodKLg) for at least one project the custom field is used in, and the field is visible in at least one layout the user has permission to view.
    pub fn get_custom_field_option(&self, id: impl Into<String>) -> GetCustomFieldOptionRequest<'a> {
        GetCustomFieldOptionRequest::new(self.client, id)
    }

    /// Returns a [paginated](#pagination) list of all custom field option for a context. Options are returned first then cascading options, in the order they display in Jira.
    ///
    /// This operation works for custom field options created in Jira or the operations from this resource. **To work with issue field select list options created for Connect apps use the [Issue custom field options (apps)](#api-group-issue-custom-field-options--apps-) operations.**
    ///
    /// **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg). *Edit Workflow* [edit workflow permission](https://support.atlassian.com/jira-cloud-administration/docs/permissions-for-company-managed-projects/#Edit-Workflows)
    pub fn get_options_for_context(
        &self,
        field_id: impl Into<String>,
        context_id: i64,
    ) -> GetOptionsForContextRequest<'a> {
        GetOptionsForContextRequest::new(self.client, field_id, context_id)
    }

    /// Creates options and, where the custom select field is of the type Select List (cascading), cascading options for a custom select field. The options are added to a context of the field.
    ///
    /// The maximum number of options that can be created per request is 1000 and each field can have a maximum of 10000 options.
    ///
    /// This operation works for custom field options created in Jira or the operations from this resource. **To work with issue field select list options created for Connect apps use the [Issue custom field options (apps)](#api-group-issue-custom-field-options--apps-) operations.**
    ///
    /// **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
    pub fn create_custom_field_option(
        &self,
        field_id: impl Into<String>,
        context_id: i64,
        bulk_custom_field_option_create_request: BulkCustomFieldOptionCreateRequest,
    ) -> CreateCustomFieldOptionRequest<'a> {
        CreateCustomFieldOptionRequest::new(
            self.client,
            field_id,
            context_id,
            bulk_custom_field_option_create_request,
        )
    }

    /// Updates the options of a custom field.
    ///
    /// If any of the options are not found, no options are updated. Options where the values in the request match the current values aren't updated and aren't reported in the response.
    ///
    /// Note that this operation **only works for issue field select list options created in Jira or using operations from the [Issue custom field options](#api-group-Issue-custom-field-options) resource**, it cannot be used with issue field select list options created by Connect apps.
    ///
    /// **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
    pub fn update_custom_field_option(
        &self,
        field_id: impl Into<String>,
        context_id: i64,
        bulk_custom_field_option_update_request: BulkCustomFieldOptionUpdateRequest,
    ) -> UpdateCustomFieldOptionRequest<'a> {
        UpdateCustomFieldOptionRequest::new(
            self.client,
            field_id,
            context_id,
            bulk_custom_field_option_update_request,
        )
    }

    /// Changes the order of custom field options or cascading options in a context.
    ///
    /// This operation works for custom field options created in Jira or the operations from this resource. **To work with issue field select list options created for Connect apps use the [Issue custom field options (apps)](#api-group-issue-custom-field-options--apps-) operations.**
    ///
    /// **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
    pub fn reorder_custom_field_options(
        &self,
        field_id: impl Into<String>,
        context_id: i64,
        order_of_custom_field_options: OrderOfCustomFieldOptions,
    ) -> ReorderCustomFieldOptionsRequest<'a> {
        ReorderCustomFieldOptionsRequest::new(self.client, field_id, context_id, order_of_custom_field_options)
    }

    /// Deletes a custom field option.
    ///
    /// Options with cascading options cannot be deleted without deleting the cascading options first.
    ///
    /// This operation works for custom field options created in Jira or the operations from this resource. **To work with issue field select list options created for Connect apps use the [Issue custom field options (apps)](#api-group-issue-custom-field-options--apps-) operations.**
    ///
    /// **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
    pub fn delete_custom_field_option(
        &self,
        field_id: impl Into<String>,
        context_id: i64,
        option_id: i64,
    ) -> DeleteCustomFieldOptionRequest<'a> {
        DeleteCustomFieldOptionRequest::new(self.client, field_id, context_id, option_id)
    }

    /// Replaces the options of a custom field.
    ///
    /// Note that this operation **only works for issue field select list options created in Jira or using operations from the [Issue custom field options](#api-group-Issue-custom-field-options) resource**, it cannot be used with issue field select list options created by Connect or Forge apps.
    ///
    /// **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
    pub fn replace_custom_field_option(
        &self,
        field_id: impl Into<String>,
        option_id: i64,
        context_id: i64,
    ) -> ReplaceCustomFieldOptionRequest<'a> {
        ReplaceCustomFieldOptionRequest::new(self.client, field_id, option_id, context_id)
    }
}

/// Returns a custom field option. For example, an option in a select list.
///
/// Note that this operation **only works for issue field select list options created in Jira or using operations from the [Issue custom field options](#api-group-Issue-custom-field-options) resource**, it cannot be used with issue field select list options created by Connect apps.
///
/// This operation can be accessed anonymously.
///
/// **[Permissions](#permissions) required:** The custom field option is returned as follows:
///
///  *  if the user has the *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
///  *  if the user has the *Browse projects* [project permission](https://confluence.atlassian.com/x/yodKLg) for at least one project the custom field is used in, and the field is visible in at least one layout the user has permission to view.
pub struct GetCustomFieldOptionRequest<'a> {
    client: &'a crate::core::Client,
    id: String,
}

impl<'a> GetCustomFieldOptionRequest<'a> {
    fn new(client: &'a crate::core::Client, id: impl Into<String>) -> Self {
        Self { client, id: id.into() }
    }

    /// The request as the transport will send it.
    pub fn config(&self) -> crate::core::Result<crate::core::RequestConfig> {
        let config = crate::core::RequestConfig::new(
            crate::core::Method::GET,
            format!("/rest/api/3/customFieldOption/{}", self.id),
        );

        Ok(config)
    }

    /// Sends the request.
    pub async fn send(self) -> crate::core::Result<CustomFieldOption> {
        self.client.send(&self.config()?).await
    }

    /// Sends the request and hands back the body unmodelled.
    pub async fn send_raw(self) -> crate::core::Result<serde_json::Value> {
        self.client.send_raw(&self.config()?).await
    }
}

/// Returns a [paginated](#pagination) list of all custom field option for a context. Options are returned first then cascading options, in the order they display in Jira.
///
/// This operation works for custom field options created in Jira or the operations from this resource. **To work with issue field select list options created for Connect apps use the [Issue custom field options (apps)](#api-group-issue-custom-field-options--apps-) operations.**
///
/// **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg). *Edit Workflow* [edit workflow permission](https://support.atlassian.com/jira-cloud-administration/docs/permissions-for-company-managed-projects/#Edit-Workflows)
pub struct GetOptionsForContextRequest<'a> {
    client: &'a crate::core::Client,
    field_id: String,
    context_id: i64,
    option_id: Option<i64>,
    only_options: Option<bool>,
    start_at: Option<i64>,
    max_results: Option<i64>,
}

impl<'a> GetOptionsForContextRequest<'a> {
    fn new(client: &'a crate::core::Client, field_id: impl Into<String>, context_id: i64) -> Self {
        Self {
            client,
            field_id: field_id.into(),
            context_id,
            option_id: None,
            only_options: None,
            start_at: None,
            max_results: None,
        }
    }

    /// The ID of the option.
    #[must_use]
    pub fn option_id(mut self, value: i64) -> Self {
        self.option_id = Some(value);

        self
    }

    /// Whether only options are returned.
    #[must_use]
    pub fn only_options(mut self, value: bool) -> Self {
        self.only_options = Some(value);

        self
    }

    /// The index of the first item to return in a page of results (page offset).
    #[must_use]
    pub fn start_at(mut self, value: i64) -> Self {
        self.start_at = Some(value);

        self
    }

    /// The maximum number of items to return per page.
    #[must_use]
    pub fn max_results(mut self, value: i64) -> Self {
        self.max_results = Some(value);

        self
    }

    /// The request as the transport will send it.
    pub fn config(&self) -> crate::core::Result<crate::core::RequestConfig> {
        let mut config = crate::core::RequestConfig::new(
            crate::core::Method::GET,
            format!("/rest/api/3/field/{}/context/{}/option", self.field_id, self.context_id),
        );

        if let Some(value) = &self.option_id {
            config.query.push((
                "optionId".to_owned(),
                crate::core::QueryValue::Scalar(value.to_string()),
            ));
        }

        if let Some(value) = &self.only_options {
            config.query.push((
                "onlyOptions".to_owned(),
                crate::core::QueryValue::Scalar(value.to_string()),
            ));
        }

        if let Some(value) = &self.start_at {
            config
                .query
                .push(("startAt".to_owned(), crate::core::QueryValue::Scalar(value.to_string())));
        }

        if let Some(value) = &self.max_results {
            config.query.push((
                "maxResults".to_owned(),
                crate::core::QueryValue::Scalar(value.to_string()),
            ));
        }

        Ok(config)
    }

    /// Sends the request.
    pub async fn send(self) -> crate::core::Result<Page<CustomFieldContextOption>> {
        self.client.send(&self.config()?).await
    }

    /// Sends the request and hands back the body unmodelled.
    pub async fn send_raw(self) -> crate::core::Result<serde_json::Value> {
        self.client.send_raw(&self.config()?).await
    }
}

/// Creates options and, where the custom select field is of the type Select List (cascading), cascading options for a custom select field. The options are added to a context of the field.
///
/// The maximum number of options that can be created per request is 1000 and each field can have a maximum of 10000 options.
///
/// This operation works for custom field options created in Jira or the operations from this resource. **To work with issue field select list options created for Connect apps use the [Issue custom field options (apps)](#api-group-issue-custom-field-options--apps-) operations.**
///
/// **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
pub struct CreateCustomFieldOptionRequest<'a> {
    client: &'a crate::core::Client,
    field_id: String,
    context_id: i64,
    bulk_custom_field_option_create_request: BulkCustomFieldOptionCreateRequest,
}

impl<'a> CreateCustomFieldOptionRequest<'a> {
    fn new(
        client: &'a crate::core::Client,
        field_id: impl Into<String>,
        context_id: i64,
        bulk_custom_field_option_create_request: BulkCustomFieldOptionCreateRequest,
    ) -> Self {
        Self {
            client,
            field_id: field_id.into(),
            context_id,
            bulk_custom_field_option_create_request,
        }
    }

    /// The request as the transport will send it.
    pub fn config(&self) -> crate::core::Result<crate::core::RequestConfig> {
        let mut config = crate::core::RequestConfig::new(
            crate::core::Method::POST,
            format!("/rest/api/3/field/{}/context/{}/option", self.field_id, self.context_id),
        );

        let body = match serde_json::to_value(&self.bulk_custom_field_option_create_request)? {
            serde_json::Value::Object(object) => object,
            _ => serde_json::Map::new(),
        };

        config.body = Some(crate::core::Body::Json(serde_json::Value::Object(body)));

        Ok(config)
    }

    /// Sends the request.
    pub async fn send(self) -> crate::core::Result<CustomFieldCreatedContextOptionsList> {
        self.client.send(&self.config()?).await
    }

    /// Sends the request and hands back the body unmodelled.
    pub async fn send_raw(self) -> crate::core::Result<serde_json::Value> {
        self.client.send_raw(&self.config()?).await
    }
}

/// Updates the options of a custom field.
///
/// If any of the options are not found, no options are updated. Options where the values in the request match the current values aren't updated and aren't reported in the response.
///
/// Note that this operation **only works for issue field select list options created in Jira or using operations from the [Issue custom field options](#api-group-Issue-custom-field-options) resource**, it cannot be used with issue field select list options created by Connect apps.
///
/// **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
pub struct UpdateCustomFieldOptionRequest<'a> {
    client: &'a crate::core::Client,
    field_id: String,
    context_id: i64,
    bulk_custom_field_option_update_request: BulkCustomFieldOptionUpdateRequest,
}

impl<'a> UpdateCustomFieldOptionRequest<'a> {
    fn new(
        client: &'a crate::core::Client,
        field_id: impl Into<String>,
        context_id: i64,
        bulk_custom_field_option_update_request: BulkCustomFieldOptionUpdateRequest,
    ) -> Self {
        Self {
            client,
            field_id: field_id.into(),
            context_id,
            bulk_custom_field_option_update_request,
        }
    }

    /// The request as the transport will send it.
    pub fn config(&self) -> crate::core::Result<crate::core::RequestConfig> {
        let mut config = crate::core::RequestConfig::new(
            crate::core::Method::PUT,
            format!("/rest/api/3/field/{}/context/{}/option", self.field_id, self.context_id),
        );

        let body = match serde_json::to_value(&self.bulk_custom_field_option_update_request)? {
            serde_json::Value::Object(object) => object,
            _ => serde_json::Map::new(),
        };

        config.body = Some(crate::core::Body::Json(serde_json::Value::Object(body)));

        Ok(config)
    }

    /// Sends the request.
    pub async fn send(self) -> crate::core::Result<CustomFieldUpdatedContextOptionsList> {
        self.client.send(&self.config()?).await
    }

    /// Sends the request and hands back the body unmodelled.
    pub async fn send_raw(self) -> crate::core::Result<serde_json::Value> {
        self.client.send_raw(&self.config()?).await
    }
}

/// Changes the order of custom field options or cascading options in a context.
///
/// This operation works for custom field options created in Jira or the operations from this resource. **To work with issue field select list options created for Connect apps use the [Issue custom field options (apps)](#api-group-issue-custom-field-options--apps-) operations.**
///
/// **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
pub struct ReorderCustomFieldOptionsRequest<'a> {
    client: &'a crate::core::Client,
    field_id: String,
    context_id: i64,
    order_of_custom_field_options: OrderOfCustomFieldOptions,
}

impl<'a> ReorderCustomFieldOptionsRequest<'a> {
    fn new(
        client: &'a crate::core::Client,
        field_id: impl Into<String>,
        context_id: i64,
        order_of_custom_field_options: OrderOfCustomFieldOptions,
    ) -> Self {
        Self {
            client,
            field_id: field_id.into(),
            context_id,
            order_of_custom_field_options,
        }
    }

    /// The request as the transport will send it.
    pub fn config(&self) -> crate::core::Result<crate::core::RequestConfig> {
        let mut config = crate::core::RequestConfig::new(
            crate::core::Method::PUT,
            format!(
                "/rest/api/3/field/{}/context/{}/option/move",
                self.field_id, self.context_id
            ),
        );

        let body = match serde_json::to_value(&self.order_of_custom_field_options)? {
            serde_json::Value::Object(object) => object,
            _ => serde_json::Map::new(),
        };

        config.body = Some(crate::core::Body::Json(serde_json::Value::Object(body)));

        Ok(config)
    }

    /// Sends the request.
    pub async fn send(self) -> crate::core::Result<()> {
        self.client.send_empty(&self.config()?).await
    }

    /// Sends the request and hands back the body unmodelled.
    pub async fn send_raw(self) -> crate::core::Result<serde_json::Value> {
        self.client.send_raw(&self.config()?).await
    }
}

/// Deletes a custom field option.
///
/// Options with cascading options cannot be deleted without deleting the cascading options first.
///
/// This operation works for custom field options created in Jira or the operations from this resource. **To work with issue field select list options created for Connect apps use the [Issue custom field options (apps)](#api-group-issue-custom-field-options--apps-) operations.**
///
/// **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
pub struct DeleteCustomFieldOptionRequest<'a> {
    client: &'a crate::core::Client,
    field_id: String,
    context_id: i64,
    option_id: i64,
}

impl<'a> DeleteCustomFieldOptionRequest<'a> {
    fn new(client: &'a crate::core::Client, field_id: impl Into<String>, context_id: i64, option_id: i64) -> Self {
        Self {
            client,
            field_id: field_id.into(),
            context_id,
            option_id,
        }
    }

    /// The request as the transport will send it.
    pub fn config(&self) -> crate::core::Result<crate::core::RequestConfig> {
        let config = crate::core::RequestConfig::new(
            crate::core::Method::DELETE,
            format!(
                "/rest/api/3/field/{}/context/{}/option/{}",
                self.field_id, self.context_id, self.option_id
            ),
        );

        Ok(config)
    }

    /// Sends the request.
    pub async fn send(self) -> crate::core::Result<()> {
        self.client.send_empty(&self.config()?).await
    }

    /// Sends the request and hands back the body unmodelled.
    pub async fn send_raw(self) -> crate::core::Result<serde_json::Value> {
        self.client.send_raw(&self.config()?).await
    }
}

/// Replaces the options of a custom field.
///
/// Note that this operation **only works for issue field select list options created in Jira or using operations from the [Issue custom field options](#api-group-Issue-custom-field-options) resource**, it cannot be used with issue field select list options created by Connect or Forge apps.
///
/// **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
pub struct ReplaceCustomFieldOptionRequest<'a> {
    client: &'a crate::core::Client,
    replace_with: Option<i64>,
    jql: Option<String>,
    field_id: String,
    option_id: i64,
    context_id: i64,
}

impl<'a> ReplaceCustomFieldOptionRequest<'a> {
    fn new(client: &'a crate::core::Client, field_id: impl Into<String>, option_id: i64, context_id: i64) -> Self {
        Self {
            client,
            field_id: field_id.into(),
            option_id,
            context_id,
            replace_with: None,
            jql: None,
        }
    }

    /// The ID of the option that will replace the currently selected option.
    #[must_use]
    pub fn replace_with(mut self, value: i64) -> Self {
        self.replace_with = Some(value);

        self
    }

    /// A JQL query that specifies the issues to be updated. For example, *project=10000*.
    #[must_use]
    pub fn jql(mut self, value: impl Into<String>) -> Self {
        self.jql = Some(value.into());

        self
    }

    /// The request as the transport will send it.
    pub fn config(&self) -> crate::core::Result<crate::core::RequestConfig> {
        let mut config = crate::core::RequestConfig::new(
            crate::core::Method::DELETE,
            format!(
                "/rest/api/3/field/{}/context/{}/option/{}/issue",
                self.field_id, self.context_id, self.option_id
            ),
        );

        if let Some(value) = &self.replace_with {
            config.query.push((
                "replaceWith".to_owned(),
                crate::core::QueryValue::Scalar(value.to_string()),
            ));
        }

        if let Some(value) = &self.jql {
            config
                .query
                .push(("jql".to_owned(), crate::core::QueryValue::Scalar(value.clone())));
        }

        Ok(config)
    }

    /// Sends the request.
    pub async fn send(self) -> crate::core::Result<TaskProgressRemoveOptionFromIssuesResult> {
        self.client.send(&self.config()?).await
    }

    /// Sends the request and hands back the body unmodelled.
    pub async fn send_raw(self) -> crate::core::Result<serde_json::Value> {
        self.client.send_raw(&self.config()?).await
    }
}
