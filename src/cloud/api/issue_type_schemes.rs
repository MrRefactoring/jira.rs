// @generated by apis-code-gen. Do not edit: change the generator or the specification.

use super::super::models::*;
use serde::{Deserialize, Serialize};

crate::open_enum! {
    /// [Order](#ordering) the results by a field:
    ///
    ///  *  `name` Sorts by issue type scheme name.
    ///  *  `id` Sorts by issue type scheme ID.
    pub enum GetAllIssueTypeSchemesRequestOrderBy {
        Name => "name",
        NameDescending => "-name",
        NameAscending => "+name",
        Id => "id",
        IdDescending => "-id",
        IdAscending => "+id",
    }
}

crate::open_enum! {
    pub enum GetAllIssueTypeSchemesRequestExpandVariant2 {
        Projects => "projects",
        IssueTypes => "issueTypes",
    }
}

crate::open_enum! {
    pub enum GetAllIssueTypeSchemesRequestExpandUnion3 {
        Projects => "projects",
        IssueTypes => "issueTypes",
    }
}

/// Use [expand](#expansion) to include additional information in the response. This parameter accepts a comma-separated list. Expand options include:
///
///  *  `projects` For each issue type schemes, returns information about the projects the issue type scheme is assigned to.
///  *  `issueTypes` For each issue type schemes, returns information about the issueTypes the issue type scheme have.
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
#[allow(clippy::large_enum_variant)]
pub enum GetAllIssueTypeSchemesRequestExpand {
    Variant0(String),
    Variant1(Vec<String>),
    Variant2(GetAllIssueTypeSchemesRequestExpandVariant2),
    Variant3(Vec<GetAllIssueTypeSchemesRequestExpandUnion3>),
    /// A shape the specification does not describe.
    Other(serde_json::Value),
}

/// The IssueTypeSchemes operations.
pub struct IssueTypeSchemesService<'a> {
    client: &'a crate::core::Client,
}

impl<'a> IssueTypeSchemesService<'a> {
    pub(crate) fn new(client: &'a crate::core::Client) -> Self {
        Self { client }
    }

    /// Returns a [paginated](#pagination) list of issue type schemes.
    ///
    /// Only issue type schemes used in classic projects are returned.
    ///
    /// **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
    pub fn get_all_issue_type_schemes(&self) -> GetAllIssueTypeSchemesRequest<'a> {
        GetAllIssueTypeSchemesRequest::new(self.client)
    }

    /// Creates an issue type scheme.
    ///
    /// **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
    pub fn create_issue_type_scheme(
        &self,
        issue_type_scheme_details: IssueTypeSchemeDetails,
    ) -> CreateIssueTypeSchemeRequest<'a> {
        CreateIssueTypeSchemeRequest::new(self.client, issue_type_scheme_details)
    }

    /// Returns a [paginated](#pagination) list of issue type scheme items.
    ///
    /// Only issue type scheme items used in classic projects are returned.
    ///
    /// **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
    pub fn get_issue_type_schemes_mapping(&self) -> GetIssueTypeSchemesMappingRequest<'a> {
        GetIssueTypeSchemesMappingRequest::new(self.client)
    }

    /// Returns a [paginated](#pagination) list of issue type schemes and, for each issue type scheme, a list of the projects that use it.
    ///
    /// Only issue type schemes used in classic projects are returned.
    ///
    /// **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
    pub fn get_issue_type_scheme_for_projects(
        &self,
        project_id: impl IntoIterator<Item = i64>,
    ) -> GetIssueTypeSchemeForProjectsRequest<'a> {
        GetIssueTypeSchemeForProjectsRequest::new(self.client, project_id)
    }

    /// Assigns an issue type scheme to a project.
    ///
    /// If any issues in the project are assigned issue types not present in the new scheme, the operation will fail. To complete the assignment those issues must be updated to use issue types in the new scheme.
    ///
    /// Issue type schemes can only be assigned to classic projects.
    ///
    /// **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
    pub fn assign_issue_type_scheme_to_project(
        &self,
        issue_type_scheme_project_association: IssueTypeSchemeProjectAssociation,
    ) -> AssignIssueTypeSchemeToProjectRequest<'a> {
        AssignIssueTypeSchemeToProjectRequest::new(self.client, issue_type_scheme_project_association)
    }

    /// Updates an issue type scheme.
    ///
    /// **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
    pub fn update_issue_type_scheme(
        &self,
        issue_type_scheme_id: i64,
        issue_type_scheme_update_details: IssueTypeSchemeUpdateDetails,
    ) -> UpdateIssueTypeSchemeRequest<'a> {
        UpdateIssueTypeSchemeRequest::new(self.client, issue_type_scheme_id, issue_type_scheme_update_details)
    }

    /// Deletes an issue type scheme.
    ///
    /// Only issue type schemes used in classic projects can be deleted. Only issue type schemes not associated with a project can be deleted
    ///
    /// A validation error will be returned if the specified scheme is associated with one or more projects. Use [Get issue type scheme API](https://developer.atlassian.com/cloud/jira/platform/rest/v3/api-group-issue-type-schemes/#api-rest-api-3-issuetypescheme-get) (with the projects expand, and id query parameter) to get a list of projects. Then, use [Assign issue type scheme to project API](https://developer.atlassian.com/cloud/jira/platform/rest/v3/api-group-issue-type-schemes/#api-rest-api-3-issuetypescheme-project-put) to associate all projects to another scheme before deleting.
    ///
    /// **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
    pub fn delete_issue_type_scheme(&self, issue_type_scheme_id: i64) -> DeleteIssueTypeSchemeRequest<'a> {
        DeleteIssueTypeSchemeRequest::new(self.client, issue_type_scheme_id)
    }

    /// Adds issue types to an issue type scheme.
    ///
    /// The added issue types are appended to the issue types list.
    ///
    /// If any of the issue types exist in the issue type scheme, the operation fails and no issue types are added.
    ///
    /// **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
    pub fn add_issue_types_to_issue_type_scheme(
        &self,
        issue_type_scheme_id: i64,
        issue_type_ids: IssueTypeIds,
    ) -> AddIssueTypesToIssueTypeSchemeRequest<'a> {
        AddIssueTypesToIssueTypeSchemeRequest::new(self.client, issue_type_scheme_id, issue_type_ids)
    }

    /// Changes the order of issue types in an issue type scheme.
    ///
    /// The request body parameters must meet the following requirements:
    ///
    ///  *  all of the issue types must belong to the issue type scheme.
    ///  *  either `after` or `position` must be provided.
    ///  *  the issue type in `after` must not be in the issue type list.
    ///
    /// **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
    pub fn reorder_issue_types_in_issue_type_scheme(
        &self,
        issue_type_scheme_id: i64,
        order_of_issue_types: OrderOfIssueTypes,
    ) -> ReorderIssueTypesInIssueTypeSchemeRequest<'a> {
        ReorderIssueTypesInIssueTypeSchemeRequest::new(self.client, issue_type_scheme_id, order_of_issue_types)
    }

    /// Removes an issue type from an issue type scheme.
    ///
    /// This operation cannot remove:
    ///
    ///  *  any issue type used by issues.
    ///  *  any issue types from the default issue type scheme.
    ///  *  the last standard issue type from an issue type scheme.
    ///
    /// **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
    pub fn remove_issue_type_from_issue_type_scheme(
        &self,
        issue_type_scheme_id: i64,
        issue_type_id: i64,
    ) -> RemoveIssueTypeFromIssueTypeSchemeRequest<'a> {
        RemoveIssueTypeFromIssueTypeSchemeRequest::new(self.client, issue_type_scheme_id, issue_type_id)
    }
}

/// Returns a [paginated](#pagination) list of issue type schemes.
///
/// Only issue type schemes used in classic projects are returned.
///
/// **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
pub struct GetAllIssueTypeSchemesRequest<'a> {
    client: &'a crate::core::Client,
    start_at: Option<i64>,
    max_results: Option<i64>,
    id: Option<Vec<i64>>,
    order_by: Option<GetAllIssueTypeSchemesRequestOrderBy>,
    expand: Option<GetAllIssueTypeSchemesRequestExpand>,
    query_string: Option<String>,
}

impl<'a> GetAllIssueTypeSchemesRequest<'a> {
    fn new(client: &'a crate::core::Client) -> Self {
        Self { client, start_at: None, max_results: None, id: None, order_by: None, expand: None, query_string: None }
    }

    /// The index of the first item to return in a page of results (page offset).
    #[must_use]
    pub fn start_at(mut self, value: i64) -> Self {
        self.start_at = Some(value);

        self
    }

    /// The maximum number of items to return per page.
    #[must_use]
    pub fn max_results(mut self, value: i64) -> Self {
        self.max_results = Some(value);

        self
    }

    /// The list of issue type schemes IDs. To include multiple IDs, provide an ampersand-separated list. For example, `id=10000&id=10001`.
    #[must_use]
    pub fn id(mut self, value: impl IntoIterator<Item = i64>) -> Self {
        self.id = Some(value.into_iter().collect());

        self
    }

    /// [Order](#ordering) the results by a field:
    ///
    ///  *  `name` Sorts by issue type scheme name.
    ///  *  `id` Sorts by issue type scheme ID.
    #[must_use]
    pub fn order_by(mut self, value: impl Into<GetAllIssueTypeSchemesRequestOrderBy>) -> Self {
        self.order_by = Some(value.into());

        self
    }

    /// Use [expand](#expansion) to include additional information in the response. This parameter accepts a comma-separated list. Expand options include:
    ///
    ///  *  `projects` For each issue type schemes, returns information about the projects the issue type scheme is assigned to.
    ///  *  `issueTypes` For each issue type schemes, returns information about the issueTypes the issue type scheme have.
    #[must_use]
    pub fn expand(mut self, value: GetAllIssueTypeSchemesRequestExpand) -> Self {
        self.expand = Some(value);

        self
    }

    /// String used to perform a case-insensitive partial match with issue type scheme name.
    #[must_use]
    pub fn query_string(mut self, value: impl Into<String>) -> Self {
        self.query_string = Some(value.into());

        self
    }

    /// The request as the transport will send it.
    pub fn config(&self) -> crate::core::Result<crate::core::RequestConfig> {
        let mut config =
            crate::core::RequestConfig::new(crate::core::Method::GET, "/rest/api/3/issuetypescheme".to_owned());

        if let Some(value) = &self.start_at {
            config.query.push(("startAt".to_owned(), crate::core::QueryValue::Scalar(value.to_string())));
        }

        if let Some(value) = &self.max_results {
            config.query.push(("maxResults".to_owned(), crate::core::QueryValue::Scalar(value.to_string())));
        }

        if let Some(value) = &self.id {
            config.query.push(("id".to_owned(), crate::core::QueryValue::from_serializable(value)?));
        }

        if let Some(value) = &self.order_by {
            config.query.push(("orderBy".to_owned(), crate::core::QueryValue::from_serializable(value)?));
        }

        if let Some(value) = &self.expand {
            config.query.push(("expand".to_owned(), crate::core::QueryValue::from_serializable(value)?));
        }

        if let Some(value) = &self.query_string {
            config.query.push(("queryString".to_owned(), crate::core::QueryValue::Scalar(value.clone())));
        }

        Ok(config)
    }

    /// Sends the request.
    pub async fn send(self) -> crate::core::Result<Page<IssueTypeScheme>> {
        self.client.send(&self.config()?).await
    }

    /// Sends the request and hands back the body unmodelled.
    pub async fn send_raw(self) -> crate::core::Result<serde_json::Value> {
        self.client.send_raw(&self.config()?).await
    }
}

/// Creates an issue type scheme.
///
/// **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
pub struct CreateIssueTypeSchemeRequest<'a> {
    client: &'a crate::core::Client,
    issue_type_scheme_details: IssueTypeSchemeDetails,
}

impl<'a> CreateIssueTypeSchemeRequest<'a> {
    fn new(client: &'a crate::core::Client, issue_type_scheme_details: IssueTypeSchemeDetails) -> Self {
        Self { client, issue_type_scheme_details }
    }

    /// The request as the transport will send it.
    pub fn config(&self) -> crate::core::Result<crate::core::RequestConfig> {
        let mut config =
            crate::core::RequestConfig::new(crate::core::Method::POST, "/rest/api/3/issuetypescheme".to_owned());

        let body = match serde_json::to_value(&self.issue_type_scheme_details)? {
            serde_json::Value::Object(object) => object,
            _ => serde_json::Map::new(),
        };

        config.body = Some(crate::core::Body::Json(serde_json::Value::Object(body)));

        Ok(config)
    }

    /// Sends the request.
    pub async fn send(self) -> crate::core::Result<IssueTypeSchemeID> {
        self.client.send(&self.config()?).await
    }

    /// Sends the request and hands back the body unmodelled.
    pub async fn send_raw(self) -> crate::core::Result<serde_json::Value> {
        self.client.send_raw(&self.config()?).await
    }
}

/// Returns a [paginated](#pagination) list of issue type scheme items.
///
/// Only issue type scheme items used in classic projects are returned.
///
/// **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
pub struct GetIssueTypeSchemesMappingRequest<'a> {
    client: &'a crate::core::Client,
    start_at: Option<i64>,
    max_results: Option<i64>,
    issue_type_scheme_id: Option<Vec<i64>>,
}

impl<'a> GetIssueTypeSchemesMappingRequest<'a> {
    fn new(client: &'a crate::core::Client) -> Self {
        Self { client, start_at: None, max_results: None, issue_type_scheme_id: None }
    }

    /// The index of the first item to return in a page of results (page offset).
    #[must_use]
    pub fn start_at(mut self, value: i64) -> Self {
        self.start_at = Some(value);

        self
    }

    /// The maximum number of items to return per page.
    #[must_use]
    pub fn max_results(mut self, value: i64) -> Self {
        self.max_results = Some(value);

        self
    }

    /// The list of issue type scheme IDs. To include multiple IDs, provide an ampersand-separated list. For example, `issueTypeSchemeId=10000&issueTypeSchemeId=10001`.
    #[must_use]
    pub fn issue_type_scheme_id(mut self, value: impl IntoIterator<Item = i64>) -> Self {
        self.issue_type_scheme_id = Some(value.into_iter().collect());

        self
    }

    /// The request as the transport will send it.
    pub fn config(&self) -> crate::core::Result<crate::core::RequestConfig> {
        let mut config =
            crate::core::RequestConfig::new(crate::core::Method::GET, "/rest/api/3/issuetypescheme/mapping".to_owned());

        if let Some(value) = &self.start_at {
            config.query.push(("startAt".to_owned(), crate::core::QueryValue::Scalar(value.to_string())));
        }

        if let Some(value) = &self.max_results {
            config.query.push(("maxResults".to_owned(), crate::core::QueryValue::Scalar(value.to_string())));
        }

        if let Some(value) = &self.issue_type_scheme_id {
            config.query.push(("issueTypeSchemeId".to_owned(), crate::core::QueryValue::from_serializable(value)?));
        }

        Ok(config)
    }

    /// Sends the request.
    pub async fn send(self) -> crate::core::Result<Page<IssueTypeSchemeMapping>> {
        self.client.send(&self.config()?).await
    }

    /// Sends the request and hands back the body unmodelled.
    pub async fn send_raw(self) -> crate::core::Result<serde_json::Value> {
        self.client.send_raw(&self.config()?).await
    }
}

/// Returns a [paginated](#pagination) list of issue type schemes and, for each issue type scheme, a list of the projects that use it.
///
/// Only issue type schemes used in classic projects are returned.
///
/// **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
pub struct GetIssueTypeSchemeForProjectsRequest<'a> {
    client: &'a crate::core::Client,
    start_at: Option<i64>,
    max_results: Option<i64>,
    project_id: Vec<i64>,
}

impl<'a> GetIssueTypeSchemeForProjectsRequest<'a> {
    fn new(client: &'a crate::core::Client, project_id: impl IntoIterator<Item = i64>) -> Self {
        Self { client, project_id: project_id.into_iter().collect(), start_at: None, max_results: None }
    }

    /// The index of the first item to return in a page of results (page offset).
    #[must_use]
    pub fn start_at(mut self, value: i64) -> Self {
        self.start_at = Some(value);

        self
    }

    /// The maximum number of items to return per page.
    #[must_use]
    pub fn max_results(mut self, value: i64) -> Self {
        self.max_results = Some(value);

        self
    }

    /// The request as the transport will send it.
    pub fn config(&self) -> crate::core::Result<crate::core::RequestConfig> {
        let mut config =
            crate::core::RequestConfig::new(crate::core::Method::GET, "/rest/api/3/issuetypescheme/project".to_owned());

        if let Some(value) = &self.start_at {
            config.query.push(("startAt".to_owned(), crate::core::QueryValue::Scalar(value.to_string())));
        }

        if let Some(value) = &self.max_results {
            config.query.push(("maxResults".to_owned(), crate::core::QueryValue::Scalar(value.to_string())));
        }

        config.query.push(("projectId".to_owned(), crate::core::QueryValue::from_serializable(&self.project_id)?));

        Ok(config)
    }

    /// Sends the request.
    pub async fn send(self) -> crate::core::Result<Page<IssueTypeSchemeProjects>> {
        self.client.send(&self.config()?).await
    }

    /// Sends the request and hands back the body unmodelled.
    pub async fn send_raw(self) -> crate::core::Result<serde_json::Value> {
        self.client.send_raw(&self.config()?).await
    }
}

/// Assigns an issue type scheme to a project.
///
/// If any issues in the project are assigned issue types not present in the new scheme, the operation will fail. To complete the assignment those issues must be updated to use issue types in the new scheme.
///
/// Issue type schemes can only be assigned to classic projects.
///
/// **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
pub struct AssignIssueTypeSchemeToProjectRequest<'a> {
    client: &'a crate::core::Client,
    issue_type_scheme_project_association: IssueTypeSchemeProjectAssociation,
}

impl<'a> AssignIssueTypeSchemeToProjectRequest<'a> {
    fn new(
        client: &'a crate::core::Client,
        issue_type_scheme_project_association: IssueTypeSchemeProjectAssociation,
    ) -> Self {
        Self { client, issue_type_scheme_project_association }
    }

    /// The request as the transport will send it.
    pub fn config(&self) -> crate::core::Result<crate::core::RequestConfig> {
        let mut config =
            crate::core::RequestConfig::new(crate::core::Method::PUT, "/rest/api/3/issuetypescheme/project".to_owned());

        let body = match serde_json::to_value(&self.issue_type_scheme_project_association)? {
            serde_json::Value::Object(object) => object,
            _ => serde_json::Map::new(),
        };

        config.body = Some(crate::core::Body::Json(serde_json::Value::Object(body)));

        Ok(config)
    }

    /// Sends the request.
    pub async fn send(self) -> crate::core::Result<()> {
        self.client.send_empty(&self.config()?).await
    }

    /// Sends the request and hands back the body unmodelled.
    pub async fn send_raw(self) -> crate::core::Result<serde_json::Value> {
        self.client.send_raw(&self.config()?).await
    }
}

/// Updates an issue type scheme.
///
/// **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
pub struct UpdateIssueTypeSchemeRequest<'a> {
    client: &'a crate::core::Client,
    issue_type_scheme_id: i64,
    issue_type_scheme_update_details: IssueTypeSchemeUpdateDetails,
}

impl<'a> UpdateIssueTypeSchemeRequest<'a> {
    fn new(
        client: &'a crate::core::Client,
        issue_type_scheme_id: i64,
        issue_type_scheme_update_details: IssueTypeSchemeUpdateDetails,
    ) -> Self {
        Self { client, issue_type_scheme_id, issue_type_scheme_update_details }
    }

    /// The request as the transport will send it.
    pub fn config(&self) -> crate::core::Result<crate::core::RequestConfig> {
        let mut config = crate::core::RequestConfig::new(
            crate::core::Method::PUT,
            format!("/rest/api/3/issuetypescheme/{}", self.issue_type_scheme_id),
        );

        let body = match serde_json::to_value(&self.issue_type_scheme_update_details)? {
            serde_json::Value::Object(object) => object,
            _ => serde_json::Map::new(),
        };

        config.body = Some(crate::core::Body::Json(serde_json::Value::Object(body)));

        Ok(config)
    }

    /// Sends the request.
    pub async fn send(self) -> crate::core::Result<()> {
        self.client.send_empty(&self.config()?).await
    }

    /// Sends the request and hands back the body unmodelled.
    pub async fn send_raw(self) -> crate::core::Result<serde_json::Value> {
        self.client.send_raw(&self.config()?).await
    }
}

/// Deletes an issue type scheme.
///
/// Only issue type schemes used in classic projects can be deleted. Only issue type schemes not associated with a project can be deleted
///
/// A validation error will be returned if the specified scheme is associated with one or more projects. Use [Get issue type scheme API](https://developer.atlassian.com/cloud/jira/platform/rest/v3/api-group-issue-type-schemes/#api-rest-api-3-issuetypescheme-get) (with the projects expand, and id query parameter) to get a list of projects. Then, use [Assign issue type scheme to project API](https://developer.atlassian.com/cloud/jira/platform/rest/v3/api-group-issue-type-schemes/#api-rest-api-3-issuetypescheme-project-put) to associate all projects to another scheme before deleting.
///
/// **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
pub struct DeleteIssueTypeSchemeRequest<'a> {
    client: &'a crate::core::Client,
    issue_type_scheme_id: i64,
}

impl<'a> DeleteIssueTypeSchemeRequest<'a> {
    fn new(client: &'a crate::core::Client, issue_type_scheme_id: i64) -> Self {
        Self { client, issue_type_scheme_id }
    }

    /// The request as the transport will send it.
    pub fn config(&self) -> crate::core::Result<crate::core::RequestConfig> {
        let config = crate::core::RequestConfig::new(
            crate::core::Method::DELETE,
            format!("/rest/api/3/issuetypescheme/{}", self.issue_type_scheme_id),
        );

        Ok(config)
    }

    /// Sends the request.
    pub async fn send(self) -> crate::core::Result<()> {
        self.client.send_empty(&self.config()?).await
    }

    /// Sends the request and hands back the body unmodelled.
    pub async fn send_raw(self) -> crate::core::Result<serde_json::Value> {
        self.client.send_raw(&self.config()?).await
    }
}

/// Adds issue types to an issue type scheme.
///
/// The added issue types are appended to the issue types list.
///
/// If any of the issue types exist in the issue type scheme, the operation fails and no issue types are added.
///
/// **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
pub struct AddIssueTypesToIssueTypeSchemeRequest<'a> {
    client: &'a crate::core::Client,
    issue_type_scheme_id: i64,
    issue_type_ids: IssueTypeIds,
}

impl<'a> AddIssueTypesToIssueTypeSchemeRequest<'a> {
    fn new(client: &'a crate::core::Client, issue_type_scheme_id: i64, issue_type_ids: IssueTypeIds) -> Self {
        Self { client, issue_type_scheme_id, issue_type_ids }
    }

    /// The request as the transport will send it.
    pub fn config(&self) -> crate::core::Result<crate::core::RequestConfig> {
        let mut config = crate::core::RequestConfig::new(
            crate::core::Method::PUT,
            format!("/rest/api/3/issuetypescheme/{}/issuetype", self.issue_type_scheme_id),
        );

        let body = match serde_json::to_value(&self.issue_type_ids)? {
            serde_json::Value::Object(object) => object,
            _ => serde_json::Map::new(),
        };

        config.body = Some(crate::core::Body::Json(serde_json::Value::Object(body)));

        Ok(config)
    }

    /// Sends the request.
    pub async fn send(self) -> crate::core::Result<()> {
        self.client.send_empty(&self.config()?).await
    }

    /// Sends the request and hands back the body unmodelled.
    pub async fn send_raw(self) -> crate::core::Result<serde_json::Value> {
        self.client.send_raw(&self.config()?).await
    }
}

/// Changes the order of issue types in an issue type scheme.
///
/// The request body parameters must meet the following requirements:
///
///  *  all of the issue types must belong to the issue type scheme.
///  *  either `after` or `position` must be provided.
///  *  the issue type in `after` must not be in the issue type list.
///
/// **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
pub struct ReorderIssueTypesInIssueTypeSchemeRequest<'a> {
    client: &'a crate::core::Client,
    issue_type_scheme_id: i64,
    order_of_issue_types: OrderOfIssueTypes,
}

impl<'a> ReorderIssueTypesInIssueTypeSchemeRequest<'a> {
    fn new(
        client: &'a crate::core::Client,
        issue_type_scheme_id: i64,
        order_of_issue_types: OrderOfIssueTypes,
    ) -> Self {
        Self { client, issue_type_scheme_id, order_of_issue_types }
    }

    /// The request as the transport will send it.
    pub fn config(&self) -> crate::core::Result<crate::core::RequestConfig> {
        let mut config = crate::core::RequestConfig::new(
            crate::core::Method::PUT,
            format!("/rest/api/3/issuetypescheme/{}/issuetype/move", self.issue_type_scheme_id),
        );

        let body = match serde_json::to_value(&self.order_of_issue_types)? {
            serde_json::Value::Object(object) => object,
            _ => serde_json::Map::new(),
        };

        config.body = Some(crate::core::Body::Json(serde_json::Value::Object(body)));

        Ok(config)
    }

    /// Sends the request.
    pub async fn send(self) -> crate::core::Result<()> {
        self.client.send_empty(&self.config()?).await
    }

    /// Sends the request and hands back the body unmodelled.
    pub async fn send_raw(self) -> crate::core::Result<serde_json::Value> {
        self.client.send_raw(&self.config()?).await
    }
}

/// Removes an issue type from an issue type scheme.
///
/// This operation cannot remove:
///
///  *  any issue type used by issues.
///  *  any issue types from the default issue type scheme.
///  *  the last standard issue type from an issue type scheme.
///
/// **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
pub struct RemoveIssueTypeFromIssueTypeSchemeRequest<'a> {
    client: &'a crate::core::Client,
    issue_type_scheme_id: i64,
    issue_type_id: i64,
}

impl<'a> RemoveIssueTypeFromIssueTypeSchemeRequest<'a> {
    fn new(client: &'a crate::core::Client, issue_type_scheme_id: i64, issue_type_id: i64) -> Self {
        Self { client, issue_type_scheme_id, issue_type_id }
    }

    /// The request as the transport will send it.
    pub fn config(&self) -> crate::core::Result<crate::core::RequestConfig> {
        let config = crate::core::RequestConfig::new(
            crate::core::Method::DELETE,
            format!("/rest/api/3/issuetypescheme/{}/issuetype/{}", self.issue_type_scheme_id, self.issue_type_id),
        );

        Ok(config)
    }

    /// Sends the request.
    pub async fn send(self) -> crate::core::Result<()> {
        self.client.send_empty(&self.config()?).await
    }

    /// Sends the request and hands back the body unmodelled.
    pub async fn send_raw(self) -> crate::core::Result<serde_json::Value> {
        self.client.send_raw(&self.config()?).await
    }
}
