// @generated by apis-code-gen. Do not edit: change the generator or the specification.

use super::super::models::*;

crate::open_enum! {
    /// The avatar type.
    pub enum GetAllSystemAvatarsRequestType {
        Issuetype => "issuetype",
        Project => "project",
        User => "user",
        Priority => "priority",
    }
}

crate::open_enum! {
    /// The avatar type.
    pub enum GetAvatarsRequestType {
        Project => "project",
        Issuetype => "issuetype",
        Priority => "priority",
    }
}

crate::open_enum! {
    /// The avatar type.
    pub enum StoreAvatarRequestType {
        Project => "project",
        Issuetype => "issuetype",
        Priority => "priority",
    }
}

crate::open_enum! {
    /// The avatar type.
    pub enum DeleteAvatarRequestType {
        Project => "project",
        Issuetype => "issuetype",
        Priority => "priority",
    }
}

crate::open_enum! {
    /// The icon type of the avatar.
    pub enum GetAvatarImageByTypeRequestType {
        Issuetype => "issuetype",
        Project => "project",
        Priority => "priority",
    }
}

crate::open_enum! {
    /// The size of the avatar image. If not provided the default size is returned.
    pub enum GetAvatarImageByTypeRequestSize {
        Xsmall => "xsmall",
        Small => "small",
        Medium => "medium",
        Large => "large",
        Xlarge => "xlarge",
    }
}

crate::open_enum! {
    /// The format to return the avatar image in. If not provided the original content format is returned.
    pub enum GetAvatarImageByTypeRequestFormat {
        Png => "png",
        Svg => "svg",
    }
}

crate::open_enum! {
    /// The icon type of the avatar.
    pub enum GetAvatarImageByIDRequestType {
        Issuetype => "issuetype",
        Project => "project",
        Priority => "priority",
    }
}

crate::open_enum! {
    /// The size of the avatar image. If not provided the default size is returned.
    pub enum GetAvatarImageByIDRequestSize {
        Xsmall => "xsmall",
        Small => "small",
        Medium => "medium",
        Large => "large",
        Xlarge => "xlarge",
    }
}

crate::open_enum! {
    /// The format to return the avatar image in. If not provided the original content format is returned.
    pub enum GetAvatarImageByIDRequestFormat {
        Png => "png",
        Svg => "svg",
    }
}

crate::open_enum! {
    /// The icon type of the avatar.
    pub enum GetAvatarImageByOwnerRequestType {
        Issuetype => "issuetype",
        Project => "project",
        Priority => "priority",
    }
}

crate::open_enum! {
    /// The size of the avatar image. If not provided the default size is returned.
    pub enum GetAvatarImageByOwnerRequestSize {
        Xsmall => "xsmall",
        Small => "small",
        Medium => "medium",
        Large => "large",
        Xlarge => "xlarge",
    }
}

crate::open_enum! {
    /// The format to return the avatar image in. If not provided the original content format is returned.
    pub enum GetAvatarImageByOwnerRequestFormat {
        Png => "png",
        Svg => "svg",
    }
}

/// The Avatars operations.
pub struct AvatarsService<'a> {
    client: &'a crate::core::Client,
}

impl<'a> AvatarsService<'a> {
    pub(crate) fn new(client: &'a crate::core::Client) -> Self {
        Self { client }
    }

    /// Returns a list of system avatar details by owner type, where the owner types are issue type, project, user or priority.
    ///
    /// This operation can be accessed anonymously.
    ///
    /// **[Permissions](#permissions) required:** None.
    pub fn get_all_system_avatars(
        &self,
        r#type: impl Into<GetAllSystemAvatarsRequestType>,
    ) -> GetAllSystemAvatarsRequest<'a> {
        GetAllSystemAvatarsRequest::new(self.client, r#type)
    }

    /// Returns the system and custom avatars for a project, issue type or priority.
    ///
    /// This operation can be accessed anonymously.
    ///
    /// **[Permissions](#permissions) required:**
    ///
    ///  *  for custom project avatars, *Browse projects* [project permission](https://confluence.atlassian.com/x/yodKLg) for the project the avatar belongs to.
    ///  *  for custom issue type avatars, *Browse projects* [project permission](https://confluence.atlassian.com/x/yodKLg) for at least one project the issue type is used in.
    ///  *  for system avatars, none.
    ///  *  for priority avatars, none.
    pub fn get_avatars(
        &self,
        r#type: impl Into<GetAvatarsRequestType>,
        entity_id: impl Into<String>,
    ) -> GetAvatarsRequest<'a> {
        GetAvatarsRequest::new(self.client, r#type, entity_id)
    }

    /// Loads a custom avatar for a project, issue type or priority.
    ///
    /// Specify the avatar's local file location in the body of the request. Also, include the following headers:
    ///
    ///  *  `X-Atlassian-Token: no-check` To prevent XSRF protection blocking the request, for more information see [Special Headers](#special-request-headers).
    ///  *  `Content-Type: image/image type` Valid image types are JPEG, GIF, or PNG.
    ///
    /// For example:
    /// `curl --request POST `
    ///
    /// `--user email@example.com: `
    ///
    /// `--header 'X-Atlassian-Token: no-check' `
    ///
    /// `--header 'Content-Type: image/< image_type>' `
    ///
    /// `--data-binary "<@/path/to/file/with/your/avatar>" `
    ///
    /// `--url 'https://your-domain.atlassian.net/rest/api/3/universal_avatar/type/{type}/owner/{entityId}'`
    ///
    /// The avatar is cropped to a square. If no crop parameters are specified, the square originates at the top left of the image. The length of the square's sides is set to the smaller of the height or width of the image.
    ///
    /// The cropped image is then used to create avatars of 16x16, 24x24, 32x32, and 48x48 in size.
    ///
    /// After creating the avatar use:
    ///
    ///  *  [Update issue type](#api-rest-api-3-issuetype-id-put) to set it as the issue type's displayed avatar.
    ///  *  [Set project avatar](#api-rest-api-3-project-projectIdOrKey-avatar-put) to set it as the project's displayed avatar.
    ///  *  [Update priority](#api-rest-api-3-priority-id-put) to set it as the priority's displayed avatar.
    ///
    /// **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
    pub fn store_avatar(
        &self,
        r#type: impl Into<StoreAvatarRequestType>,
        entity_id: impl Into<String>,
        size: i64,
        body: impl Into<bytes::Bytes>,
    ) -> StoreAvatarRequest<'a> {
        StoreAvatarRequest::new(self.client, r#type, entity_id, size, body)
    }

    /// Deletes an avatar from a project, issue type or priority.
    ///
    /// **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
    pub fn delete_avatar(
        &self,
        r#type: impl Into<DeleteAvatarRequestType>,
        owning_object_id: impl Into<String>,
        id: i64,
    ) -> DeleteAvatarRequest<'a> {
        DeleteAvatarRequest::new(self.client, r#type, owning_object_id, id)
    }

    /// Returns the default project, issue type or priority avatar image.
    ///
    /// This operation can be accessed anonymously.
    ///
    /// **[Permissions](#permissions) required:** None.
    pub fn get_avatar_image_by_type(
        &self,
        r#type: impl Into<GetAvatarImageByTypeRequestType>,
    ) -> GetAvatarImageByTypeRequest<'a> {
        GetAvatarImageByTypeRequest::new(self.client, r#type)
    }

    /// Returns a project, issue type or priority avatar image by ID.
    ///
    /// This operation can be accessed anonymously.
    ///
    /// **[Permissions](#permissions) required:**
    ///
    ///  *  For system avatars, none.
    ///  *  For custom project avatars, *Browse projects* [project permission](https://confluence.atlassian.com/x/yodKLg) for the project the avatar belongs to.
    ///  *  For custom issue type avatars, *Browse projects* [project permission](https://confluence.atlassian.com/x/yodKLg) for at least one project the issue type is used in.
    ///  *  For priority avatars, none.
    pub fn get_avatar_image_by_id(
        &self,
        r#type: impl Into<GetAvatarImageByIDRequestType>,
        id: i64,
    ) -> GetAvatarImageByIDRequest<'a> {
        GetAvatarImageByIDRequest::new(self.client, r#type, id)
    }

    /// Returns the avatar image for a project, issue type or priority.
    ///
    /// This operation can be accessed anonymously.
    ///
    /// **[Permissions](#permissions) required:**
    ///
    ///  *  For system avatars, none.
    ///  *  For custom project avatars, *Browse projects* [project permission](https://confluence.atlassian.com/x/yodKLg) for the project the avatar belongs to.
    ///  *  For custom issue type avatars, *Browse projects* [project permission](https://confluence.atlassian.com/x/yodKLg) for at least one project the issue type is used in.
    ///  *  For priority avatars, none.
    pub fn get_avatar_image_by_owner(
        &self,
        r#type: impl Into<GetAvatarImageByOwnerRequestType>,
        entity_id: impl Into<String>,
    ) -> GetAvatarImageByOwnerRequest<'a> {
        GetAvatarImageByOwnerRequest::new(self.client, r#type, entity_id)
    }
}

/// Returns a list of system avatar details by owner type, where the owner types are issue type, project, user or priority.
///
/// This operation can be accessed anonymously.
///
/// **[Permissions](#permissions) required:** None.
pub struct GetAllSystemAvatarsRequest<'a> {
    client: &'a crate::core::Client,
    r#type: GetAllSystemAvatarsRequestType,
}

impl<'a> GetAllSystemAvatarsRequest<'a> {
    fn new(client: &'a crate::core::Client, r#type: impl Into<GetAllSystemAvatarsRequestType>) -> Self {
        Self { client, r#type: r#type.into() }
    }

    /// The request as the transport will send it.
    pub fn config(&self) -> crate::core::Result<crate::core::RequestConfig> {
        let config = crate::core::RequestConfig::new(
            crate::core::Method::GET,
            format!("/rest/api/3/avatar/{}/system", self.r#type),
        );

        Ok(config)
    }

    /// Sends the request.
    pub async fn send(self) -> crate::core::Result<SystemAvatars> {
        self.client.send(&self.config()?).await
    }

    /// Sends the request and hands back the body unmodelled.
    pub async fn send_raw(self) -> crate::core::Result<serde_json::Value> {
        self.client.send_raw(&self.config()?).await
    }
}

/// Returns the system and custom avatars for a project, issue type or priority.
///
/// This operation can be accessed anonymously.
///
/// **[Permissions](#permissions) required:**
///
///  *  for custom project avatars, *Browse projects* [project permission](https://confluence.atlassian.com/x/yodKLg) for the project the avatar belongs to.
///  *  for custom issue type avatars, *Browse projects* [project permission](https://confluence.atlassian.com/x/yodKLg) for at least one project the issue type is used in.
///  *  for system avatars, none.
///  *  for priority avatars, none.
pub struct GetAvatarsRequest<'a> {
    client: &'a crate::core::Client,
    r#type: GetAvatarsRequestType,
    entity_id: String,
}

impl<'a> GetAvatarsRequest<'a> {
    fn new(
        client: &'a crate::core::Client,
        r#type: impl Into<GetAvatarsRequestType>,
        entity_id: impl Into<String>,
    ) -> Self {
        Self { client, r#type: r#type.into(), entity_id: entity_id.into() }
    }

    /// The request as the transport will send it.
    pub fn config(&self) -> crate::core::Result<crate::core::RequestConfig> {
        let config = crate::core::RequestConfig::new(
            crate::core::Method::GET,
            format!("/rest/api/3/universal_avatar/type/{}/owner/{}", self.r#type, self.entity_id),
        );

        Ok(config)
    }

    /// Sends the request.
    pub async fn send(self) -> crate::core::Result<Avatars> {
        self.client.send(&self.config()?).await
    }

    /// Sends the request and hands back the body unmodelled.
    pub async fn send_raw(self) -> crate::core::Result<serde_json::Value> {
        self.client.send_raw(&self.config()?).await
    }
}

/// Loads a custom avatar for a project, issue type or priority.
///
/// Specify the avatar's local file location in the body of the request. Also, include the following headers:
///
///  *  `X-Atlassian-Token: no-check` To prevent XSRF protection blocking the request, for more information see [Special Headers](#special-request-headers).
///  *  `Content-Type: image/image type` Valid image types are JPEG, GIF, or PNG.
///
/// For example:
/// `curl --request POST `
///
/// `--user email@example.com: `
///
/// `--header 'X-Atlassian-Token: no-check' `
///
/// `--header 'Content-Type: image/< image_type>' `
///
/// `--data-binary "<@/path/to/file/with/your/avatar>" `
///
/// `--url 'https://your-domain.atlassian.net/rest/api/3/universal_avatar/type/{type}/owner/{entityId}'`
///
/// The avatar is cropped to a square. If no crop parameters are specified, the square originates at the top left of the image. The length of the square's sides is set to the smaller of the height or width of the image.
///
/// The cropped image is then used to create avatars of 16x16, 24x24, 32x32, and 48x48 in size.
///
/// After creating the avatar use:
///
///  *  [Update issue type](#api-rest-api-3-issuetype-id-put) to set it as the issue type's displayed avatar.
///  *  [Set project avatar](#api-rest-api-3-project-projectIdOrKey-avatar-put) to set it as the project's displayed avatar.
///  *  [Update priority](#api-rest-api-3-priority-id-put) to set it as the priority's displayed avatar.
///
/// **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
pub struct StoreAvatarRequest<'a> {
    client: &'a crate::core::Client,
    r#type: StoreAvatarRequestType,
    entity_id: String,
    x: Option<i64>,
    y: Option<i64>,
    size: i64,
    body: bytes::Bytes,
    content_type: Option<String>,
}

impl<'a> StoreAvatarRequest<'a> {
    fn new(
        client: &'a crate::core::Client,
        r#type: impl Into<StoreAvatarRequestType>,
        entity_id: impl Into<String>,
        size: i64,
        body: impl Into<bytes::Bytes>,
    ) -> Self {
        Self {
            client,
            r#type: r#type.into(),
            entity_id: entity_id.into(),
            size,
            body: body.into(),
            x: None,
            y: None,
            content_type: None,
        }
    }

    /// The X coordinate of the top-left corner of the crop region.
    #[must_use]
    pub fn x(mut self, value: i64) -> Self {
        self.x = Some(value);

        self
    }

    /// The Y coordinate of the top-left corner of the crop region.
    #[must_use]
    pub fn y(mut self, value: i64) -> Self {
        self.y = Some(value);

        self
    }

    /// The media type of the bytes being sent, e.g. `image/png`.
    #[must_use]
    pub fn content_type(mut self, value: impl Into<String>) -> Self {
        self.content_type = Some(value.into());

        self
    }

    /// The request as the transport will send it.
    pub fn config(&self) -> crate::core::Result<crate::core::RequestConfig> {
        let mut config = crate::core::RequestConfig::new(
            crate::core::Method::POST,
            format!("/rest/api/3/universal_avatar/type/{}/owner/{}", self.r#type, self.entity_id),
        );

        if let Some(value) = &self.x {
            config.query.push(("x".to_owned(), crate::core::QueryValue::Scalar(value.to_string())));
        }

        if let Some(value) = &self.y {
            config.query.push(("y".to_owned(), crate::core::QueryValue::Scalar(value.to_string())));
        }

        config.query.push(("size".to_owned(), crate::core::QueryValue::Scalar(self.size.to_string())));

        config.headers.push(("X-Atlassian-Token".to_owned(), "no-check".to_owned()));

        config.body = Some(crate::core::Body::Bytes(self.body.clone()));

        config.content_type = self.content_type.clone().or(None);

        Ok(config)
    }

    /// Sends the request.
    pub async fn send(self) -> crate::core::Result<Avatar> {
        self.client.send(&self.config()?).await
    }

    /// Sends the request and hands back the body unmodelled.
    pub async fn send_raw(self) -> crate::core::Result<serde_json::Value> {
        self.client.send_raw(&self.config()?).await
    }
}

/// Deletes an avatar from a project, issue type or priority.
///
/// **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
pub struct DeleteAvatarRequest<'a> {
    client: &'a crate::core::Client,
    r#type: DeleteAvatarRequestType,
    owning_object_id: String,
    id: i64,
}

impl<'a> DeleteAvatarRequest<'a> {
    fn new(
        client: &'a crate::core::Client,
        r#type: impl Into<DeleteAvatarRequestType>,
        owning_object_id: impl Into<String>,
        id: i64,
    ) -> Self {
        Self { client, r#type: r#type.into(), owning_object_id: owning_object_id.into(), id }
    }

    /// The request as the transport will send it.
    pub fn config(&self) -> crate::core::Result<crate::core::RequestConfig> {
        let config = crate::core::RequestConfig::new(
            crate::core::Method::DELETE,
            format!(
                "/rest/api/3/universal_avatar/type/{}/owner/{}/avatar/{}",
                self.r#type, self.owning_object_id, self.id
            ),
        );

        Ok(config)
    }

    /// Sends the request.
    pub async fn send(self) -> crate::core::Result<()> {
        self.client.send_empty(&self.config()?).await
    }

    /// Sends the request and hands back the body unmodelled.
    pub async fn send_raw(self) -> crate::core::Result<serde_json::Value> {
        self.client.send_raw(&self.config()?).await
    }
}

/// Returns the default project, issue type or priority avatar image.
///
/// This operation can be accessed anonymously.
///
/// **[Permissions](#permissions) required:** None.
pub struct GetAvatarImageByTypeRequest<'a> {
    client: &'a crate::core::Client,
    r#type: GetAvatarImageByTypeRequestType,
    size: Option<GetAvatarImageByTypeRequestSize>,
    format: Option<GetAvatarImageByTypeRequestFormat>,
}

impl<'a> GetAvatarImageByTypeRequest<'a> {
    fn new(client: &'a crate::core::Client, r#type: impl Into<GetAvatarImageByTypeRequestType>) -> Self {
        Self { client, r#type: r#type.into(), size: None, format: None }
    }

    /// The size of the avatar image. If not provided the default size is returned.
    #[must_use]
    pub fn size(mut self, value: impl Into<GetAvatarImageByTypeRequestSize>) -> Self {
        self.size = Some(value.into());

        self
    }

    /// The format to return the avatar image in. If not provided the original content format is returned.
    #[must_use]
    pub fn format(mut self, value: impl Into<GetAvatarImageByTypeRequestFormat>) -> Self {
        self.format = Some(value.into());

        self
    }

    /// The request as the transport will send it.
    pub fn config(&self) -> crate::core::Result<crate::core::RequestConfig> {
        let mut config = crate::core::RequestConfig::new(
            crate::core::Method::GET,
            format!("/rest/api/3/universal_avatar/view/type/{}", self.r#type),
        );

        if let Some(value) = &self.size {
            config.query.push(("size".to_owned(), crate::core::QueryValue::from_serializable(value)?));
        }

        if let Some(value) = &self.format {
            config.query.push(("format".to_owned(), crate::core::QueryValue::from_serializable(value)?));
        }

        Ok(config)
    }

    /// Sends the request.
    pub async fn send(self) -> crate::core::Result<bytes::Bytes> {
        self.client.send_bytes(&self.config()?).await
    }

    /// Sends the request and hands back the body unmodelled.
    pub async fn send_raw(self) -> crate::core::Result<serde_json::Value> {
        self.client.send_raw(&self.config()?).await
    }
}

/// Returns a project, issue type or priority avatar image by ID.
///
/// This operation can be accessed anonymously.
///
/// **[Permissions](#permissions) required:**
///
///  *  For system avatars, none.
///  *  For custom project avatars, *Browse projects* [project permission](https://confluence.atlassian.com/x/yodKLg) for the project the avatar belongs to.
///  *  For custom issue type avatars, *Browse projects* [project permission](https://confluence.atlassian.com/x/yodKLg) for at least one project the issue type is used in.
///  *  For priority avatars, none.
pub struct GetAvatarImageByIDRequest<'a> {
    client: &'a crate::core::Client,
    r#type: GetAvatarImageByIDRequestType,
    id: i64,
    size: Option<GetAvatarImageByIDRequestSize>,
    format: Option<GetAvatarImageByIDRequestFormat>,
}

impl<'a> GetAvatarImageByIDRequest<'a> {
    fn new(client: &'a crate::core::Client, r#type: impl Into<GetAvatarImageByIDRequestType>, id: i64) -> Self {
        Self { client, r#type: r#type.into(), id, size: None, format: None }
    }

    /// The size of the avatar image. If not provided the default size is returned.
    #[must_use]
    pub fn size(mut self, value: impl Into<GetAvatarImageByIDRequestSize>) -> Self {
        self.size = Some(value.into());

        self
    }

    /// The format to return the avatar image in. If not provided the original content format is returned.
    #[must_use]
    pub fn format(mut self, value: impl Into<GetAvatarImageByIDRequestFormat>) -> Self {
        self.format = Some(value.into());

        self
    }

    /// The request as the transport will send it.
    pub fn config(&self) -> crate::core::Result<crate::core::RequestConfig> {
        let mut config = crate::core::RequestConfig::new(
            crate::core::Method::GET,
            format!("/rest/api/3/universal_avatar/view/type/{}/avatar/{}", self.r#type, self.id),
        );

        if let Some(value) = &self.size {
            config.query.push(("size".to_owned(), crate::core::QueryValue::from_serializable(value)?));
        }

        if let Some(value) = &self.format {
            config.query.push(("format".to_owned(), crate::core::QueryValue::from_serializable(value)?));
        }

        Ok(config)
    }

    /// Sends the request.
    pub async fn send(self) -> crate::core::Result<bytes::Bytes> {
        self.client.send_bytes(&self.config()?).await
    }

    /// Sends the request and hands back the body unmodelled.
    pub async fn send_raw(self) -> crate::core::Result<serde_json::Value> {
        self.client.send_raw(&self.config()?).await
    }
}

/// Returns the avatar image for a project, issue type or priority.
///
/// This operation can be accessed anonymously.
///
/// **[Permissions](#permissions) required:**
///
///  *  For system avatars, none.
///  *  For custom project avatars, *Browse projects* [project permission](https://confluence.atlassian.com/x/yodKLg) for the project the avatar belongs to.
///  *  For custom issue type avatars, *Browse projects* [project permission](https://confluence.atlassian.com/x/yodKLg) for at least one project the issue type is used in.
///  *  For priority avatars, none.
pub struct GetAvatarImageByOwnerRequest<'a> {
    client: &'a crate::core::Client,
    r#type: GetAvatarImageByOwnerRequestType,
    entity_id: String,
    size: Option<GetAvatarImageByOwnerRequestSize>,
    format: Option<GetAvatarImageByOwnerRequestFormat>,
}

impl<'a> GetAvatarImageByOwnerRequest<'a> {
    fn new(
        client: &'a crate::core::Client,
        r#type: impl Into<GetAvatarImageByOwnerRequestType>,
        entity_id: impl Into<String>,
    ) -> Self {
        Self { client, r#type: r#type.into(), entity_id: entity_id.into(), size: None, format: None }
    }

    /// The size of the avatar image. If not provided the default size is returned.
    #[must_use]
    pub fn size(mut self, value: impl Into<GetAvatarImageByOwnerRequestSize>) -> Self {
        self.size = Some(value.into());

        self
    }

    /// The format to return the avatar image in. If not provided the original content format is returned.
    #[must_use]
    pub fn format(mut self, value: impl Into<GetAvatarImageByOwnerRequestFormat>) -> Self {
        self.format = Some(value.into());

        self
    }

    /// The request as the transport will send it.
    pub fn config(&self) -> crate::core::Result<crate::core::RequestConfig> {
        let mut config = crate::core::RequestConfig::new(
            crate::core::Method::GET,
            format!("/rest/api/3/universal_avatar/view/type/{}/owner/{}", self.r#type, self.entity_id),
        );

        if let Some(value) = &self.size {
            config.query.push(("size".to_owned(), crate::core::QueryValue::from_serializable(value)?));
        }

        if let Some(value) = &self.format {
            config.query.push(("format".to_owned(), crate::core::QueryValue::from_serializable(value)?));
        }

        Ok(config)
    }

    /// Sends the request.
    pub async fn send(self) -> crate::core::Result<bytes::Bytes> {
        self.client.send_bytes(&self.config()?).await
    }

    /// Sends the request and hands back the body unmodelled.
    pub async fn send_raw(self) -> crate::core::Result<serde_json::Value> {
        self.client.send_raw(&self.config()?).await
    }
}
