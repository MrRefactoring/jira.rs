// @generated by apis-code-gen. Do not edit: change the generator or the specification.

use super::super::models::*;

/// The DynamicModules operations.
pub struct DynamicModulesService<'a> {
    client: &'a crate::core::Client,
}

impl<'a> DynamicModulesService<'a> {
    pub(crate) fn new(client: &'a crate::core::Client) -> Self {
        Self { client }
    }

    /// Returns all modules registered dynamically by the calling app.
    ///
    /// **[Permissions](#permissions) required:** Only Connect apps can make this request.
    pub fn get_modules(&self) -> GetModulesRequest<'a> {
        GetModulesRequest::new(self.client)
    }

    /// Registers a list of modules.
    ///
    /// **[Permissions](#permissions) required:** Only Connect apps can make this request.
    pub fn register_modules(&self, connect_modules: ConnectModules) -> RegisterModulesRequest<'a> {
        RegisterModulesRequest::new(self.client, connect_modules)
    }

    /// Remove all or a list of modules registered by the calling app.
    ///
    /// **[Permissions](#permissions) required:** Only Connect apps can make this request.
    pub fn remove_modules(&self) -> RemoveModulesRequest<'a> {
        RemoveModulesRequest::new(self.client)
    }
}

/// Returns all modules registered dynamically by the calling app.
///
/// **[Permissions](#permissions) required:** Only Connect apps can make this request.
pub struct GetModulesRequest<'a> {
    client: &'a crate::core::Client,
}

impl<'a> GetModulesRequest<'a> {
    fn new(client: &'a crate::core::Client) -> Self {
        Self { client }
    }

    /// The request as the transport will send it.
    pub fn config(&self) -> crate::core::Result<crate::core::RequestConfig> {
        let config = crate::core::RequestConfig::new(
            crate::core::Method::GET,
            "/rest/atlassian-connect/1/app/module/dynamic".to_owned(),
        );

        Ok(config)
    }

    /// Sends the request.
    pub async fn send(self) -> crate::core::Result<ConnectModules> {
        self.client.send(&self.config()?).await
    }

    /// Sends the request and hands back the body unmodelled.
    pub async fn send_raw(self) -> crate::core::Result<serde_json::Value> {
        self.client.send_raw(&self.config()?).await
    }
}

/// Registers a list of modules.
///
/// **[Permissions](#permissions) required:** Only Connect apps can make this request.
pub struct RegisterModulesRequest<'a> {
    client: &'a crate::core::Client,
    connect_modules: ConnectModules,
}

impl<'a> RegisterModulesRequest<'a> {
    fn new(client: &'a crate::core::Client, connect_modules: ConnectModules) -> Self {
        Self { client, connect_modules }
    }

    /// The request as the transport will send it.
    pub fn config(&self) -> crate::core::Result<crate::core::RequestConfig> {
        let mut config = crate::core::RequestConfig::new(
            crate::core::Method::POST,
            "/rest/atlassian-connect/1/app/module/dynamic".to_owned(),
        );

        let body = match serde_json::to_value(&self.connect_modules)? {
            serde_json::Value::Object(object) => object,
            _ => serde_json::Map::new(),
        };

        config.body = Some(crate::core::Body::Json(serde_json::Value::Object(body)));

        Ok(config)
    }

    /// Sends the request.
    pub async fn send(self) -> crate::core::Result<()> {
        self.client.send_empty(&self.config()?).await
    }

    /// Sends the request and hands back the body unmodelled.
    pub async fn send_raw(self) -> crate::core::Result<serde_json::Value> {
        self.client.send_raw(&self.config()?).await
    }
}

/// Remove all or a list of modules registered by the calling app.
///
/// **[Permissions](#permissions) required:** Only Connect apps can make this request.
pub struct RemoveModulesRequest<'a> {
    client: &'a crate::core::Client,
    module_key: Option<Vec<String>>,
}

impl<'a> RemoveModulesRequest<'a> {
    fn new(client: &'a crate::core::Client) -> Self {
        Self { client, module_key: None }
    }

    /// The key of the module to remove. To include multiple module keys, provide multiple copies of this parameter.
    /// For example, `moduleKey=dynamic-attachment-entity-property&moduleKey=dynamic-select-field`.
    /// Nonexistent keys are ignored.
    #[must_use]
    pub fn module_key(mut self, value: impl IntoIterator<Item = impl Into<String>>) -> Self {
        self.module_key = Some(value.into_iter().map(Into::into).collect());

        self
    }

    /// The request as the transport will send it.
    pub fn config(&self) -> crate::core::Result<crate::core::RequestConfig> {
        let mut config = crate::core::RequestConfig::new(
            crate::core::Method::DELETE,
            "/rest/atlassian-connect/1/app/module/dynamic".to_owned(),
        );

        if let Some(value) = &self.module_key {
            config.query.push(("moduleKey".to_owned(), crate::core::QueryValue::List(value.clone())));
        }

        Ok(config)
    }

    /// Sends the request.
    pub async fn send(self) -> crate::core::Result<()> {
        self.client.send_empty(&self.config()?).await
    }

    /// Sends the request and hands back the body unmodelled.
    pub async fn send_raw(self) -> crate::core::Result<serde_json::Value> {
        self.client.send_raw(&self.config()?).await
    }
}
