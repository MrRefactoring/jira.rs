// @generated by apis-code-gen. Do not edit: change the generator or the specification.

use super::super::models::*;
use serde::{Deserialize, Serialize};

crate::open_enum! {
    pub enum GetFieldsPaginatedRequestType {
        Custom => "custom",
        System => "system",
    }
}

crate::open_enum! {
    /// [Order](#ordering) the results by:
    ///
    ///  *  `contextsCount` sorts by the number of contexts related to a field
    ///  *  `lastUsed` sorts by the date when the value of the field last changed
    ///  *  `name` sorts by the field name
    ///  *  `screensCount` sorts by the number of screens related to a field
    pub enum GetFieldsPaginatedRequestOrderBy {
        ContextsCount => "contextsCount",
        ContextsCountDescending => "-contextsCount",
        ContextsCountAscending => "+contextsCount",
        LastUsed => "lastUsed",
        LastUsedDescending => "-lastUsed",
        LastUsedAscending => "+lastUsed",
        Name => "name",
        NameDescending => "-name",
        NameAscending => "+name",
        ScreensCount => "screensCount",
        ScreensCountDescending => "-screensCount",
        ScreensCountAscending => "+screensCount",
        ProjectsCount => "projectsCount",
        ProjectsCountDescending => "-projectsCount",
        ProjectsCountAscending => "+projectsCount",
    }
}

crate::open_enum! {
    pub enum GetFieldsPaginatedRequestExpandVariant2 {
        Key => "key",
        StableId => "stableId",
        LastUsed => "lastUsed",
        ScreensCount => "screensCount",
        ContextsCount => "contextsCount",
        IsLocked => "isLocked",
        SearcherKey => "searcherKey",
    }
}

crate::open_enum! {
    pub enum GetFieldsPaginatedRequestExpandUnion3 {
        Key => "key",
        StableId => "stableId",
        LastUsed => "lastUsed",
        ScreensCount => "screensCount",
        ContextsCount => "contextsCount",
        IsLocked => "isLocked",
        SearcherKey => "searcherKey",
    }
}

/// Use [expand](#expansion) to include additional information in the response. This parameter accepts a comma-separated list. Expand options include:
///
///  *  `key` returns the key for each field
///  *  `stableId` returns the stableId for each field
///  *  `lastUsed` returns the date when the value of the field last changed
///  *  `screensCount` returns the number of screens related to a field
///  *  `contextsCount` returns the number of contexts related to a field
///  *  `isLocked` returns information about whether the field is locked
///  *  `searcherKey` returns the searcher key for each custom field
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
#[allow(clippy::large_enum_variant)]
pub enum GetFieldsPaginatedRequestExpand {
    Variant0(String),
    Variant1(Vec<String>),
    Variant2(GetFieldsPaginatedRequestExpandVariant2),
    Variant3(Vec<GetFieldsPaginatedRequestExpandUnion3>),
}

crate::open_enum! {
    pub enum GetTrashedFieldsPaginatedRequestExpand {
        Name => "name",
        NameDescending => "-name",
        NameAscending => "+name",
        TrashDate => "trashDate",
        TrashDateDescending => "-trashDate",
        TrashDateAscending => "+trashDate",
        PlannedDeletionDate => "plannedDeletionDate",
        PlannedDeletionDateDescending => "-plannedDeletionDate",
        PlannedDeletionDateAscending => "+plannedDeletionDate",
        ProjectsCount => "projectsCount",
        ProjectsCountDescending => "-projectsCount",
        ProjectsCountAscending => "+projectsCount",
    }
}

crate::open_enum! {
    /// [Order](#ordering) the results by a field:
    ///
    ///  *  `name` sorts by the field name
    ///  *  `trashDate` sorts by the date the field was moved to the trash
    ///  *  `plannedDeletionDate` sorts by the planned deletion date
    pub enum GetTrashedFieldsPaginatedRequestOrderBy {
        Name => "name",
        NameDescending => "-name",
        NameAscending => "+name",
        TrashDate => "trashDate",
        TrashDateDescending => "-trashDate",
        TrashDateAscending => "+trashDate",
        PlannedDeletionDate => "plannedDeletionDate",
        PlannedDeletionDateDescending => "-plannedDeletionDate",
        PlannedDeletionDateAscending => "+plannedDeletionDate",
    }
}

/// The IssueFields operations.
pub struct IssueFieldsService<'a> {
    client: &'a crate::core::Client,
}

impl<'a> IssueFieldsService<'a> {
    pub(crate) fn new(client: &'a crate::core::Client) -> Self {
        Self { client }
    }

    /// Returns system and custom issue fields according to the following rules:
    ///
    ///  *  Fields that cannot be added to the issue navigator are always returned.
    ///  *  Fields that cannot be placed on an issue screen are always returned.
    ///  *  Fields that depend on global Jira settings are only returned if the setting is enabled. That is, timetracking fields, subtasks, votes, and watches.
    ///  *  Fields that are not associated to any used field configurations or screens are not returned.
    ///  *  For all other fields, this operation only returns the fields that the user has permission to view (that is, the field is used in at least one project that the user has *Browse Projects* [project permission](https://confluence.atlassian.com/x/yodKLg) for.)
    ///
    /// This operation can be accessed anonymously.
    ///
    /// **[Permissions](#permissions) required:** None.
    pub fn get_fields(&self) -> GetFieldsRequest<'a> {
        GetFieldsRequest::new(self.client)
    }

    /// Creates a custom field.
    ///
    /// **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
    pub fn create_custom_field(&self, custom_field_definition: CustomFieldDefinition) -> CreateCustomFieldRequest2<'a> {
        CreateCustomFieldRequest2::new(self.client, custom_field_definition)
    }

    /// Returns a [paginated](#pagination) list of fields for Classic Jira projects. The list can include:
    ///
    ///  *  all fields
    ///  *  specific fields, by defining `id`
    ///  *  fields that contain a string in the field name or description, by defining `query`
    ///  *  specific fields that contain a string in the field name or description, by defining `id` and `query`
    ///
    /// Use `type` must be set to `custom` to show custom fields only.
    ///
    /// **[Permissions](#permissions) required:** Permission to access Jira.
    pub fn get_fields_paginated(&self) -> GetFieldsPaginatedRequest<'a> {
        GetFieldsPaginatedRequest::new(self.client)
    }

    /// Returns a [paginated](#pagination) list of fields in the trash. The list may be restricted to fields whose field name or description partially match a string.
    ///
    /// Only custom fields can be queried, `type` must be set to `custom`.
    ///
    /// **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
    pub fn get_trashed_fields_paginated(&self) -> GetTrashedFieldsPaginatedRequest<'a> {
        GetTrashedFieldsPaginatedRequest::new(self.client)
    }

    /// Updates a custom field.
    ///
    /// **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
    pub fn update_custom_field(
        &self,
        field_id: impl Into<String>,
        update_custom_field_details: UpdateCustomFieldDetails,
    ) -> UpdateCustomFieldRequest<'a> {
        UpdateCustomFieldRequest::new(self.client, field_id, update_custom_field_details)
    }

    /// Deletes a custom field. The custom field is deleted whether it is in the trash or not. See [Edit or delete a custom field](https://confluence.atlassian.com/x/Z44fOw) for more information on trashing and deleting custom fields.
    ///
    /// This operation is [asynchronous](#async). Follow the `location` link in the response to determine the status of the task and use [Get task](#api-rest-api-3-task-taskId-get) to obtain subsequent updates.
    ///
    /// **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
    pub fn delete_custom_field(&self, id: impl Into<String>) -> DeleteCustomFieldRequest<'a> {
        DeleteCustomFieldRequest::new(self.client, id)
    }

    /// Restores a custom field from trash. See [Edit or delete a custom field](https://confluence.atlassian.com/x/Z44fOw) for more information on trashing and deleting custom fields.
    ///
    /// **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
    pub fn restore_custom_field(&self, id: impl Into<String>) -> RestoreCustomFieldRequest<'a> {
        RestoreCustomFieldRequest::new(self.client, id)
    }

    /// Moves a custom field to trash. See [Edit or delete a custom field](https://confluence.atlassian.com/x/Z44fOw) for more information on trashing and deleting custom fields.
    ///
    /// **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
    pub fn trash_custom_field(&self, id: impl Into<String>) -> TrashCustomFieldRequest<'a> {
        TrashCustomFieldRequest::new(self.client, id)
    }
}

/// Returns system and custom issue fields according to the following rules:
///
///  *  Fields that cannot be added to the issue navigator are always returned.
///  *  Fields that cannot be placed on an issue screen are always returned.
///  *  Fields that depend on global Jira settings are only returned if the setting is enabled. That is, timetracking fields, subtasks, votes, and watches.
///  *  Fields that are not associated to any used field configurations or screens are not returned.
///  *  For all other fields, this operation only returns the fields that the user has permission to view (that is, the field is used in at least one project that the user has *Browse Projects* [project permission](https://confluence.atlassian.com/x/yodKLg) for.)
///
/// This operation can be accessed anonymously.
///
/// **[Permissions](#permissions) required:** None.
pub struct GetFieldsRequest<'a> {
    client: &'a crate::core::Client,
}

impl<'a> GetFieldsRequest<'a> {
    fn new(client: &'a crate::core::Client) -> Self {
        Self { client }
    }

    /// The request as the transport will send it.
    pub fn config(&self) -> crate::core::Result<crate::core::RequestConfig> {
        let config = crate::core::RequestConfig::new(crate::core::Method::GET, "/rest/api/3/field".to_owned());

        Ok(config)
    }

    /// Sends the request.
    pub async fn send(self) -> crate::core::Result<Vec<FieldDetails>> {
        self.client.send(&self.config()?).await
    }

    /// Sends the request and hands back the body unmodelled.
    pub async fn send_raw(self) -> crate::core::Result<serde_json::Value> {
        self.client.send_raw(&self.config()?).await
    }
}

/// Creates a custom field.
///
/// **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
pub struct CreateCustomFieldRequest2<'a> {
    client: &'a crate::core::Client,
    custom_field_definition: CustomFieldDefinition,
}

impl<'a> CreateCustomFieldRequest2<'a> {
    fn new(client: &'a crate::core::Client, custom_field_definition: CustomFieldDefinition) -> Self {
        Self { client, custom_field_definition }
    }

    /// The request as the transport will send it.
    pub fn config(&self) -> crate::core::Result<crate::core::RequestConfig> {
        let mut config = crate::core::RequestConfig::new(crate::core::Method::POST, "/rest/api/3/field".to_owned());

        let body = match serde_json::to_value(&self.custom_field_definition)? {
            serde_json::Value::Object(object) => object,
            _ => serde_json::Map::new(),
        };

        config.body = Some(crate::core::Body::Json(serde_json::Value::Object(body)));

        Ok(config)
    }

    /// Sends the request.
    pub async fn send(self) -> crate::core::Result<FieldDetails> {
        self.client.send(&self.config()?).await
    }

    /// Sends the request and hands back the body unmodelled.
    pub async fn send_raw(self) -> crate::core::Result<serde_json::Value> {
        self.client.send_raw(&self.config()?).await
    }
}

/// Returns a [paginated](#pagination) list of fields for Classic Jira projects. The list can include:
///
///  *  all fields
///  *  specific fields, by defining `id`
///  *  fields that contain a string in the field name or description, by defining `query`
///  *  specific fields that contain a string in the field name or description, by defining `id` and `query`
///
/// Use `type` must be set to `custom` to show custom fields only.
///
/// **[Permissions](#permissions) required:** Permission to access Jira.
pub struct GetFieldsPaginatedRequest<'a> {
    client: &'a crate::core::Client,
    start_at: Option<i64>,
    max_results: Option<i64>,
    r#type: Option<Vec<GetFieldsPaginatedRequestType>>,
    id: Option<Vec<String>>,
    query: Option<String>,
    order_by: Option<GetFieldsPaginatedRequestOrderBy>,
    expand: Option<GetFieldsPaginatedRequestExpand>,
    project_ids: Option<Vec<i64>>,
}

impl<'a> GetFieldsPaginatedRequest<'a> {
    fn new(client: &'a crate::core::Client) -> Self {
        Self {
            client,
            start_at: None,
            max_results: None,
            r#type: None,
            id: None,
            query: None,
            order_by: None,
            expand: None,
            project_ids: None,
        }
    }

    /// The index of the first item to return in a page of results (page offset).
    #[must_use]
    pub fn start_at(mut self, value: i64) -> Self {
        self.start_at = Some(value);

        self
    }

    /// The maximum number of items to return per page.
    #[must_use]
    pub fn max_results(mut self, value: i64) -> Self {
        self.max_results = Some(value);

        self
    }

    /// The type of fields to search.
    #[must_use]
    pub fn r#type(mut self, value: impl IntoIterator<Item = impl Into<GetFieldsPaginatedRequestType>>) -> Self {
        self.r#type = Some(value.into_iter().map(Into::into).collect());

        self
    }

    /// The IDs of the custom fields to return or, where `query` is specified, filter.
    #[must_use]
    pub fn id(mut self, value: impl IntoIterator<Item = impl Into<String>>) -> Self {
        self.id = Some(value.into_iter().map(Into::into).collect());

        self
    }

    /// String used to perform a case-insensitive partial match with field names or descriptions.
    #[must_use]
    pub fn query(mut self, value: impl Into<String>) -> Self {
        self.query = Some(value.into());

        self
    }

    /// [Order](#ordering) the results by:
    ///
    ///  *  `contextsCount` sorts by the number of contexts related to a field
    ///  *  `lastUsed` sorts by the date when the value of the field last changed
    ///  *  `name` sorts by the field name
    ///  *  `screensCount` sorts by the number of screens related to a field
    #[must_use]
    pub fn order_by(mut self, value: impl Into<GetFieldsPaginatedRequestOrderBy>) -> Self {
        self.order_by = Some(value.into());

        self
    }

    /// Use [expand](#expansion) to include additional information in the response. This parameter accepts a comma-separated list. Expand options include:
    ///
    ///  *  `key` returns the key for each field
    ///  *  `stableId` returns the stableId for each field
    ///  *  `lastUsed` returns the date when the value of the field last changed
    ///  *  `screensCount` returns the number of screens related to a field
    ///  *  `contextsCount` returns the number of contexts related to a field
    ///  *  `isLocked` returns information about whether the field is locked
    ///  *  `searcherKey` returns the searcher key for each custom field
    #[must_use]
    pub fn expand(mut self, value: GetFieldsPaginatedRequestExpand) -> Self {
        self.expand = Some(value);

        self
    }

    /// The IDs of the projects to filter the fields by. Fields belonging to project Ids that the user does not have access to will not be returned
    #[must_use]
    pub fn project_ids(mut self, value: impl IntoIterator<Item = i64>) -> Self {
        self.project_ids = Some(value.into_iter().collect());

        self
    }

    /// The request as the transport will send it.
    pub fn config(&self) -> crate::core::Result<crate::core::RequestConfig> {
        let mut config =
            crate::core::RequestConfig::new(crate::core::Method::GET, "/rest/api/3/field/search".to_owned());

        if let Some(value) = &self.start_at {
            config.query.push(("startAt".to_owned(), crate::core::QueryValue::Scalar(value.to_string())));
        }

        if let Some(value) = &self.max_results {
            config.query.push(("maxResults".to_owned(), crate::core::QueryValue::Scalar(value.to_string())));
        }

        if let Some(value) = &self.r#type {
            config.query.push(("type".to_owned(), crate::core::QueryValue::from_serializable(value)?));
        }

        if let Some(value) = &self.id {
            config.query.push(("id".to_owned(), crate::core::QueryValue::List(value.clone())));
        }

        if let Some(value) = &self.query {
            config.query.push(("query".to_owned(), crate::core::QueryValue::Scalar(value.clone())));
        }

        if let Some(value) = &self.order_by {
            config.query.push(("orderBy".to_owned(), crate::core::QueryValue::from_serializable(value)?));
        }

        if let Some(value) = &self.expand {
            config.query.push(("expand".to_owned(), crate::core::QueryValue::from_serializable(value)?));
        }

        if let Some(value) = &self.project_ids {
            config.query.push(("projectIds".to_owned(), crate::core::QueryValue::from_serializable(value)?));
        }

        Ok(config)
    }

    /// Sends the request.
    pub async fn send(self) -> crate::core::Result<Page<Field>> {
        self.client.send(&self.config()?).await
    }

    /// Sends the request and hands back the body unmodelled.
    pub async fn send_raw(self) -> crate::core::Result<serde_json::Value> {
        self.client.send_raw(&self.config()?).await
    }
}

/// Returns a [paginated](#pagination) list of fields in the trash. The list may be restricted to fields whose field name or description partially match a string.
///
/// Only custom fields can be queried, `type` must be set to `custom`.
///
/// **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
pub struct GetTrashedFieldsPaginatedRequest<'a> {
    client: &'a crate::core::Client,
    start_at: Option<i64>,
    max_results: Option<i64>,
    id: Option<Vec<String>>,
    query: Option<String>,
    expand: Option<GetTrashedFieldsPaginatedRequestExpand>,
    order_by: Option<GetTrashedFieldsPaginatedRequestOrderBy>,
}

impl<'a> GetTrashedFieldsPaginatedRequest<'a> {
    fn new(client: &'a crate::core::Client) -> Self {
        Self { client, start_at: None, max_results: None, id: None, query: None, expand: None, order_by: None }
    }

    /// The index of the first item to return in a page of results (page offset).
    #[must_use]
    pub fn start_at(mut self, value: i64) -> Self {
        self.start_at = Some(value);

        self
    }

    /// The maximum number of items to return per page.
    #[must_use]
    pub fn max_results(mut self, value: i64) -> Self {
        self.max_results = Some(value);

        self
    }

    #[must_use]
    pub fn id(mut self, value: impl IntoIterator<Item = impl Into<String>>) -> Self {
        self.id = Some(value.into_iter().map(Into::into).collect());

        self
    }

    /// String used to perform a case-insensitive partial match with field names or descriptions.
    #[must_use]
    pub fn query(mut self, value: impl Into<String>) -> Self {
        self.query = Some(value.into());

        self
    }

    #[must_use]
    pub fn expand(mut self, value: impl Into<GetTrashedFieldsPaginatedRequestExpand>) -> Self {
        self.expand = Some(value.into());

        self
    }

    /// [Order](#ordering) the results by a field:
    ///
    ///  *  `name` sorts by the field name
    ///  *  `trashDate` sorts by the date the field was moved to the trash
    ///  *  `plannedDeletionDate` sorts by the planned deletion date
    #[must_use]
    pub fn order_by(mut self, value: impl Into<GetTrashedFieldsPaginatedRequestOrderBy>) -> Self {
        self.order_by = Some(value.into());

        self
    }

    /// The request as the transport will send it.
    pub fn config(&self) -> crate::core::Result<crate::core::RequestConfig> {
        let mut config =
            crate::core::RequestConfig::new(crate::core::Method::GET, "/rest/api/3/field/search/trashed".to_owned());

        if let Some(value) = &self.start_at {
            config.query.push(("startAt".to_owned(), crate::core::QueryValue::Scalar(value.to_string())));
        }

        if let Some(value) = &self.max_results {
            config.query.push(("maxResults".to_owned(), crate::core::QueryValue::Scalar(value.to_string())));
        }

        if let Some(value) = &self.id {
            config.query.push(("id".to_owned(), crate::core::QueryValue::List(value.clone())));
        }

        if let Some(value) = &self.query {
            config.query.push(("query".to_owned(), crate::core::QueryValue::Scalar(value.clone())));
        }

        if let Some(value) = &self.expand {
            config.query.push(("expand".to_owned(), crate::core::QueryValue::from_serializable(value)?));
        }

        if let Some(value) = &self.order_by {
            config.query.push(("orderBy".to_owned(), crate::core::QueryValue::from_serializable(value)?));
        }

        Ok(config)
    }

    /// Sends the request.
    pub async fn send(self) -> crate::core::Result<Page<Field>> {
        self.client.send(&self.config()?).await
    }

    /// Sends the request and hands back the body unmodelled.
    pub async fn send_raw(self) -> crate::core::Result<serde_json::Value> {
        self.client.send_raw(&self.config()?).await
    }
}

/// Updates a custom field.
///
/// **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
pub struct UpdateCustomFieldRequest<'a> {
    client: &'a crate::core::Client,
    field_id: String,
    update_custom_field_details: UpdateCustomFieldDetails,
}

impl<'a> UpdateCustomFieldRequest<'a> {
    fn new(
        client: &'a crate::core::Client,
        field_id: impl Into<String>,
        update_custom_field_details: UpdateCustomFieldDetails,
    ) -> Self {
        Self { client, field_id: field_id.into(), update_custom_field_details }
    }

    /// The request as the transport will send it.
    pub fn config(&self) -> crate::core::Result<crate::core::RequestConfig> {
        let mut config =
            crate::core::RequestConfig::new(crate::core::Method::PUT, format!("/rest/api/3/field/{}", self.field_id));

        let body = match serde_json::to_value(&self.update_custom_field_details)? {
            serde_json::Value::Object(object) => object,
            _ => serde_json::Map::new(),
        };

        config.body = Some(crate::core::Body::Json(serde_json::Value::Object(body)));

        Ok(config)
    }

    /// Sends the request.
    pub async fn send(self) -> crate::core::Result<()> {
        self.client.send_empty(&self.config()?).await
    }

    /// Sends the request and hands back the body unmodelled.
    pub async fn send_raw(self) -> crate::core::Result<serde_json::Value> {
        self.client.send_raw(&self.config()?).await
    }
}

/// Deletes a custom field. The custom field is deleted whether it is in the trash or not. See [Edit or delete a custom field](https://confluence.atlassian.com/x/Z44fOw) for more information on trashing and deleting custom fields.
///
/// This operation is [asynchronous](#async). Follow the `location` link in the response to determine the status of the task and use [Get task](#api-rest-api-3-task-taskId-get) to obtain subsequent updates.
///
/// **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
pub struct DeleteCustomFieldRequest<'a> {
    client: &'a crate::core::Client,
    id: String,
}

impl<'a> DeleteCustomFieldRequest<'a> {
    fn new(client: &'a crate::core::Client, id: impl Into<String>) -> Self {
        Self { client, id: id.into() }
    }

    /// The request as the transport will send it.
    pub fn config(&self) -> crate::core::Result<crate::core::RequestConfig> {
        let config =
            crate::core::RequestConfig::new(crate::core::Method::DELETE, format!("/rest/api/3/field/{}", self.id));

        Ok(config)
    }

    /// Sends the request.
    pub async fn send(self) -> crate::core::Result<TaskProgressObject> {
        self.client.send(&self.config()?).await
    }

    /// Sends the request and hands back the body unmodelled.
    pub async fn send_raw(self) -> crate::core::Result<serde_json::Value> {
        self.client.send_raw(&self.config()?).await
    }
}

/// Restores a custom field from trash. See [Edit or delete a custom field](https://confluence.atlassian.com/x/Z44fOw) for more information on trashing and deleting custom fields.
///
/// **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
pub struct RestoreCustomFieldRequest<'a> {
    client: &'a crate::core::Client,
    id: String,
}

impl<'a> RestoreCustomFieldRequest<'a> {
    fn new(client: &'a crate::core::Client, id: impl Into<String>) -> Self {
        Self { client, id: id.into() }
    }

    /// The request as the transport will send it.
    pub fn config(&self) -> crate::core::Result<crate::core::RequestConfig> {
        let config = crate::core::RequestConfig::new(
            crate::core::Method::POST,
            format!("/rest/api/3/field/{}/restore", self.id),
        );

        Ok(config)
    }

    /// Sends the request.
    pub async fn send(self) -> crate::core::Result<()> {
        self.client.send_empty(&self.config()?).await
    }

    /// Sends the request and hands back the body unmodelled.
    pub async fn send_raw(self) -> crate::core::Result<serde_json::Value> {
        self.client.send_raw(&self.config()?).await
    }
}

/// Moves a custom field to trash. See [Edit or delete a custom field](https://confluence.atlassian.com/x/Z44fOw) for more information on trashing and deleting custom fields.
///
/// **[Permissions](#permissions) required:** *Administer Jira* [global permission](https://confluence.atlassian.com/x/x4dKLg).
pub struct TrashCustomFieldRequest<'a> {
    client: &'a crate::core::Client,
    id: String,
}

impl<'a> TrashCustomFieldRequest<'a> {
    fn new(client: &'a crate::core::Client, id: impl Into<String>) -> Self {
        Self { client, id: id.into() }
    }

    /// The request as the transport will send it.
    pub fn config(&self) -> crate::core::Result<crate::core::RequestConfig> {
        let config =
            crate::core::RequestConfig::new(crate::core::Method::POST, format!("/rest/api/3/field/{}/trash", self.id));

        Ok(config)
    }

    /// Sends the request.
    pub async fn send(self) -> crate::core::Result<()> {
        self.client.send_empty(&self.config()?).await
    }

    /// Sends the request and hands back the body unmodelled.
    pub async fn send_raw(self) -> crate::core::Result<serde_json::Value> {
        self.client.send_raw(&self.config()?).await
    }
}
