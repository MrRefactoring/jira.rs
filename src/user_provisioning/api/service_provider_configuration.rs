// @generated by apis-code-gen. Do not edit: change the generator or the specification.

/// The ServiceProviderConfiguration operations.
pub struct ServiceProviderConfigurationService<'a> {
    client: &'a crate::core::Client,
}

impl<'a> ServiceProviderConfigurationService<'a> {
    pub(crate) fn new(client: &'a crate::core::Client) -> Self {
        Self { client }
    }

    /// Get different types of resources available on a SCIM service provider (e.g., Users and Groups).
    /// **Note:** This API does not support filtering, pagination, or sorting.
    pub fn get_resource_types(&self, directory_id: impl Into<String>) -> GetResourceTypesRequest<'a> {
        GetResourceTypesRequest::new(self.client, directory_id)
    }

    /// Retrieves user resource types from the SCIM service provider.
    ///
    /// **Note:** This API does not support filtering, pagination, or sorting.
    pub fn get_user_resource_type(&self, directory_id: impl Into<String>) -> GetUserResourceTypeRequest<'a> {
        GetUserResourceTypeRequest::new(self.client, directory_id)
    }

    /// Retrieves group resource type of this SCIM service provider.
    ///
    /// **Note:** This API does not support filtering, pagination, or sorting.
    pub fn get_group_resource_type(&self, directory_id: impl Into<String>) -> GetGroupResourceTypeRequest<'a> {
        GetGroupResourceTypeRequest::new(self.client, directory_id)
    }

    /// Get metadata about the supported SCIM features. This is a service provider configuration  endpoint providing supported SCIM features.
    ///
    /// **Note:** This API does not support filtering, pagination, or sorting.
    pub fn get_service_provider_config(&self, directory_id: impl Into<String>) -> GetServiceProviderConfigRequest<'a> {
        GetServiceProviderConfigRequest::new(self.client, directory_id)
    }
}

/// Get different types of resources available on a SCIM service provider (e.g., Users and Groups).
/// **Note:** This API does not support filtering, pagination, or sorting.
pub struct GetResourceTypesRequest<'a> {
    client: &'a crate::core::Client,
    directory_id: String,
}

impl<'a> GetResourceTypesRequest<'a> {
    fn new(client: &'a crate::core::Client, directory_id: impl Into<String>) -> Self {
        Self {
            client,
            directory_id: directory_id.into(),
        }
    }

    /// The request as the transport will send it.
    pub fn config(&self) -> crate::core::Result<crate::core::RequestConfig> {
        let config = crate::core::RequestConfig::new(
            crate::core::Method::GET,
            format!("/scim/directory/{}/ResourceTypes", self.directory_id),
        );

        Ok(config)
    }

    /// Sends the request.
    pub async fn send(self) -> crate::core::Result<String> {
        self.client.send(&self.config()?).await
    }

    /// Sends the request and hands back the body unmodelled.
    pub async fn send_raw(self) -> crate::core::Result<serde_json::Value> {
        self.client.send_raw(&self.config()?).await
    }
}

/// Retrieves user resource types from the SCIM service provider.
///
/// **Note:** This API does not support filtering, pagination, or sorting.
pub struct GetUserResourceTypeRequest<'a> {
    client: &'a crate::core::Client,
    directory_id: String,
}

impl<'a> GetUserResourceTypeRequest<'a> {
    fn new(client: &'a crate::core::Client, directory_id: impl Into<String>) -> Self {
        Self {
            client,
            directory_id: directory_id.into(),
        }
    }

    /// The request as the transport will send it.
    pub fn config(&self) -> crate::core::Result<crate::core::RequestConfig> {
        let config = crate::core::RequestConfig::new(
            crate::core::Method::GET,
            format!("/scim/directory/{}/ResourceTypes/User", self.directory_id),
        );

        Ok(config)
    }

    /// Sends the request.
    pub async fn send(self) -> crate::core::Result<String> {
        self.client.send(&self.config()?).await
    }

    /// Sends the request and hands back the body unmodelled.
    pub async fn send_raw(self) -> crate::core::Result<serde_json::Value> {
        self.client.send_raw(&self.config()?).await
    }
}

/// Retrieves group resource type of this SCIM service provider.
///
/// **Note:** This API does not support filtering, pagination, or sorting.
pub struct GetGroupResourceTypeRequest<'a> {
    client: &'a crate::core::Client,
    directory_id: String,
}

impl<'a> GetGroupResourceTypeRequest<'a> {
    fn new(client: &'a crate::core::Client, directory_id: impl Into<String>) -> Self {
        Self {
            client,
            directory_id: directory_id.into(),
        }
    }

    /// The request as the transport will send it.
    pub fn config(&self) -> crate::core::Result<crate::core::RequestConfig> {
        let config = crate::core::RequestConfig::new(
            crate::core::Method::GET,
            format!("/scim/directory/{}/ResourceTypes/Group", self.directory_id),
        );

        Ok(config)
    }

    /// Sends the request.
    pub async fn send(self) -> crate::core::Result<String> {
        self.client.send(&self.config()?).await
    }

    /// Sends the request and hands back the body unmodelled.
    pub async fn send_raw(self) -> crate::core::Result<serde_json::Value> {
        self.client.send_raw(&self.config()?).await
    }
}

/// Get metadata about the supported SCIM features. This is a service provider configuration  endpoint providing supported SCIM features.
///
/// **Note:** This API does not support filtering, pagination, or sorting.
pub struct GetServiceProviderConfigRequest<'a> {
    client: &'a crate::core::Client,
    directory_id: String,
}

impl<'a> GetServiceProviderConfigRequest<'a> {
    fn new(client: &'a crate::core::Client, directory_id: impl Into<String>) -> Self {
        Self {
            client,
            directory_id: directory_id.into(),
        }
    }

    /// The request as the transport will send it.
    pub fn config(&self) -> crate::core::Result<crate::core::RequestConfig> {
        let config = crate::core::RequestConfig::new(
            crate::core::Method::GET,
            format!("/scim/directory/{}/ServiceProviderConfig", self.directory_id),
        );

        Ok(config)
    }

    /// Sends the request.
    pub async fn send(self) -> crate::core::Result<String> {
        self.client.send(&self.config()?).await
    }

    /// Sends the request and hands back the body unmodelled.
    pub async fn send_raw(self) -> crate::core::Result<serde_json::Value> {
        self.client.send_raw(&self.config()?).await
    }
}
